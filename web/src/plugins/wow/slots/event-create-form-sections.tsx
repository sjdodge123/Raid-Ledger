/**
 * This slot provides a WoW-specific form extension point.
 * It's registered as a slot but is currently a no-op placeholder for the registry.
 * Helper functions (getWowVariant, getContentType) are in ../utils.ts.
 */
export function EventCreateFormSections() {
    return null;
}
