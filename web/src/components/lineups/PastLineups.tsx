import type { JSX } from 'react';

/**
 * Past lineups history section — placeholder for now.
 * Will be populated when a list endpoint is available.
 */
export function PastLineups(): JSX.Element {
  return (
    <div className="mt-8">
      <h3 className="text-sm font-semibold text-secondary mb-3">Past Lineups</h3>
      <p className="text-xs text-dim">History coming soon.</p>
    </div>
  );
}
