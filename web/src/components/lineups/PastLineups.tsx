import type { JSX } from 'react';

/**
 * Past lineups history section — placeholder for now.
 * Will be populated when a list endpoint is available.
 */
export function PastLineups(): JSX.Element | null {
  // No list endpoint yet — hide section until data is available
  return null;
}
