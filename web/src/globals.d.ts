/** Injected by Vite at build time from root package.json version */
declare const __APP_VERSION__: string;
/** Per-build token from vite.config.ts `define` — see ndt7-load.ts. */
declare const __BUILD_ID__: string;
