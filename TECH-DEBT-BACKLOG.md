# Tech Debt Backlog

Working document for reviewer findings that don't warrant immediate fixes. Appended by `/build`, `/dispatch`, and `/fix-batch` reviewer reports. Operator triages: file Linear stories for items worth doing, delete the rest.

> **Archive:** sections dated before 2026-07-01 (70 sections, 2026-05-03 → 2026-06-30) were moved verbatim to `TECH-DEBT-BACKLOG-archive.md` on 2026-09-01. Append new entries here as before; do not append to the archive.

**Why this exists:** findings used to be auto-filed as Linear `tech-debt:` stories, which created a self-perpetuating queue (reviewer flags items → Lead files story → next batch picks up the story → reviewer flags more items → loop). Findings now land here instead. Nothing in this file should be acted on by an agent without an explicit operator instruction.

## Format for new entries

Append to the bottom under a new dated section. The Lead does this as part of the batch's commits — no separate PR.

```
### 2026-05-03 — fix/batch-2026-05-02 (PR #XXX)

- **[low]** `path/to/file.ts:42` — short description.
  Suggested: one-line fix idea (optional).
- **[med]** `path/other.tsx` — description.
```

Severities: `crit` should never land here (those are auto-fixed during review or sent back to dev). Use `high` / `med` / `low` / `nit`.

## Operator workflow

1. Read new entries when reviewing the PR diff.
2. For each entry: file a Linear story manually, OR delete the entry, OR leave it for later.
3. Pruning is part of triage — old or duplicate items should be removed, not left to bloat.

## Format for skills that parse this file

A future triage skill (or operator-invoked agent) can rely on:

- Each batch is a level-3 heading: `### YYYY-MM-DD — <branch> (PR #<num>)`. Date is ISO. PR ref optional if the PR doesn't exist yet.
- Each finding is a single bullet starting with `- **[<sev>]**` where `<sev>` ∈ {`high`, `med`, `low`, `nit`}.
- File path follows in single backticks; `path:line` is preferred over `path` alone.
- Description is the rest of the bullet up to optional `Suggested:` line indented two spaces.
- The append marker (`<!-- agents append below this line -->`) divides header from entries — skills should read from there forward.

A skill's job is typically: parse → group duplicates by file path → propose Linear stories grouped by area label → present for operator approval. Never auto-file.

---

<!-- agents append below this line -->

## Reconciliation 2026-06-27 (verify-first sweep — 12 agents)

A 12-agent pass verified all 293 backlog entries against current `main` (tsc clean; PRs through #919). **95 entries (~32%) are RESOLVED** and can be archived; the remaining ~198 are genuine open items (mostly unactioned low/nit code-quality suggestions + real flakes, conservatively kept). Approx line + evidence below — safe to strike/remove the cited entries.

### High confidence — shipped PRs / tsc-clean / already-struck (26)

- **~L61** — fleet sync_settings sudo-grep reads /srv/rl-infra/.env via rl-agent — _already struck; read_infra_env() present sync-local-to-env.sh:487 + test sync-local-to-env-infra-read.test.sh exists (fix 2026-06-02)_
- **~L63** — sync error can't distinguish missing-var from SSH-read-failure — _already struck; INFRA_ENV_READ_RC differentiation present in read_infra_env() + same test file (fix 2026-06-02)_
- **~L196** — migration 0140 audit prepopulation bug (prod outage) — _fixed in ROK-1281 run-migrations-with-sentry.ts (text self-states fix)_
- **~L417** — ROK-1319 reconcile trust-mode marks unapplied as applied — _reconcile-migrations.mjs now probeEffectExists-gated (#869)_
- **~L418** — ROK-1320 deploy_dev.sh migration-drift probe — _shipped PR#869_
- **~L437** — ROK-1316 lineup suggestions block on Gemini cache miss — _shipped PR#888 (commit a56be725)_
- **~L440** — ROK-1319 reconcile trust-mode bug (dup of L417) — _probe fix in reconcile-migrations.mjs (#869)_
- **~L441** — ROK-1320 deploy_dev.sh drift probe (dup of L418) — _shipped PR#869_
- **~L468** — games-lookup.integration.spec:306 TS2307 missing controller — _import removed, controller present, tsc clean_
- **~L484** — validate-ci.sh run_typecheck masks api tsc failure — _fixed with `_
- **~L526** — common-ground spec missing itadLowestPrice (TS2741) — _tsc-clean + fixtures added at spec L60/L100 on origin/main_
- **~L531** — invite-page test "/join event/i" button not found — _invite-page.test.tsx passes 18/18 now; renderer emits "Join Event" (invite-character-step.tsx)_
- **~L576** — gc-sweeper jq filters missing closing paren (silent no-op reapers) — _filters present + reaper loops fire; folded into M5a, merged #832_
- **~L596** — gc-sweeper orphan kill -0 wrong PID namespace — _switched to per-task pidfile + chmod 664; ROK-1336 #833 (61d348be)_
- **~L622** — Mutagen sync didn't block claim return (silent hang) — _flush_mutagen added in cmd_claim; merged #834 (rl:287)_
- **~L667** — validate-migrations.sh drizzle-kit silent-exit — _already labeled [RESOLVED] in-file — commit 9eb6a3e3 (archive whole block)_
- **~L742** — rl_env_deploy served stale build (fix/batch-2026-06-02-ui) — _already-marked RESOLVED; sync-guard shipped PR #868 (6e78107e)_
- **~L747** — rok-fleet-stale-build-sync-guard — _entry marked [high — FIXED]; PR #868 merged, sync-guard.ts present_
- **~L815** — fleet admin@local 401 blocker (RL_ADMIN_PASSWORD) — _already marked RESOLVED; VM env set, confirmed_
- **~L817** — Playwright runner image v1.50 vs project 1.60 — _rl-infra/runner/Dockerfile now `v1.60.0-jammy` (commit 8fdfa260)_
- **~L838** — gc-sweeper PID-namespace false-orphan bug — _ROK-1336 PR #833 (pidfile contract shipped)_
- **~L839** — validate-ci perf_emit_local silent-failure diagnostic — _fix on main (.errors append, validate-ci.sh ~191-200)_
- **~L840** — fleet admin@local password rotation (ROK-1356) — _ROK-1356 shipped PR #884 (MERGED)_
- **~L841** — env-spin stale-image reuse on redeploy (ROK-1357) — _shipped PR #884_
- **~L903** — rl-infra runner Playwright v1.50 vs required v1.60 image lag — _PR #890 / 8fdfa260; runner/Dockerfile now FROM playwright:v1.60.0-jammy_
- **~L932** — onboarding Steam selector strict-mode collision — _scripts/smoke/onboarding.smoke.spec.ts:370 now `{ name: 'Steam', exact: true }`_

### Medium confidence (35)

- **~L342** — community-insights operator-role 401 on GET endpoints — _controller now class-level `@Roles('operator')` (community-insights.controller.ts:46) covers all GET endpoints + CI green on main_
- **~L374** — lineup-confirmation-pills invitee hero/checkmark selectors — _spec rewritten for new hero by ROK-1297 PR #823 (`/nominate a game/i` markup)_
- **~L376** — events smoke attendance light-mode theme tokens — _deterministic Playwright check, CI green on main (required 5-shard suite blocks merge)_
- **~L378** — onboarding game-time compact GameTimeGrid viewport — _smoke-flake hardening PR #900 (2026-06-18) touched spec_
- **~L379** — community-lineup banner visible on mobile — _reworked + flake-hardened by PR #900 / #871 / #854_
- **~L380** — lineup-carryover auto-populate from prior decided matches — _deterministic Playwright check, CI green on main_
- **~L438** — ROK-1317 useAiFeatures polls 403 for non-admin — _shipped PR#843_
- **~L439** — ROK-1318 env_lock_release no-ops after re-anchor — _shipped PR#843_
- **~L462** — games-dedup-audit.integration.spec:386 TS2769 — _api tsc (spec-inclusive tsconfig.json) clean, 0 errors_
- **~L464** — games-dedup-audit.service.spec:443 TS2502 — _api tsc clean_
- **~L466** — games-dedup-merge.integration.spec TS2352 x3 — _api tsc clean_
- **~L470** — lineup-deadline-vote-race.integration.spec:186 TS2345 — _api tsc clean_
- **~L472** — lineup-notification.service.private-visibility.spec TS2556 — _file deleted + tsc clean_
- **~L593** — NUL-byte header-injection guard regex broken — _impl regex now /[\r\n\x00]/ at test-plan.ts:149_
- **~L609** — test_task_list.sh 12 of 13 fail on main — _now 15 pass 0 fail; fixed in #884 (ROK-1356..1361)_
- **~L619** — env-spin failure rollback leaves orphaned PG container — _EXIT trap docker rm PG+APP added (ROK-1357, env-spin:35-73)_
- **~L638** — .deployed_sha never written by deploy script — _deploy.sh:47 now ssh-writes /srv/rl-infra/.deployed_sha_
- **~L641** — traefik conf.d perms not persisted in provisioning — _deploy.sh:37-38 chgrp rl-fleet + chmod 2775_
- **~L735** — rl-infra VM missing RL_ENV_JWT_SECRET blocks sync_settings/admin-seed — _doc self-reports resolved at L766 (sync_settings ok + admin@local seeded on retry)_
- **~L753** — orchestrator lease-status AC9 `timeout: command not found` on macOS — _test now wraps with `rl_timeout` helper (lease-status.test.sh:139), bare `timeout` gone_
- **~L767** — fleet Playwright --only-e2e 401 admin@local random pw — _ROK-1368 commit 61722b66 threads re-seeded ADMIN_PASSWORD into rl_validate_ci --only-e2e_
- **~L781** — env_spin aborted leaving healthy-but-unreachable env — _resolution L787 + PR #880 (85782bd7): exec 9> guards, idempotent route fail-loud_
- **~L782** — Mutagen sync-probe re-wedge (sentinel-deletion race) — _resolution L788 + PR #880; sync-guard.ts finally-flush block (L332/L340) present_
- **~L799** — rl_env_sync_from_local "public schema is empty" wrong probe — _regression test added: sync-local-to-env-host-resolve.test.sh covers exact exit-3 host-resolve bug_
- **~L800** — fleet slot route file stale blocks deploy/dead-ends slot host — _PR #877 (ec0aac82) TTL-reap removes env Traefik route file; PR #880 safe deploy_
- **~L809** — runner missing tools/test-bot deps for Discord smoke — _ROK-1359 shipped PR #884 (7827627f)_
- **~L818** — fleet env Discord OAuth internal-hostname redirect — _already FIXED; env-spin CLIENT_URL committed, verified_
- **~L842** — rl_env_sync_from_local false empty-schema probe (ROK-1358) — _shipped PR #884_
- **~L843** — runner test-bot deps tracked (ROK-1359) — _shipped PR #884_
- **~L844** — mcp-rl-fleet NUL-byte/regex guards (ROK-1360) — _shipped PR #884_
- **~L845** — orchestrator macOS/BSD coreutils failures (ROK-1361) — _shipped PR #884_
- **~L869** — rl_test_plan_wait/rl_task_wait long-block visibility hang — _ROK-1362 bounded waits + 120s cap, PR #889_
- **~L907** — exec.ts:854 worktreePathSchema .refine TS2345/TS7006 — _`npx tsc --noEmit` in tools/mcp-rl-fleet now EXIT=0_
- **~L917** — LineupPhaseQueueService registered in two modules — _single provider exported by LineupsModule, standalone-poll imports it (ROK-1206)_
- **~L954** — test-plan-v2 executeWait directory-watch assertion fails — _`vitest run test-plan-v2.spec.ts` now 14/14 pass_

### Low confidence (28)

- **~L167** — use-standalone-poll queryKey mismatch ['scheduling-banner'] — _line 29 now uses correct `['scheduling','banner']` tuple; PR #910 "fix(low-hanging): banner refresh"_
- **~L224** — seed-igdb per-row rowExistsWithIgdbId extra DB roundtrip — _function gone, replaced by batched IN query (only in comments now)_
- **~L280** — lineups-matching `decided` preserved-list comment clarified — _comment rewritten at lineups-matching.helpers.ts:36 — now frames `decided` as the lineup-row status, preserved list is only `scheduled`/`archived`; file refactored (mtime May 31)_
- **~L284** — stillWaitingOnVoters frontend drops optional-chaining — _page now reads `lineup.stillWaitingOnVoters.length > 0` directly (lineup-detail-page.tsx:234), contract field non-optional (lineup.schema.ts:260) — exactly the suggested fix_
- **~L396** — 0142_snapshot.json missing community_lineup_user_submissions — _snapshot now contains the table (8 refs in api/src/drizzle/migrations/meta/0142_snapshot.json)_
- **~L401** — feedback.controller attachSlowQueryContext untrimmed clientLogs — _now passes `truncatedClientLogs` (feedback.controller.ts), shipped via PR #910_
- **~L409** — chrome-mcp env_lock_release no-op after deploy re-anchor (doc note) — _root-fixed by ROK-1318 PR#843, release now matches via agent_id_
- **~L489** — vitest MSW leak invite/admin-dashboard cross-suite flake — _setup.ts has afterEach resetHandlers + admin-dashboard-page.test.tsx deleted + invite passes 18/18_
- **~L536** — use-lineup-submit:193 isError bare-expect race flake — _waitFor wrap applied on origin/main (L196) with TECH-DEBT-2026-05-19 comment_
- **~L563** — lease-status.test.sh bare `timeout` not on macOS — _now uses rl_timeout wrapper (test_helpers.sh:137, gtimeout-detect + pure-bash fallback) + inotifywait skip guard_
- **~L568** — NUL-byte header test unsatisfiable via process.env — _test now calls validateCurlHeaderValueForTest helper directly; tsc-clean_
- **~L571** — env-destroy AC12 flock test fails on macOS — _test rewritten to force flock-failure deterministically on both platforms_
- **~L580** — heartbeat-emitter timeout test fails on macOS — _now uses portable rl_timeout shim instead of bare timeout_
- **~L588** — rl validate-ci doesn't auto-release slot on exit — _--release-on-exit opt-in flag added (ROK-1336, rl:641-659)_
- **~L604** — itadLowestPrice missing-in-type TS errors slip CI gate — _tsc-clean + field now present in spec (lines 60,100)_
- **~L629** — task-inspect/infra-logs inline execFileP duplication — _both now import execFileP from '../exec.js'_
- **~L632** — env-spin idempotent path missing LAST_PHASE markers — _three markers present (env-spin:319,396,456), commit efc42850_
- **~L651** — test-plan.spec.ts AC6 NUL-byte header test — _rewritten to validateCurlHeaderValueForTest().toThrow, fn correct — PR #833/#842_
- **~L705** — community-lineup banner-visibility smoke flake — _tests renamed + beforeEach lineup precondition + deep-link — PR #900 smoke-flake hardening_
- **~L707** — bootstrap-admin FK violation on community_lineups — _idempotent upsert (onConflictDoUpdate) — PR #863 on main_
- **~L792** — no-sleep-lint false-positive on JSDoc `sleep()` comment — _lint now exits 0 ("No sleep() calls found"); offending comment gone from lineup-abort.test.ts_
- **~L793** — test-bot tsc `Cannot find module @discordjs/voice` — _dep added: tools/test-bot/package.json:14 `@discordjs/voice ^0.19.2`_
- **~L810** — gc-sweeper auto-release strands child envs — _ROK-1357 shipped PR #884_
- **~L830** — seed-fixture-user not clearing deactivated_at — _already FIXED in-branch (deactivatedAt: null)_
- **~L887** — lineup-abort no-sleep lint false-positive on `sleep()` comment — _comment reworded to "never a fixed delay" — sleep() token gone_
- **~L900** — api/package.json test:integration uses dead --testPathPattern flag — _script now `jest --config ./jest.integration.config.js --runInBand`, flag removed_
- **~L901** — test-bot voice.ts TS2307 @discordjs/voice not installed — _package now present in tools/test-bot/node_modules (env/install issue)_
- **~L909** — no GitHub job runs mcp-rl-fleet Vitest suite — _ci.yml:197 tools-tests job runs `npm test -w @raid-ledger/mcp-rl-fleet`_

### Nit — comment rewrites / lint-nit fixes / already-struck (6)

- **~L92** — env-lock mid-run preemption / heartbeat re-anchor (ROK-1209) — _entry's own inline note: fixed by ROK-1209 "re-anchor deploy_dev.sh lease to API PID" commit_
- **~L146** — games-dedup doc comment references nonexistent extendedBlastRadiusKeys — _`grep extendedBlastRadiusKeys` GONE everywhere; comment rewritten to lockstep buildDirectCountQueries note_
- **~L232** — no-sleep-lint flags lineup-abort.test.ts:26 comment — _comment rewritten + lint clean, PR #910 (6098fffc)_
- **~L237** — no-sleep-lint nit duplicate (still-unresolved note) — _same fix, lint now "OK: No sleep() calls", PR #910_
- **~L316** — games-dedup-union-find numeric-sort comment added — _explanatory comment present at games-dedup-union-find.helpers.ts:172 (`// igdb/steam ids are numeric strings → sort numerically; names → lex.`)_
- **~L902** — dup lineup-abort.test.ts:26 no-sleep lint comment — _same fix as L887 — `sleep(` token removed from comment_

### 2026-07-01 — fix/rok-1367-raw-fetch-401-refresh (surfaced during full `vitest run --coverage` gate)

- **[med — pre-existing load flake, NOT this branch; already tracked ROK-1285/ROK-1290]** `web/src/hooks/use-ai-suggestions.test.ts` — one of the real-timer poll-cap tests (`reports pollExhausted=true …` / `resets the poll budget …`, 55s–70s `waitFor` budgets) times out under full-coverage load: `Error: Test timed out in 70000ms` at `Timeout.checkRealTimersCallback` (@testing-library/dom wait-for). Reproduced twice this session (full suite: that file ran 644s; isolated re-run: 1024s for 6 tests — an absurd duration proving CPU/event-loop starvation, not logic). File is UNCHANGED by this branch (`git diff --name-only origin/main..HEAD` does not list it) and this branch's `getAiSuggestions` change is byte-identical for the 200/503 payloads these poll tests use (no 401 → no refresh probe). Passed green when run in a lighter batch earlier the same session. This is the known ROK-1285 (raised `asyncUtilTimeout` for this exact file) / ROK-1290 deep-carrier poll flake — GitHub CI runs sharded + randomized so it lands in a lighter shard and passes. Suggested: convert the two poll-cap tests to fake timers (`vi.useFakeTimers()` + `advanceTimersByTimeAsync`) so the 45s/55s budgets don't depend on wall-clock under load — tracked in ROK-1285/ROK-1290. Not fixing here (out of scope, test not touched by this story).

### 2026-07-02 — fix/rok-1370-poll-start-rescheduling (surfaced during fleet validate-ci)

- **[med]** `api/**` (dozens of files, e.g. `api/src/auth/auth.controller.ts`, `api/src/admin/demo-data-gen-taste-profile.ts`, `api/src/discord-bot/services/discord-embed.factory.ts`) — `npm run prettier:check -w api` fails on a FRESH `npm ci` install: PR #940 (2026-06-30 combined dependency bumps) bumped prettier 3.8.4 → 3.9.3 in the lockfile and 3.9.3 reformats existing files (union-type wrapping etc.). Pre-existing on origin/main: main-repo checkout with pre-#940 node_modules (prettier 3.8.4) passes with 0 warnings on identical file content; the fleet runner passed earlier tonight on cached node_modules and failed after a force-resync triggered fresh `npm ci`. GitHub CI never catches it because the lint job runs `eslint --fix` (silently reformats its own checkout, exits 0) — only `validate-ci.sh`'s `prettier:check` is strict. Blocks the fleet lint gate for every branch validated on a freshly-provisioned runner from now on.
  Suggested: one-shot `npx prettier --write "{src,apps,libs,test}/**/*.ts"` in api (+ web check) as a standalone `chore(format):` PR, immediately after the current in-flight PRs land to minimize rebase conflicts.

### 2026-07-02 — fix/rok-1391-se-lockin-race (surfaced during local Discord-smoke AC gate, overnight batch)

- **[med — pre-existing smoke-suite design, NOT this branch]** `tools/test-bot/src/smoke/` flow category + `/admin/test/{enable,disable}-scheduled-events` — flow tests run at `SMOKE_CONCURRENCY=5` (config.ts:31) against ONE global `scheduledEventsEnabled` toggle; every SE test's `finally` disables it, killing sibling tests' in-flight SE creates (create path reads the toggle at execution). Verbatim log signature: interleaved `POST /admin/test/disable-scheduled-events` + zero `DISCORD | scheduledEvents.create` for the victim's event. Historically masked by the dev reconciler healing missing SEs: when the `ScheduledEventReconciliation_reconcileMissing` cron_jobs row is ABSENT (fresh DB — e.g. CI shards), smoke's `pauseReconciliation` cannot stick (`executeWithTracking` runs untracked, cron-job.service.ts:210-213) and the reconciler heals every ~70s; tonight's dev DB HAS the row → pause sticks → 3-4 SE-dependent flow tests (ROK-944 completion, completion-future, ROK-1347 reconciliation, ROK-1370 pair) fail per parallel run on ANY branch. Confirmed pre-existing: completion-future + 1347-reconciliation PASS serially on this branch. Suggested: per-test SE-toggle scoping (e.g. fixture refcount so disable only lands when no sibling holds enable), or serialize the SE-dependent subset like the `voice` category.
- **[med — pre-existing, NOT this branch]** serial workaround is non-viable: `SMOKE_CONCURRENCY=1 SMOKE_CATEGORY=flow` takes ~50 min → smoke ApiClient JWT expires mid-run (last 4 tests fail `POST … 401`) and Discord rate-limit debt accumulates (guild `scheduledEvents.fetch` 118ms → 807ms → >5s across runs; one undici `Connect Timeout … 162.159.136.232:443`). Suggested: token refresh in the smoke ApiClient (or re-login per test file) before any serialization work.
- **[low — pre-existing, already noted in session notes 2026-07-02]** `Multiple users sign up -> roster updates correctly` — failed all 3 modes tonight (parallel 61s pollForEmbed timeout ×1, serial 82s undici connect-timeout variant ×1; also 2/2 on 2026-07-02 pre-batch). Untouched by this branch. Suggested: next flake pass; likely embed-sync poll budget vs Discord latency, not logic.
- **[high — pre-existing, MAIN-reproduced 2026-07-02, NOT this branch]** reschedule-poll lock-in embed re-render is carried by AMBIENT embed-sync traffic, not the lock-in itself — `POST /scheduling-polls/:id/complete` → UPDATED re-emit correctly recreates the SE (fresh time verified) but produces NO embed re-render: `discord_event_messages.embed_state` stays `rescheduling` until some OTHER event's signup/edit enqueues an embed sync whose level-triggered processor heals it. Deterministic under isolation: `SMOKE_NAME_FILTER="ROK-1370" SMOKE_CONCURRENCY=1` fails `waitForLiveEmbed` on EVERY attempt on BOTH `fix/rok-1391-se-lockin-race` AND `main@89529dfb` (api.log evidence: 1391 branch event 82 4:34:53→4:35:54 stuck; main event 82 5:13:47→5:29:25 stuck, retries included). Masked in CI + parallel local runs by sibling-test embed traffic — which is why #955's smoke passed. User impact: a real lock-in on a quiet server leaves the event card stuck on "RESCHEDULING" until the next unrelated embed touch (or ~15-min reconcile-adjacent traffic). Suggested: make the lock-in path enqueue an explicit embed sync for the linked event (mirror the RESCHEDULING teardown's `updateEmbedState` with a symmetric restore), or have `completeStandalonePoll` enqueue `embed-sync` directly. Candidate Linear story drafted in the 2026-07-03 morning report; operator files.

### 2026-07-02 — fix/rok-1389-series-voice-channel + fix/rok-1390-quickplay-fallback (reviewer LOWs, overnight batch)

- **[low — pre-existing, outside ROK-1389's 8-site inventory]** `api/src/events/pug-invite.service.ts:151,253` + `api/src/discord-bot/services/voice-attendance-snapshot.helpers.ts` snapshot path — still call the raw 3-arg `resolveVoiceChannelForEvent`/tier resolver without the ROK-1389 override guard, so a PUG-invite Join-Voice link and the start-of-event snapshot don't honor a voice `notificationChannelOverride` while reminders/website/embeds/SE now do. Behavior unchanged from pre-1389; operator to decide whether to fold PUG-invite into the unification. Suggested: one-line delegation each to `resolveVoiceChannelHonoringOverride` in a follow-up.
- **[low]** `api/src/discord-bot/listeners/voice-state.listener.ts:128` — the ROK-1389 WARN-only binding heal report re-fires on every Discord READY (gateway reconnects), not once per boot. Harmless (read-only WARNs) but noisy on flappy connections. Suggested: `firstReadyOnly` gate flag.

### 2026-07-06 — dispatch batch ROK-1365 + ROK-1392 (surfaced during dispatch)

- **[med — pre-existing CI coverage gap, NOT this branch]** The GitHub `discord-smoke` job's path filter (`.github/workflows/*` Discord-smoke `paths:` set — discord-bot/**, notifications/**, events/signups*, events/event-lifecycle*, admin/demo-test*, tools/test-bot/src/smoke/**) and the mirrored local detector in `scripts/validate-ci.sh` do NOT include `api/src/lineups/standalone-poll/**`. But standalone-poll enqueues an embed-sync at reschedule-poll lock-in (ROK-1392) → it directly drives Discord embed behavior. Net: an embed-affecting change under `lineups/standalone-poll/**` gets its GitHub `discord-smoke` job path-filtered to SKIPPED, so GitHub provides NO Discord behavioral coverage for it. ROK-1392 was safe because the companion-bot smoke was run LOCALLY (GREEN ×2, the `ROK-1370: lock-in restores the live embed` acceptance test), but a future embed-affecting lineup change could merge with zero Discord CI signal. Suggested: add `api/src/lineups/standalone-poll/**` (and audit sibling lineup embed paths) to both the GitHub discord-smoke `paths:` filter and the `validate-ci.sh` Discord-smoke trigger list, so embed-affecting lineup changes auto-trigger the Discord smoke on push.

### 2026-07-06 — feat/rok-313 (surfaced during ROK-313 full local gate)

- **[med — pre-existing local-env, NOT this branch]** `npm run test:integration -w api` (the FULL local integration suite) SIGABRTs (exit 134) at global setup — 0 test suites run, native V8/libuv crash dump, no ROK-313 code loaded yet. Reproduces run-alone (not concurrency). ROK-313's own integration specs pass 9/9 in isolation (`test:integration -- users-moderation.integration`). Class of the known socket-carrier / OOM local flakiness (`reference_bullmq_ioredis_test_carrier`, `reference_integration_404_socket_carrier`), but here the crash is at Testcontainers global setup rather than mid-suite. GitHub CI runs integration SHARDED so it never hits the monolithic single-process accumulation. Suggested: shard the local `test:integration` invocation (or raise `--max-old-space-size` in the jest.integration runner) so the full suite is runnable locally; low priority since CI shards.
- **[low — pre-existing local-env, NOT this branch]** `./scripts/validate-migrations.sh` fails locally at migration `0000` with `psql: connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed: FATAL: database "raid_ledger" does not exist` — the script's psql hits the laptop's default local socket (PGHOST/PGDATABASE from the shell profile) instead of the temp container's TCP port. Passes cleanly on the fleet runner (`Applied 153 migration(s) cleanly`). Local-shell-env artifact, not a script/migration bug. Suggested: `validate-migrations.sh` should `unset PGHOST PGDATABASE PGPORT` (or set them explicitly to the temp container) before its psql loop so a developer's ambient PG env can't hijack the connection.
- **[note]** The prettier 3.9.3 vs 3.8.4 (#940) drift documented 2026-07-02 recurred on this branch's fresh-`npm install` worktree (local `prettier:check` flags ~20 pre-existing files; api-workspace prettier is the CI-authoritative context). ROK-313's own api files were formatted to 3.9.3 in-branch. No new action — the standing `chore(format):` suggestion stands.

### 2026-07-13 — fix/scheduling-poll-voter-membership (surfaced during adversarial review of the voter-membership fix)

- **[low]** `api/src/lineups/scheduling/scheduling.controller.ts:152` — `DELETE .../votes` (retractAllVotes) and the poll-page GET remain ungated for private lineups after the vote/suggest surface gained `assertCallerMayVote` (fixed in this same branch — Codex security P2). Retract only deletes the caller's own votes (self-cleanup, harmless) and the GET is read-only, but for full parity with nominate/game-vote the whole private-lineup scheduling surface could run the participation check. Suggested: add `assertCallerMayVote` to retract + consider gating `getSchedulePoll` for private lineups.
- **[low]** `api/src/lineups/lineups-bandwagon.helpers.ts:71-91` `checkAutoPromote` — schedule-vote enrollment grows the member set of `suggested` matches but never triggers the promote check itself; the NEXT bandwagon join computes its threshold percentage against the enlarged member count and can promote earlier than the pre-fix math would have. Behaviorally defensible (slot votes are a demand signal) but undocumented. Suggested: either call the promote check after vote-enrollment on `suggested` matches too (consistent), or note the semantic in the helper docstring.
- **[low]** `api/src/lineups/standalone-poll/standalone-poll-reminder.service.ts:159-173` + `lineup-reminder-target.helpers.ts` — sticky vote-enrollment means a user who un-votes (or retracts all votes) stays a match member and becomes a standing "you haven't voted" reminder-cron target with no leave-poll mechanism. Matches from-match semantics (member sets never shrink) but is new for standalone polls. Suggested: consider excluding users whose LAST action was retract-all, or add an explicit leave/mute affordance if operators report DM complaints.

### 2026-07-15 — fix/running-late-followups (surfaced during high-effort code review of the running-late/delay follow-ups)

- **[low — defense in depth]** `formatEpoch` (`api/src/notifications/format-helpers.ts:19-29`) renders ANY residual `<t:` token that reaches `buildPlaintextContent` in the server process TZ (no `timeZone` option), and `stripDiscordMarkup` (:39) discards the style char so `:R` (relative "in N hours") collapses to an absolute duplicate. Even after the two fixes above, any future notification type that stores `<t:>` markup inherits the bug. Suggested: thread the recipient timezone into the DM job data and have `formatEpoch` render in it (and render `:R` as a real relative delta). Note: `tools/test-bot/src/helpers/discord-markup.ts` mirrors `format-helpers.ts` and must stay in sync.
- **[low — product/observability]** The new `running_late` notification type is default-on across inApp/push/discord and has no toggle row in the web `NotificationPreferencesSection` (`web/src/components/profile/NotificationPreferencesSection.tsx`) — same as the shipped `event_delayed` precedent, so users cannot opt out of either. It also sets no `expiresAt`, so a late notice lingers in the in-app list after the event. Suggested (if operators report noise): add prefs rows for `event_delayed` + `running_late` and set `expiresAt = event end` on both.

### 2026-07-15 — rok-1394-nonseries-zero-presence-guard (surfaced during fleet validate-ci run on runner-1)

- **[med]** Fleet runners run validate-ci against stale `/workspace/node_modules` relative to the Mutagen-synced `package-lock.json`, producing phantom failures — missing newly-added deps at build; version-drifted eslint/prettier reformatting files unrelated to the branch diff. Flagged across 2026-06-07, 2026-07-15: rok-1353-refresh-tokens, rok-1394-nonseries-zero-presence-guard. Instances: `scripts/validate-ci.sh::run_lint` (mutating `eslint --fix` before `prettier:check` — check-only-gate half shipped with ROK-1404 in fix/batch-2026-07-17), runner `/workspace/node_modules` never refreshed on lockfile change (ROK-1404 AC3, open). Last surfaced 2026-07-17 (fleet tasks 88dd18de5e4c/0c0e69d49eff, per the fix/batch-2026-07-17 entry below). Tracked in ROK-1404 (In Progress). Consolidated 2026-07-17.
  Suggested: have `rl_validate_ci` run `npm ci` (workspace root + tools/test-bot) when the synced `package-lock.json` hash differs from the runner's last-install stamp; keep the lint gate check-only.

### 2026-07-17 — fix/batch-2026-07-17 (PR pending)

- **[RESOLVED, was med]** `.github/workflows/discord-smoke.yml` + `scripts/validate-ci.sh:305` discord-smoke filter gap for `api/src/lineups/standalone-poll/**` (2026-07-06 entry) — both filter locations extended in this batch; CLAUDE.md trigger lists updated to match.
- **[RESOLVED, was low]** `scripts/validate-migrations.sh` "database raid_ledger does not exist" startup race (2026-07-06 entry) — **diagnosis correction:** the suggested `unset PGHOST/PGDATABASE/PGPORT` was a misdiagnosis (psql runs INSIDE the container via docker exec; laptop env can't hijack it). Actual mechanism: `pg_isready` succeeds against initdb's temporary socket-only bootstrap server before `POSTGRES_DB` exists. Fixed in this batch: readiness poll now runs `SELECT 1` against `raid_ledger` over TCP (`-h 127.0.0.1`) — the bootstrap server is socket-only, so TCP success guarantees the final server.
- **[half-RESOLVED, was med]** 2026-07-15 `run_lint --fix` order-dependence entry — the check-only gate half shipped in this batch (ROK-1404), including the forced one-shot prettier 3.9.4 reformat of 32 files (laptop node_modules carried prettier 3.8.4 vs lockfile 3.9.4 — the drift can point EITHER direction: this time the runner was lockfile-correct and the laptop was stale). The fleet lockfile-exact-refresh guard half (ROK-1404 AC3) remains open; this batch hit it live (fleet tasks 88dd18de5e4c/0c0e69d49eff: 42 phantom prettier errors until `npm ci` on both ends).
- **[med]** `tools/mcp-rl-fleet` `rl_env_spin` — hung >30 min with NO response, NO task record (`rl_task_list` empty of it), and NO env-registry entry; the MCP idle timeout (1899s) aborted the call and the env never materialized. Violates the ROK-1362 every-blocking-wait-caps-at-120s design — spin appears to run its build/deploy synchronously inside the tool handler instead of dispatching a task like `rl_env_deploy` does. Bit this batch's Chrome-MCP gate (fell back to local deploy_dev). ROK-1338 no-SSH umbrella adjacent.
  Suggested: make `rl_env_spin` async-by-default returning `{task_id}` (laptop or VM task), polled via `rl_task_status` like deploy/clone-prod.

### 2026-07-18 — fix/batch-2026-07-17-b (PR #994)

- **[RESOLVED, was med]** `tools/mcp-rl-fleet/src/tools/__tests__/test-plan-v2.spec.ts` "ROK-1337 AC2 — executeWait watches the slug directory" red (2026-06-08 entry, ~L584) — **diagnosis correction:** neither an fs.watch mock wiring issue nor a path-normalization regression (the product code watches the correct slug directory). Actual mechanism: `vi.resetModules()` in `beforeEach` wipes the `resolveProxmoxHost` memo, so every test re-paid a REAL `dns.promises.lookup('rl-infra.lan')` inside the AC2 test's 100ms fixed race — runners with >100ms DNS never captured the spawn argv. Fixed in this batch: non-default `RL_PROXMOX_HOST` stub (RFC-5737 address, short-circuits before DNS) + `vi.waitFor` on the capture instead of a fixed timer. 15 consecutive green runs; suite 2.16s → 200ms.
- **[RESOLVED, was high]** `scripts/smoke/profile-avatar-prefs.smoke.spec.ts` timezone `toHaveValue('auto')` deterministic failure on cloned-prod/long-lived DBs (2026-06-18 entry, ~L574) — fixed in this batch: the spec now resets the admin timezone pref to `'auto'` through the app's own `PATCH /users/me/preferences` + reloads before asserting (assertion preserved, not weakened). Green against the live dev env (13 passed) and inside the full 646-test suite run.

### 2026-07-24 — rok-1418-suppression-scoping (surfaced during B1)

Deferred findings from the ROK-1418 suppression-scoping + bounded-extension fix (D4). All are out of scope for this `--static` PR (spec Deliverable 8); recorded here so B3/B4/B5 and the operator triage don't rediscover them. None is a failure on `origin/main` — they are known asymmetries the D4 change touches but deliberately does not resolve.

- **[med]** Duplicate suppression check per join — `api/src/discord-bot/listeners/voice-state-join.handlers.ts:77` (`handleGameBindingJoin`) calls `adHocEventService.trySuppressForScheduled(...)` and, when it returns false, `checkGameBindingThreshold` → `handleVoiceJoin` (`api/src/discord-bot/services/ad-hoc-event.service.ts:105`) runs the same suppression query a second time. Both queries + the (now bounded) `extended_until` write execute twice on every game-binding join. Not deleted here: the listener call is B3/B4 blast radius (ROK-1415 rewrites `checkGameBindingThreshold`). Suggested: after B3/B4 land, have the listener pass its suppression result through to `handleVoiceJoin` (or drop the listener-side check) so the query runs once.
- **[med]** Suppression-window extension skips the sync fan-out that the scheduled auto-extend path performs. `extendScheduledEventWindow` (`api/src/discord-bot/services/ad-hoc-suppression.helpers.ts`) writes only `extended_until` + `updated_at`, whereas `event-auto-extend.service.ts:161-180` also `eventCache.invalidate()`/`refresh()`, `adHocGateway.emitEndTimeExtended()`, and `scheduledEventService.updateEndTime()` (Discord scheduled-event end sync). A suppressed-join extension therefore leaves the event cache, the live web clients, and the Discord event end time stale until the next natural refresh. Suggested: extract the auto-extend fan-out into a shared helper and call it from the suppression write, or emit `emitEndTimeExtended` from the suppression path at minimum.
- **[low]** Dead OR-terms retained in the suppression clause. `buildBindingClause` still carries `events.channel_binding_id = <bindingId>` and (for the game path) the sibling subquery, but for `is_ad_hoc = false` rows `events.channel_binding_id` is only ever written by the ad-hoc INSERT, so both channel-based terms are structurally unreachable for scheduled events. Kept byte-identical on purpose (the intended landing zone if scheduled events ever gain a binding link — see spec D4(a)), not deleted. Suggested: leave until scheduled events gain a `channel_binding_id`, then either wire it or delete the terms with a test.
- **[low]** `extractGameIds` collapses to "all games" on any null-game or non-monitor binding. `api/src/discord-bot/services/voice-attendance-flush.helpers.ts:260-268` returns `null` (meaning "count every member regardless of game") as soon as one binding on the channel is not `game-voice-monitor` or has `gameId === null`. On a mixed channel (e.g. a monitor + a general-lobby, the permitted B2 shape) this silently widens attendance/flush counting to all games. Orthogonal to D4's suppression fix but adjacent in the same multi-binding topology. Suggested: decide whether a null-game monitor should widen the whole channel or be skipped, and encode it explicitly.
- **[low]** Multi-binding runtime asymmetries (batch plan flagged as deferred — the "§8" reference in spec D4 Deliverable 8 points at the resolved-questions section; the enumeration lives in the batch synthesis). Concrete instances the D4 change exposes but does not fix: (a) the anchored game clause scopes by the *querying* channelId while the sibling subquery matches *any* sibling binding on the physical channel, so game-match and sibling-match use different channel semantics; (b) `matchedBy` is a best-effort SQL `CASE` and can mislabel when more than one OR-term matches; (c) suppression runs once per binding, so an N-monitor channel evaluates the same scheduled event N times (the SQL monotonic guard dedups only the *write*, not the reads/logs). Suggested: revisit as a group when the multi-binding admin work (ROK-1416/B5) settles the intended per-channel topology.
- **[low]** `api/src/discord-bot/services/ad-hoc-suppression.helpers.ts:157` — `skipCappedWarnedAt` throttle Map never prunes expired TTL entries, so distinct capped event ids accumulate for the process lifetime (Codex review finding, ROK-1418 PR). In practice bounded by the daily 5AM Watchtower restart, so impact is a day's worth of capped events (tiny). Suggested: prune entries older than the 30-min TTL on each `warnSkipCapped` call, mirroring the prune-on-call pattern D5 Tier 1 mandates for the ROK-1417 trace Maps.

### 2026-07-24 — rok-1419-nonseries-unique-index (PR pending)

- **[med]** `api/src/drizzle/schema/channel-bindings.ts` (index `channel_bindings_nonseries_nullgame_unique`) + `api/src/igdb/igdb-dedup-fk-reassign.helpers.ts:167-186` — the new partial unique indexes create a `games` DELETE collision hazard. ROK-842 permits two `game-voice-monitor` bindings on ONE channel for DIFFERENT games (both `game_id` non-null → distinct rows in the game-partial index). A `DELETE FROM games` that removes BOTH those games fires the FK `ON DELETE set null`, collapsing both bindings to `(guild, channel, 'game-voice-monitor', NULL)` — which now BOTH qualify for the null-game partial index, so the second SET NULL raises 23505 and aborts the whole DELETE. Pre-index this succeeded. `safeReassign`'s bare `catch { ROLLBACK TO SAVEPOINT }` swallows it silently, so an IGDB dedup that merges two such twin-bound games would no-op that group with no signal. Narrow trigger (needs 2+ same-channel same-purpose monitors for different games AND both games deleted together), hence med not high.
  Suggested: before `DELETE FROM games`, null-and-dedupe co-channel monitor bindings first, or teach `safeReassign` to detect 23505 on `channel_bindings_nonseries_*` and repoint the surviving row instead of swallowing.
- **[RESOLVED in-batch, was low]** `api/src/drizzle/migrations/0161_channel_bindings_nonseries_unique.sql` (DML statements 3-4) — the audit `moved_events` / `events_repointed` / `events_nulled` columns were recomputed UNCONDITIONALLY by `run_id` on every execution, so a `reconcile-migrations.mjs` re-run after the losers were deleted would wipe the operator UNDO record to empty/0. Fixed pre-merge by the Lead (this PR): both audit UPDATEs now carry `EXISTS` re-run guards (statement 3: only rows with entries in this run's `moved` set — first-run empties keep the column DEFAULT `'[]'`; statement 4: only while the loser row still exists, i.e. pre-DELETE). Validated: `validate-migrations.sh` 162 clean + the B2-2/B2-3 dedupe/restore suites 20/20 against the edited file.
- **[low]** `api/tsconfig.json` (local only) — bare `npx tsc --noEmit -p api/tsconfig.json` reports ~36k `TS2304/TS2593 Cannot find name 'it'/'expect'/'describe'` across EVERY `*.spec.ts` locally (both this worktree and the main checkout, unaffected by `npm install`). `@types/jest` is present at the repo-root `node_modules/@types` but tsc's automatic `@types` discovery is not picking it up for the spec files; jest itself (ts-jest) compiles them fine and GitHub CI's fresh `npm ci` typechecks green (main merges), so this is a local `@types` auto-discovery quirk, not a code failure. It makes `validate-ci.sh --static`'s typecheck step unusable as a local signal — production source typechecks clean and `nest build` passes.
  Suggested: add an explicit `"types": ["node", "jest"]` (or `typeRoots`) to `api/tsconfig.json`, or a `tsconfig.spec.json`, so the local typecheck matches CI.
  **VERIFIED RESOLVED 2026-09-01** — measured on a clean main checkout: `npx tsc --noEmit -p api/tsconfig.json` exits **0** with **zero** errors (no TS2304/TS2593 jest-globals noise, and none of the 156 residual spec errors). Fixed at some point after 2026-08-21; no `types` array was needed. Do NOT re-file or 'fix' this — the local typecheck is a trustworthy signal today.

### 2026-07-25 — rok-1415-binding-invariant-guard (surfaced during B3)

Deferred by design in the ROK-1415 root fix (spec D2(f)): the invariant guard enforces WRITE intent at the two service sites only. The following paths can still produce the inert `(voice, game-voice-monitor, NULL)` shape and are deliberately NOT prevented — they are exactly why the runtime tolerance (raw-count spawn) + boot detection (`[binding-heal] INERT` WARN + Sentry) + `GET /admin/discord/bindings/health` ship in the same PR:

- **[med]** `pg_restore` / prod-clone flows — `channel_bindings` is not excluded from backups, so restoring a dump that predates a game deletion resurrects inert rows wholesale. Detection fires on next bot CONNECT; the ROK-1416 UI repair (B5) is the remediation path. Suggested: none (tolerated by design; revisit only if inert-row Sentry events become recurrent).
- **[med]** IGDB dedup raw reassign (`igdb-dedup-fk-reassign.helpers.ts` `safeReassign`) and 0140-style bulk UPDATE migrations — raw SQL bypasses the service guard AND `normalizeAndDeleteGames`. The dedup helpers currently repoint bindings before deleting games (verified — they never orphan on their own), but nothing structurally forces future raw paths to. Suggested: route any future app-side games deletion through `normalizeAndDeleteGames` (it owns delete + collision-aware normalization).
- **[low]** `voice-attendance-flush.helpers.ts::extractGameIds` still widens to all-games when a null-game binding is present on a mixed channel (pre-existing, flagged in B1's section) — the ROK-1415 tolerance makes null-game monitors SPAWN now, so this asymmetry is reachable slightly more often. Same suggested fix as B1's entry.
- **[med]** `channel-bindings-invariant.helpers.ts::normalizeAndDeleteGames` has NO production call site yet (Codex ROK-1415 review P1) — the only current app-side game deletes are the IGDB dedup paths, which deliberately repoint bindings to the canonical game (a DIFFERENT policy than normalize-to-lobby) before deleting, so wiring them through the normalizer would change dedup semantics. The helper is the sanctioned entry point for any FUTURE app-side delete (admin endpoint, cleanup job). B2's games-DELETE 23505/safeReassign entry remains the tracker for the dedup-side hazard; the normalizer is the building block for its fix. Suggested: route any new games-delete path through the helper; revisit safeReassign with it when the dedup hazard is picked up.
- **[low]** `voice-state.handlers.ts:211` `!binding.gameId` treats `gameId === 0` as missing (Codex ROK-1415 security P3) — the contract now correctly derives a monitor for game id 0, but a monitor bound to a hypothetical id-0 game would zero-count at getGameFilteredCount and NOT be flagged inert (detection checks `=== null`). Unreachable today (serial ids start at 1; `gameExists(0)` 404s the POST path) and the line is deliberately untouched in B3 (ROK-1390/1394 degrade-guard adjacency, plan D2(g)/D5). Suggested: change to `binding.gameId == null` in a batch that owns the degrade-guard tests.

### 2026-07-27 — rok-1417-quickplay-observability (surfaced during ROK-1417 D5)

Deferred by design in the ROK-1417 observability spec (plan D5, Tiers 1/2/3). The per-join `[voice-gate]` trace + the `[quick-play-health]` canary ship in this PR; the items below were explicitly scoped OUT and are logged here so the operator can decide whether any warrant a follow-up.

- **[low]** `api/src/discord-bot/listeners/voice-state.handlers.ts:205` `getGameFilteredCount` left untouched (D5 explicit out-of-scope) — the trace does NOT reach inside it; its "who counted / who confirmed" information is instead carried by `game=null counted=0` on the `[voice-gate]` line for the null-game path and by `counted=N confirmed=N` on the fixed-game path. Suggested: none — deliberate; revisit only if the counted/confirmed split proves insufficient in a real incident.
- **[low]** six `[voice-pipe]` printf survivors still pass `%s`/`%d` args that the NestJS `Logger` does NOT substitute (they print the literal `%s`): `api/src/discord-bot/listeners/voice-state.handlers.ts:46`, `api/src/discord-bot/services/voice-attendance-flush.helpers.ts:240,253,279,284`, `api/src/discord-bot/services/voice-attendance.service.ts:120`. ROK-1417's new lines use single-string interpolation; these older debug lines were left as-is (out of D5 scope). Suggested: convert to template interpolation like `voice-attendance-flush.helpers.ts:301` already does.
- **[nit]** ENV `LOG_LEVEL` / `DEBUG` unverified from the repo — the whole point of `traceGate` using `logger.log` (INFO) is that it surfaces at the prod default threshold, but the allinone container's actual log level is not asserted anywhere in-repo. Operator action (D5/§7): pin `LOG_LEVEL`/`DEBUG` so INFO lines are retained, and note the retrieval path is `zgrep -h '\[voice-gate\]' *.log*` on the admin Logs export. Suggested: document the container log level in `Dockerfile.allinone` / deploy env and add a smoke assertion.
- **[low]** `api/src/discord-bot/listeners/voice-state-leave.handlers.ts:131` the 7s presence-recheck re-arm (`}, 7000)`) is neither bounded nor traced — a channel that keeps re-arming the recheck produces no `[voice-gate]` signal (the recheck path returns before a terminal outcome). Out of D5 scope (trace covers terminal spawn outcomes, not the recheck loop). Suggested: emit a throttled `recheck-rearmed` trace or cap the re-arm count if this loop is ever suspected in an incident.
- **[med]** `api/src/igdb/igdb-dedup-fk-reassign.helpers.ts` `safeReassign`'s bare `catch { ROLLBACK TO SAVEPOINT }` still swallows the `channel_bindings` 23505 collision silently (already tracked under 2026-07-2x B2 and the ROK-1415 section). Re-noted here because the ROK-1417 canary surfaces the RESULT (inert bindings + zero ad-hoc events → FAILED job) but NOT the swallowed CAUSE, so the two should be triaged together. Suggested: as per the B2 entry — teach `safeReassign` to detect 23505 on `channel_bindings_nonseries_*` and repoint instead of swallowing.

### 2026-07-31 — rok-1416-binding-purpose-game-editor (surfaced during B5 gates)

- **[med]** rl-infra runner image bundles Playwright 1.60.0 browsers while the repo (dependabot, on main) moved to 1.61.1 — `validate-ci` on the fleet fails at Playwright globalSetup with "Executable doesn't exist… update docker image" (task f728a138ce50). NOT caused by this branch (inherited via rebase onto main). Worked around per plan: the B5 Playwright gate ran LOCALLY against the fleet env (125 passed + 4 isolation-cleared load-flakes + 5 flaky-passed). Suggested: bump the runner image's Playwright base to v1.61.1-jammy (rl-infra/** operator-config change; ride-along or own PR).
- **[low]** `web/src/hooks/use-channel-bindings.ts` / `BindingConfigForm` — the update mutation's `.error` persists across form close/reopen, so a stale error banner can flash on reopen (workspace review). Suggested: `mutation.reset()` on form open/close.
- **[low]** The AC8 form-stays-open-on-reject path is covered by the live Chrome-MCP gate but has no unit test (workspace review). Suggested: 2 ChannelBindingList tests (reject → form open + saveError rendered; success → collapsed).
- **[low]** Purpose-option maps + classifier gate expressions are duplicated between BindingConfigForm and BindingCreateForm, and the config-prune rule exists client- AND server-side (server is the enforcing backstop, so drift is cosmetic-only) (workspace review). Suggested: extract a shared `binding-form.helpers.ts` when either form next changes.

### 2026-08-05 — fix/batch-2026-08-05 (surfaced during smoke validation)

- **[high]** `tools/test-bot/src/smoke/tests/series-dual-binding.test.ts` ROK-1390 "series-linked quick-play announces to series text channel" FAILS on `origin/main@ef71d919` — reproduced in an ISOLATED voice-category baseline run (and 3× on the batch branch): `pollForEmbed timed out after 60000ms` on the series announce channel, and the API log shows NO `[voice-gate] outcome=` line for the join, i.e. the spawn path exits before the gate. Pre-existing: identical failure on a clean main checkout (batch adds only the null-game suppression clause; this test joins WITH a `/playing` override, so it never touches the null-game path). **This test never runs in GitHub CI** (`SMOKE_SKIP_VOICE_JOIN=1` — no UDP voice on runners), so any regression in the #1006–#1015 window was invisible; prime suspect is the #1015 binding-PATCH rewrite (the fixture PATCHes a series voice binding's config), but unproven. Series quick-play routing may be broken on prod (Watchtower already pulled #1015). Suggested: dedicated investigation story — bisect #1006→#1015 locally with `SMOKE_CATEGORY=voice`; check the #1014 canary + prod `[voice-gate]` traces for series-channel spawns.
- **[med]** Three signup→embed-update smoke tests ("Embed updates to FILLING on signup", "Tentative signup reflected in embed", "Embed updates when signup cancelled") FAIL on `origin/main@ef71d919` in an ISOLATED embed-category baseline (~71s `waitForEmbedUpdate` timeouts on the events channel) while embed POSTING passes in the same run — the embed UPDATE sync path never lands within 60s on this env. Pre-existing on main; not attributable to this batch (its diff touches neither embed sync nor signup flows). Suggested: investigate the embed-update queue path locally (embed sync queue depth / debounce timing); confirm whether GitHub CI (fresh DB, sharded) sees it — if not, likely local-env-shape-dependent.
- **[note]** "Departure from full generic event sends notification (ROK-919)", "Bench player promoted when main slot vacates", and "Completion cron does not complete events with future end times" each failed only under full-suite load and pass in isolated category runs on the same env (demonstrated: "Full roster FULL" / "Tentative displaced" failed full-suite, passed isolated) — the documented single-worker load-flake family, no action. ROK-1347 ×2 / ROK-1370 / ROK-985 remain the already-documented known set (`reference_known_smoke_flakes`).

### 2026-08-15 — combined/unblock-2026-08-15 (surfaced during /unblock-prs dependabot batch validation)

- **[med]** `npm run test:integration -w api` (`api/jest.integration.config.js`, `--runInBand`) dies with `FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory` → `Abort trap: 6` (exit 134). Reproduced TWICE: once at Node's default heap, once with the previously-documented workaround `NODE_OPTIONS=--max-old-space-size=8192` — **8GB is no longer sufficient**, which is a regression against the workaround recorded in `reference_bullmq_ioredis_test_carrier`. Failure point: 74 suites PASS, then `src/events/signups-embed-sync.integration.spec.ts` is reported `FAIL (1002.482 s)` as the OOM victim. That spec is only the carrier, not the cause — run in isolation it passes 8/8 in 6.0s. Jest also prints `Force exiting Jest: Have you considered using --detectOpenHandles` at the end of the isolated run, i.e. the open-handle/socket leak (BullMQ ioredis family) is still live and accumulates across the serial run until heap is exhausted.
  Pre-existing / NOT caused by this branch: the branch is a pure dependabot dependency batch, and `git diff origin/main..HEAD -- api/package.json` touches **only `devDependencies`** (`@types/node`, `eslint`, `globals`, `prettier`, `ts-jest`, `typescript-eslint`) — zero api RUNTIME dependencies changed, so api server behaviour under test is byte-identical to main. The only bump that touches test execution at all is `ts-jest` 29.4.11→29.4.12 (patch), and 74 consecutive passing suites plus the isolated carrier pass demonstrate transpilation is healthy. Invisible in GitHub CI because the integration job is sharded (no single process accumulates the whole suite) AND because this diff is deps-only, so GitHub's path filter skips `api`/`web` jobs entirely.
  Suggested: (a) short term, raise the documented local workaround above 8GB (12–16GB) and update `reference_bullmq_ioredis_test_carrier`, or shard/`--maxWorkers` the local integration run the way CI does; (b) real fix, run the suite with `--detectOpenHandles` to identify the leaking BullMQ/ioredis handles and close them in `afterAll` teardown so heap does not grow monotonically across suites.

### 2026-08-15 — fix/batch-2026-08-15 (per-story reviewer findings)

- **[low]** `api/src/admin/demo-test-core.helpers.ts` — `getNotificationsForTest` has no direct unit test after extraction (only reached via controller specs that mock the whole service); the optional `type` filter branch is uncovered.
  Suggested: one `it` in `demo-test-core.helpers.spec.ts` asserting the type condition and custom `limit` are applied.
- **[low]** `api/src/admin/demo-test-voice.helpers.ts` / `demo-test-lineup.helpers.ts` / `demo-test-scheduled-event.helpers.ts` — `flushVoiceSessionsForTest`, `cancelLineupPhaseJobsForTest`, `triggerScheduledEventCompletionForTest` were added to these pre-existing helper files (ROK-1072) with no co-located spec; the moduleRef delegation bodies run under no test (service/controller specs mock the facade).
  Suggested: minimal specs using the `mockModuleRef` pattern from the new demo-test spec files.
- **[low]** `api/src/admin/demo-data-install-orchestrate.helpers.ts` — ROK-1105 TD-3 split reorders the `origEvents` SELECT after the availability/game-time INSERTs vs the original orchestrator body. Reviewer traced it as behaviorally inert (reads `events`, populated earlier by `installCoreEntities`; INSERTs touch unrelated tables) — recorded here so the sequencing change is on the books, not silent.
- **[nit]** `api/src/admin/demo-test-core.helpers.spec.ts:27`, `demo-test-signups.helpers.spec.ts:23` — new `it` blocks at 63/68 lines trip the 60-line spec-function warn; split before the rule upgrades to error.
- **[nit]** `api/src/admin/demo-data-gen-taste-profile.spec.ts:23` — outer describe arrow now 168 lines (was ~148 pre-ROK-1105); on next touch, split per TESTING.md `{name}.{concern}.spec.ts` convention.

### 2026-08-19 — combined/unblock-2026-08-16 (surfaced during /unblock-prs dependabot batch validation)

- **[high]** `npm run test:integration -w api` is **currently unusable on the local machine** — and this is now demonstrated pre-existing by a clean-baseline run, not inferred. Two distinct terminal signatures observed in one session on the same env:
  1. On `combined/unblock-2026-08-16`: `Segmentation fault: 11` (native crash, no OOM text anywhere in the log) after **64 suites passed**.
  2. On a clean `origin/main@64b30940` checkout (main worktree, untouched tree, node_modules matching main's lockfile): **13 suites FAIL**, all by timeout, with per-suite wall times of **190s–1086s**. No segfault and no OOM in the baseline — the run simply crawls until suites blow their Jest timeout.

  Baseline failures on clean main: `availability.integration.spec.ts` (914s), `cron-jobs/cron-job` (996s), `discord-bot/ad-hoc-suppression` (925s), `discord-bot/services/scheduled-event.capacity-recovery` (1087s), `drizzle/schema/community-lineup-matches` (514s), `igdb/igdb-discover.community-playing` (931s), `lineups/common-ground-taste` (961s), `lineups/lineup-deadline-transition-notify` (192s), `lineups/lineup-notification-tiebreaker` (996s), `lineups/standalone-poll/standalone-poll-reschedule-cycle` (959s), `lineups/submit/lineup-submit` (921s), `notifications/post-event-followup` (956s), `users/users.integration.spec.ts` (327s).

  **Pre-existing / NOT caused by the dependabot batch.** The discriminating experiment was run explicitly: the same command on a clean `origin/main` tree with none of the branch's changes fails 13 suites. The branch's diff touches only `devDependencies` (`@nestjs/testing` 11.1.28→11.1.29, `@types/node`, `eslint`, `globals`, `typescript-eslint`, `@testing-library/*`, `eslint-plugin-react-refresh`, `tsx`) — zero api runtime dependencies. Note the varying signature (heap OOM on 2026-08-15, segfault + timeouts here) is itself evidence of environmental instability rather than a deterministic code fault.

  **This supersedes nothing in the 2026-08-15 entry** — that entry recorded the heap-OOM variant and correctly noted 8GB was insufficient; this entry adds the clean-main baseline that the earlier one lacked, plus the segfault and timeout variants.

  Invisible in GitHub CI for two independent reasons: the integration job is sharded (no single process accumulates the whole serial suite), AND deps-only diffs are skipped by GitHub's `api`/`web` path filter entirely.

  Suggested: (a) treat local `--full` integration results as **unreliable on this host** until fixed — do not gate deps-only PRs on them; (b) investigate host-level contention first (Docker Desktop `raid-ledger-db` had `Up 36 hours` uptime during these runs; a container bounce + `docker system df` check is the cheap first probe); (c) run with `--detectOpenHandles` to chase the BullMQ/ioredis handle leak already recorded in `reference_bullmq_ioredis_test_carrier`; (d) shard the local integration run the way CI does (`--maxWorkers`) so no single process accumulates the whole suite.

### 2026-08-19 — rok-1398-coop-detail-section (surfaced during ROK-1398 TDD test authoring)

- **[med]** `scripts/smoke/game-detail.smoke.spec.ts:16` — `navigateToFirstGame` soft-skips the WHOLE spec on the local dev env: `npx playwright test scripts/smoke/game-detail.smoke.spec.ts` reports `16 skipped` on both projects, because no `a[href*="/games/"]` becomes visible on `/games` within the 10s probe. The DB is not empty — `GET /games/search?q=a` returns seeded games (ARK, etc.) — so the skip reason it prints (`No games seeded — skipping game detail tests`) is misleading. Pre-existing: `navigateToFirstGame` is untouched by this branch (the branch only appends a new `describe` block that navigates to `/games/:id` directly), and the env is a plain `deploy_dev.sh` dev stack. Net effect: every existing game-detail smoke assertion is silently inert locally, so local Playwright runs give false confidence for game-detail UI work.
  Suggested: replace the soft `test.skip` with an explicit precondition (query `/games/search` via `api-helpers`, hard-fail if games exist but no card link renders), and investigate why `/games` renders no card links locally (discover-page gating / lineup banner overlay / onboarding modal are the likely candidates).

### 2026-08-19 — review/coop-batch-1 (surfaced during ROK-1398/1399 pre-review e2e gate)

- **[med]** `tools/test-bot` Discord smoke — 15/112 tests fail locally on this host, identical set across two consecutive runs (departure-notification cluster ×6, ROK-1347 reconciliation ×2, ROK-1370 lock-in, ROK-1351 series bindings ×2, deactivation-cancel, completion-cron, ROK-985 attendance "No games", ROK-1390 quick-play announce). Believed pre-existing/environmental, NOT branch-caused: the branch diff over `api/src/notifications`, `api/src/discord-bot`, `api/src/events`, `api/src/lineups`, and `tools/test-bot` is EMPTY (verified via git diff against origin/main); smoke setup `resetToSeed`s the DB; failure signature is 100–400s timeouts matching the host instability documented in the 2026-08-19 /unblock-prs entry above (clean-main integration suites timing out at 190–1086s on this machine). Not yet confirmed on a clean-main smoke run (env was needed for the operator review window) — GitHub CI's fresh runners are the deciding gate for the batch.
  Suggested: fold into the existing host-instability investigation (Docker container bounce + `docker system df` first probe); rerun local smoke after the host issue clears before trusting local smoke for gating again.

### 2026-08-20 — review/coop-batch-2 (surfaced during devedup-rl review of ROK-1400/1402)

- **[low]** `api` local typecheck noise — **duplicate of the 2026-08-16 entry above (`api/tsconfig.json`, line ~789); that entry is the accurate one.** CORRECTION (measured 2026-08-21, ROK-1401 ship gate): this is NOT worktree-specific as originally written here. Filtering the jest-globals errors leaves **exactly 156 residual errors in the rok-1401 worktree AND exactly 156 in the main-repo checkout** — identical, and every one lives in a `*.spec.ts` / `*.spec-helpers.ts` file. So it is a repo-wide local `@types` auto-discovery quirk, not a worktree `npm install` gap; a per-worktree reinstall will NOT fix it. Agents keep rediscovering it once per batch — the durable fix is the `types`-array/tsconfig change suggested in the earlier entry.
  Suggested: reproduce once with a fresh `npm install` inside a worktree and either fix the types resolution (tsconfig `types` array vs hoisting) or document the known-benign signature in TESTING.md.
  **VERIFIED RESOLVED 2026-09-01** — measured on a clean main checkout: `npx tsc --noEmit -p api/tsconfig.json` exits **0** with **zero** errors (no TS2304/TS2593 jest-globals noise, and none of the 156 residual spec errors). Fixed at some point after 2026-08-21; no `types` array was needed. Do NOT re-file or 'fix' this — the local typecheck is a trustworthy signal today.
- **[low]** `web/src/hooks/use-session-state.ts` vs `web/src/pages/games/use-coop-filter-state.ts` — two sessionStorage hooks landed in one batch (deliberate, to avoid cross-agent file races). Once the shared hook has a validate param, the page-local one should become a thin wrapper.

### 2026-08-21 — rok-1401-coop-fit-signals (surfaced during ROK-1401 `validate-ci.sh --full`)

- ~~**[high]** `scripts/validate-ci.sh:250-252` (+ `run_typecheck` at `:418-426`) — **a real TypeScript failure is reported as `SKIPPED`, not `FAIL`, and the run continues.** `run_step` uses `rc == 2` as the SKIPPED sentinel (`elif [ "$rc" -eq 2 ]; then ... record_result "$name" "SKIPPED"`), but `tsc` exits with **2** when diagnostics are present (verified on this host: `npx tsc --noEmit <file-with-TS2322>; echo $?` → `2`). The ROK-1336 #5 fix (`npx tsc --noEmit -p api/tsconfig.json || return $?`) faithfully propagates tsc's rc, which then collides with the sentinel. Observed live: my `--full` run printed `TypeScript (all): SKIPPED` while `npx tsc --noEmit -p api/tsconfig.json` on the same tree emitted 3× `TS2345` in `api/src/lineups/lineups-match-response.helpers.spec.ts`. Pre-existing — `scripts/validate-ci.sh` is untouched by this branch, and the collision is structural (any tsc error yields rc 2). Note the backlog's 2026-07 audit line ~609 marks an *earlier* `run_typecheck` masking bug as fixed; this is the successor bug that fix left behind. Only GitHub CI catches these today.~~ **FIXED 2026-08-30** — `run_step` no longer infers a skip from an exit code. A step now declares one explicitly via `skip_step` AND must exit cleanly, so a tool returning 2 for a real failure (`tsc`, `tsc -b`) can no longer be reported as SKIPPED. Regression test: `scripts/test/validate-ci-skip-contract.test.sh` (verified non-vacuous — 5 cases fail against the pre-fix script).
  Suggested: stop overloading the exit code — have skip-capable steps `return 200` (or set a `STEP_SKIPPED=1` global) instead of `2`, and/or normalize in `run_typecheck` with `|| return 1`.

### 2026-08-21 — rok-1401-coop-fit-signals (surfaced during ROK-1401 local verification)

- **[med]** `web/src/hooks/use-ai-suggestions.test.ts` (ROK-1316 pending→poll cases) — real-timer polling tests (15 × 3s budgets, 70s per-test timeout) fail non-deterministically whenever the host sleeps mid-run: observed 2-of-6 failing with per-test durations of 394s–996s on BOTH this branch AND a clean `origin/main` (0c380c29) checkout; `pmset -g log` shows the laptop cycling Sleep/DarkWake every ~16 min overnight. Pre-existing — file untouched by this branch; varying failing subset between runs. Same mechanism also produced the 9-hour "Playwright FAIL" on review/coop-batch-1 (2026-08-20) and killed two dev subagents mid-response.
  Suggested: (a) fake timers (`vi.useFakeTimers` + `advanceTimersByTime`) for the poll-budget cases so wall-clock sleep can't skew them; (b) `scripts/validate-ci.sh` and `/build` long-running steps should wrap under `caffeinate -is` (process-level sleep assertion) so local gates don't silently degrade on a closed-lid laptop.

### 2026-08-22 — rok-1401-coop-fit-signals (surfaced during ROK-1401 ship gate)

- **[med]** `scripts/validate-ci.sh` integration step — **an integration OOM is reported as a plain `FAIL` with no indication of how much of the suite never ran.** On this branch's `--full` run, jest died at `FATAL ERROR: Reached heap limit` / `Abort trap: 6` after **75 of 134** integration specs; the other **59 never executed** — including BOTH specs this branch adds (`api/src/lineups/common-ground-coop.integration.spec.ts`, `api/src/lineups/lineups-coop-fit.integration.spec.ts`) plus `common-ground.integration.spec.ts`, `lineups-matches.integration.spec.ts`, `cooptimus-sync.integration.spec.ts` and `cooptimus-prose-gating.integration.spec.ts`. The log's long run of `PASS` lines plus a well-documented OOM (entry 2026-08-16, line ~827) makes "known flake, everything relevant passed" the natural read — and it is wrong. The OOM itself is already tracked; this bullet is about the **reporting**, which is what makes it dangerous. All 59 were re-run here in batches of 14 and pass (6 co-op specs: 96 tests; remainder green), so nothing is failing — the gap was purely invisible coverage loss.
  Suggested: on a non-zero integration exit, have `validate-ci.sh` diff `PASS`/`FAIL` spec paths in the jest output against `find api/src -name '*.integration.spec.ts'` and print `N spec(s) never ran:` with the list, so the summary can never imply coverage it did not achieve. Cheap follow-on: batch the local invocation (~14 specs per process) instead of one `--runInBand` process, which sidesteps the OOM entirely — raising `--max-old-space-size` does not (8GB already documented insufficient).
- **[nit]** `scripts/smoke/lineup-nominating-composite.smoke.spec.ts` (pre-existing, from #924) — `await page.waitForTimeout(500)` violates the STRICT "no `sleep()` in smoke tests" rule. Untouched by this branch (only the trailing `void apiGet` suppression was corrected here, since ROK-1401's test now genuinely uses that import).
  Suggested: replace with the deterministic wait the surrounding assertions already use (`expect(...).toBeVisible({ timeout })` / `pollForCondition`).

### 2026-08-23 — fix/poll-nudge-24h-cadence (surfaced during poll-nudge cadence verification)

- **[med]** `api/src/lineups/standalone-poll/standalone-poll-reschedule-cycle.integration.spec.ts` — **cold-start schema race: the first integration run in a session can fail 18 tests with migration DDL errors rather than assertion failures.** Observed once on this branch: the run printed a wall of `ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY` errors inside `PL/pgSQL function inline_code_block` (community_lineup_schedule_slots / _votes / _match_members / community_lineup_entries FKs) and reported `18 failed, 25 passed`. Not caused by the branch — the diff touches only two nudge constants, a poll-duration default and doc comments, none of which participate in schema setup. Did not reproduce in **5 consecutive subsequent runs** (1 spec-alone + 4 three-spec combos, 43/43 each), and the same three-spec combo passed 42/42 on a stashed clean `origin/main` tree. Signature is distinct from the documented BullMQ/ioredis socket carrier and the integration-404 carrier: this one fails during test-app schema bootstrap, before any request is made.
  Suggested: have the integration `test-app` bootstrap take an advisory lock (or serialize schema setup across Jest workers) so two workers can't race the same `CREATE`/`ALTER` DDL against the shared test DB; failing that, log a clear "schema bootstrap failed — re-run" banner so the next agent doesn't spend a cycle bisecting their own diff.
- **[med]** `.claude/skills/push/SKILL.md` Step 8.5 — **the Codex pre-push review command is invalid on codex-cli 0.133.0 and silently degrades the gate.** The skill runs `codex review --base main "<critical-only prompt>"`, but 0.133.0 rejects it: `error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'` — the two are mutually exclusive (`Usage: codex review --base <BRANCH> [PROMPT]` notwithstanding). Falling back to a bare `codex review --base main` then hit a second, independent failure: `ERROR codex_models_manager::manager: failed to refresh available models: ... unknown variant \`max\`, expected one of \`none\`, \`minimal\`, \`low\`, \`medium\`, \`high\`, \`xhigh\`` — the installed CLI can't decode the server's model list, so it emitted ~2800 lines of file dump and no verdict. Net effect: the "one review pass" that the trivial-tier fast lane treats as non-negotiable produced nothing, and the skill's own error path ("log and proceed") means this failure is easy to wave through unnoticed on every push.
  Suggested: (a) drop the custom prompt when `--base` is used, or move the critical-only instruction into `~/.codex/config.toml`; (b) upgrade codex-cli past the `reasoning_level: max` variant; (c) have Step 8.5 distinguish "Codex ran and found nothing" from "Codex never ran" in the Step 12 report table, so a silently-skipped review can't read as a passed one.

### 2026-08-23 — fix/cooptimus-combo-from-game-page (surfaced during the Co-Optimus combo ship gate)

- **[med]** `scripts/smoke/admin-user-moderation.smoke.spec.ts:50` — **"kicks a member (Kicked badge appears) then unkicks (badge clears)" is flaky under parallel workers, on BOTH desktop and mobile.** Measured across three full local runs on this branch: run 1 it FAILED on mobile; run 2 it passed while two unrelated specs failed instead; a targeted re-run of the three suspect specs at `--workers=2` produced `41 passed, 2 flaky` with this test flaky on both projects. It passes deterministically in isolation (2/2). No deterministic failure survived isolation in any run, and the branch's diff (Co-Optimus sync + game-detail co-op section) has no path into the kick/unkick flow. Strong prior on the cause: moderation actions revoke the cached admin token shared across smoke workers, so whichever worker holds it next fails — the same interaction already recorded for `blockUser`. This is NOT the documented community-lineup/onboarding flake set; that set also appeared in these runs, separately.
  Suggested: give the moderation specs their own admin token (or re-authenticate after any kick/unkick/ban) instead of sharing the cached one, so a moderation action cannot invalidate a sibling worker's session mid-run.
- **[med]** `scripts/deploy_dev.sh --ci --rebuild` — **`--rebuild` did not refresh `packages/contract/dist/`.** After running it on this branch, `dist/games.schema.d.ts` was still June-dated and did not contain the field this branch adds to the contract, while source, `tsc --noEmit`, and every unit suite were green (they resolve the contract differently than the running servers do). The API and Vite therefore booted against a stale contract, and an e2e run against that env would have "passed" while exercising code that lacked the change. An explicit `npm run build -w @raid-ledger/contract` fixed it; the servers then needed a restart, since Nest's watcher does not watch `node_modules`.
  Suggested: have `--rebuild` actually rebuild the contract workspace (and fail loudly if the emitted `dist` is older than `packages/contract/src`), so a stale `dist` cannot silently invalidate local e2e results.
- **[nit]** Vite binds **IPv6-only** (`[::1]:5173`), so `curl 127.0.0.1:5173` fails while `localhost:5173` succeeds. Any readiness probe using the IPv4 literal reports the web server down when it is healthy — in `validate-ci.sh` that surfaces as `Playwright: SKIPPED — Dev env not responding`, i.e. silently skipped e2e rather than an error.
  Suggested: probe `localhost` (or try both families) in the e2e gate's readiness check.
### 2026-08-23 — fix/batch-2026-08-22 (surfaced during per-story reviewer passes on ROK-1424/1425/1427)

- **[RESOLVED 2026-08-24 — PR pending, branch `fix/emit-connected-isolation`]** ~~**[med]**~~ `api/src/discord-bot/discord-bot-client.service.ts:329-341` — `emitConnected()` wraps
  `emitAsync` in ONE try/catch around the whole chain rather than per-listener. EventEmitter2 invokes
  listeners in a plain loop, so a synchronous throw from any earlier `CONNECTED` subscriber aborts the
  rest and every listener registered after it silently never attaches. Only trace is
  `Error in CONNECTED event handlers`. This is an unclosed candidate root cause for ROK-1425 and is
  NOT fixed by that story's commits.
  Suggested: wrap each subscriber, or emit per-listener with individual catch + log.
  **Fixed:** `emitConnected` now delegates to `emitIsolated` (`discord-bot-event-dispatch.helpers.ts`), which enumerates subscribers and runs each in its own `Promise.allSettled` slot. Premise verified empirically against a real EventEmitter2 (old: A,B then abort; new: A,B,C,D). Regression spec falsified — 8 of 11 cases fail against the pre-fix implementation.
- **[med]** `api/src/discord-bot/listeners/signup-interaction.listener.ts:103`,
  `api/src/discord-bot/listeners/pug-invite.listener.ts:89` — null their handler refs on DISCONNECTED
  without calling `removeListener`. Functionally correct TODAY only because `connect()` always builds a
  new `Client`; if the client service ever reuses a Client across DISCONNECTED/CONNECTED these stack
  instantly. Half-migrated pattern now that eight siblings use `DiscordListenerBinding`.
  Suggested: convert both to `DiscordListenerBinding` for uniformity.
- **[low]** `api/src/discord-bot/listeners/discord-listener-binding.ts:19-24` — `GatewayBinding` is
  exported with `handler: (...args: never[]) => void`. Every function is assignable to that, so the
  `gatewayBinding()` factory can be bypassed with a wrong handler signature that still typechecks.
  Suggested: brand the type, or don't export it and type `attach` as `ReturnType<typeof gatewayBinding>[]`.
- **[nit]** `api/src/discord-bot/listeners/reschedule-response.listener.ts` — 298/300 eslint-counted
  lines. Two lines of headroom before `max-lines` hard-fails CI. Next author must extract first.
- **[low]** `api/src/notifications/live-noshow-grace.helpers.ts:47-53` — Phase 1 reads the running-late
  marker off `fetchNonBenchSignups` (filtered to `status='signed_up'` + non-bench) while Phase 2's
  `fetchLateGraceByUserId` filters only on `eventId` + `runningLateAt IS NOT NULL`. A user benched or
  roached-out between +5 and +15 gets Phase 2 grace that Phase 1 would not grant. Always errs toward
  suppressing an alert, and rare.
  Suggested: add the same `status='signed_up'` predicate to `fetchLateGraceByUserId`.
- **[low]** `api/src/notifications/live-noshow.helpers.ts:82` — `findLiveEventsInNoShowWindow` requires
  the event to still be running, so on a short (~30 min) event a default-grace deferral can push the
  Phase 2 deadline to or past the event end and the escalation never fires at all. Pre-fix it fired at
  start+15. Probably correct product behavior ("their slot is free to PUG" is useless after the event),
  but it is an unstated consequence of stacking grace on the phase offset.
  Suggested: one sentence in the helper's header JSDoc so it reads as intentional.
- **[nit]** `api/src/notifications/live-noshow-running-late.integration.spec.ts:186,199,221,238,253,316`
  — `toEqual([...])` against selects with no `orderBy`. Every case expects exactly one row today, so
  they pass deterministically, but a case that grows to two rows becomes a nondeterministic flake.
  Suggested: `expect.arrayContaining` + explicit length, or sort uniformly (`:296` already sorts).
- **[low]** `packages/contract/src/game-time.schema.ts:108-113` — `GameTimeAbsenceInputSchema` is bare
  `z.string()` for both dates: no format check, no `start <= end`, no past guard. A user can create a
  2020-01-01 → 2020-01-07 absence, get a 201 and a success toast, and see nothing appear (ROK-1427 now
  filters it out of the list). Pre-existing weak validation that ROK-1427 makes more visible.
  Suggested: `.refine()` for `endDate >= startDate`, plus a client-side `min` on the start input or a
  toast when the created row is already expired.
- **[low]** `api/src/users/game-time-absence.helpers.ts:36-55` — `fetchAbsencesEndingOnOrAfter` lacks
  the `isMissingTableError` → `[]` guard that its sibling `fetchAbsences`
  (`game-time-composite.helpers.ts:105-124`) has for pre-migration DBs. Not a regression (the pre-fix
  `getAbsences` had no guard either), but the inconsistency is now adjacent and visible.
- **[nit]** `api/src/users/users-me.controller.ts` (~295) and `api/src/users/game-time.service.ts`
  (~289) are close to the 300-line `max-lines` error. Both pass today; the next feature touching either
  will trip CI.
- **[nit]** `api/src/users/game-time.integration.spec.ts` — `describeRok1427` is 131 lines and
  `describeGameTime` has grown to ~547 (both over the 60-line `max-lines-per-function` warn; file is
  ~613 of the 750 test-file cap).
  Suggested: split to `game-time.absence-expiry.integration.spec.ts` per the TESTING.md
  `{name}.{concern}.spec.ts` convention on next touch.
- **[nit]** `api/src/users/game-time.types.ts:47` + `web/src/lib/api/game-time-api.ts:55` —
  `AbsenceRecord` is hand-duplicated on both sides rather than shared through
  `packages/contract`, which `project-context.md` prohibits. Pre-existing; this batch does not widen it.

### 2026-08-24 — fix/post-event-followup-prefill (surfaced during the follow-up-prefill ship gate)

- **[med — pre-existing local-env, NOT this branch]** `./scripts/validate-ci.sh --full` failed at the **Integration tests (api)** step on `api/src/availability/availability.integration.spec.ts` → `Availability (integration) › CRUD operations › should delete an availability window` with a bare **`socket hang up`** (no assertion failure, no stack in test code). Same family as the documented BullMQ/ioredis cross-suite socket carrier (`reference_bullmq_ioredis_test_carrier`, and the 2026-08-21/22 entries above recording the local full-suite OOM + "unreliable on this host" verdict). Evidence it is NOT this branch: (a) the spec passes **17/17 run in isolation**; (b) the branch diff touches `notifications/`, `lineups/scheduling/`, `discord-bot/listeners/`, one migration and the create-event web form — nothing in `availability/`, redis, or queue config; (c) the branch's own integration surface passes **101/101 across 9 suites** (`src/lineups/scheduling` + `src/notifications/post-event-followup`); (d) api unit is green at **6883/6883**. GitHub CI shards integration 3×3 so it never accumulates the handles that produce this. Suggested: no new action — this is another datapoint for the existing "shard or fix the BullMQ/ioredis handle leak in local `test:integration`" item; worth noting the carrier spec rotates (previously `signups-embed-sync`, now `availability`), which is itself evidence of nondeterministic handle accumulation rather than a spec-specific bug.

### 2026-08-24 — rok-1436-bullmq6-ioredis6 (surfaced during the ROK-1436 integration A/B)

- **[low — pre-existing local-env, NOT this branch]** `api/src/common/testing/test-app.ts:131` — `provisionDatabase` fails with **`Timed out after 10000ms while waiting for container ports to be bound to the host`** (Testcontainers `PostgreSqlContainer.start`), taking down every test in the affected spec file (7 failures in one run of `public-lineup.integration.spec.ts`). Surfaced only under sustained local churn — 1 hit in 12 consecutive 14-spec batch runs while A/B-ing the socket carrier — i.e. Docker is slow to bind ports when containers are created and torn down back to back. Evidence it is NOT this branch: the same batch passed 5/6 on the bumped tree and 6/6 on a clean `origin/main` checkout, and the failure is in Testcontainers' own port-inspection retry loop before any app code loads. Note **ROK-1288** ("bump Testcontainers port-bind timeout to 30s in test-app.ts") proposed exactly this fix and was **Canceled**, so this entry is a datapoint for re-deciding rather than a new proposal. Suggested: no action unless it recurs outside back-to-back-run stress; if it does, revisit ROK-1288's 30s timeout.
- **[info]** The BullMQ/ioredis cross-suite socket carrier reproduced **on untouched `origin/main`** (1/6 runs, `Parse Error: Expected HTTP/, RTSP/ or ICE/` in `public-lineup.integration.spec.ts`) and **0/6 runs** on the bullmq 6 + ioredis 6 branch. Recorded here because the majors were a plausible moment for that behaviour to change: at n=6 per arm there is no evidence the bump worsens it, and no basis to claim it improves it either. Already-open carrier items above stand unchanged.
- ~~**[med — pre-existing fleet infra, NOT this branch]** `api/package.json:25` — `"test:cov": "jest --coverage"` carries **no `--max-old-space-size`**, unlike its sibling `test:integration` (which sets 4096). Node's default heap is derived from the host, so the api unit suite peaks *between* the two ceilings: **4288 MB on the operator laptop (passes 6944/6944)** vs **3264 MB inside `rl-runner-1` (OOMs)**. Result: `rl_validate_ci --full` dies at the "Unit tests + coverage" step on the fleet with `FATAL ERROR: Ineffective mark-compacts near heap limit` / `npm error code 134`, and stop-on-first-failure means **integration never runs on the fleet at all**. Verified NOT branch-specific: re-running the identical command on the same runner with `NODE_OPTIONS="--max-old-space-size=8192"` passes **527 suites / 6944 tests**. Note `rl_fleet_health.perf_summary.last_validate_ci` shows the previous fleet run (`rok-1417-quickplay-observability`, 2026-07-28) also exited 1 — plausibly the same wall. Suggested: add `NODE_OPTIONS="--max-old-space-size=8192"` to the `test:cov` script (mirroring `test:integration`), or set it fleet-side in the runner image, so the fleet path is usable for any branch.~~ **FIXED 2026-08-25** — `test:cov` now sets `NODE_OPTIONS="--max-old-space-size=8192"` (operator-approved follow-up; dogfooded on `rl-runner-1`).

### 2026-08-25 — fix-test-cov-heap-flag (surfaced while shipping PR #1050)

- **[high — pre-existing CI config gap, NOT caused by that branch]** `.github/workflows/ci.yml:36-46` — the `code` path filter lists `'package.json'` / `'package-lock.json'`, which in `dorny/paths-filter` match **only the repo-root files**. There is no `api/package.json` / `web/package.json` pattern in ANY filter (`code`, `api`, `web`, `contract` all miss them). Net effect: a change to a **workspace** `package.json` matches nothing, so `lint`, `unit-tests-api`, `unit-tests-web`, `integration-test-shard`, `smoke-test-shard` and the rest all report `skipping`, `mergeStateStatus` goes `CLEAN`, and auto-merge lands the change with **zero real CI**. Observed directly on PR #1050 (diff = `api/package.json` + `TECH-DEBT-BACKLOG.md`): every substantive job skipped, and `lint` skipping is the proof — `lint` is gated on `code`, so `code` evaluated false for `api/package.json`. That PR was still safe because the full gate had been run on the fleet first, but nothing in CI enforced that. Note this also partly undercuts the CLAUDE.md "Local CI — lite gate" rule for dependency changes: that rule says GitHub treats dependency files as `code` (so lint runs) but not `api`/`web` — true for the ROOT manifests, but for a **workspace** manifest not even lint runs. A workspace-only dep bump would land completely unverified. Suggested: add `'*/package.json'`, `'*/package-lock.json'` (or explicit `api/package.json`, `web/package.json`) to the `code` filter, and add `'api/package.json'` to `api` / `'web/package.json'` to `web` so unit+integration engage for a workspace dep change.

### 2026-08-29 — rok-1439-milestone-nominator-name (surfaced during the ROK-1439 pre-push review pass)

- **[med — pre-existing local tooling, NOT this branch]** The `codex` CLI (`/Users/sdodge/.nvm/versions/node/v24.11.1/bin/codex`) floods `codex review` stdout with a repeating startup error, once per exec step: `ERROR codex_models_manager::manager: failed to refresh available models: stream disconnected before completion: failed to decode models response: unknown variant `max`, expected one of `none`, `minimal`, `low`, `medium`, `high`, `xhigh``. Each occurrence dumps the **entire** server model-list JSON inline, so a review of a 2-file diff produced **3.4 MB / 327 KB of output across two runs** and the actual one-paragraph verdict was the very last line, unfindable without `grep -v`. The review itself completes and exits 0 — this is noise, not failure, which is what makes it expensive: the first run reads as a crash. Root cause is a client/server version skew — the deployed model list advertises a `max` reasoning effort that this CLI build's enum doesn't know. Evidence it is NOT this branch: the error fires during CLI startup before any diff is read, and is independent of repo state. Suggested: `npm i -g @openai/codex@latest` to pick up a build that knows the `max` variant; if the skew persists, have `.claude/skills/security-review` pipe through `grep -v 'codex_models_manager'` and print only the trailing verdict.
- **[low — pre-existing, NOT this branch]** `codex review` self-selects verification commands and ran `cd api && npm run build -- --noEmit`, which fails immediately: `nest build` has no `--noEmit` option (`error: unknown option '--noEmit'`). Harmless — Codex recovered and re-ran plain `npm run build`, which succeeded — but it burns a step every review and the failed-command output reads like a branch breakage in the log. Separately, Codex's own `tsc` invocation reports hundreds of `Cannot find name 'it'` / `Cannot find name 'expect'` errors across `*.spec.ts` (e.g. `src/version/version.controller.spec.ts:44`) because it typechecks specs without the Jest types; the repo's own `npx tsc --noEmit -p api/tsconfig.json` is clean. Suggested: no code change — note in the security-review skill that Codex's ad-hoc build/tsc output is not the repo gate and should be ignored in favour of `validate-ci.sh`.
### 2026-08-29 — rok-1440-public-lineup-invitees (surfaced during the ROK-1440 pre-push gate)

- ~~**[high — pre-existing gate bug, NOT this branch]** `scripts/validate-ci.sh:250-252` — `run_step()` maps **exit code 2 to `SKIPPED`**, so any step whose command exits 2 is silently reclassified as "not run" instead of failing the gate. `npm run build -w web` runs `tsc -b && vite build`, and **`tsc -b` exits 2 on a type error**, which npm surfaces as `npm error code 2`. Net effect: a genuinely broken web build prints its full error mid-run, is recorded as `Build (all workspaces): SKIPPED`, and the script still terminates with a green **`All checks passed!`** summary. Observed directly on this branch: `use-scheduling.ts` imported `addPollMembers` from the `api-client` barrel before the re-export existed, `npm run build -w web` failed with `error TS2305`, and `validate-ci.sh --static` reported all-clear. The failure was only caught because the SKIPPED row looked wrong for a diff that touches the contract and was investigated by hand — a less suspicious run would have pushed a branch that cannot build. Note the same reclassification applies to every step, so any tool that uses exit 2 for "real failure" is affected; `tsc -b` is simply the one in the default path. Evidence it is pre-existing: `git diff origin/main -- scripts/` is empty on this branch. Suggested: reserve a dedicated sentinel for skips (e.g. exit 77, or have skip-capable steps `echo` a marker) and treat 2 as a failure like any other non-zero code; at minimum, special-case the build/typecheck steps so `tsc`'s exit 2 cannot be read as a skip.~~ **FIXED 2026-08-30** — `run_step` no longer infers a skip from an exit code. A step now declares one explicitly via `skip_step` AND must exit cleanly, so a tool returning 2 for a real failure (`tsc`, `tsc -b`) can no longer be reported as SKIPPED. Regression test: `scripts/test/validate-ci-skip-contract.test.sh` (verified non-vacuous — 5 cases fail against the pre-fix script).
- **[low — pre-existing, NOT this branch]** `npx tsc --noEmit -p web/tsconfig.json` does **not** reproduce the web build's type errors — it passed clean while `npm run build -w web` (`tsc -b`) failed with `TS2305` on a missing barrel export. The project-references build (`tsc -b`) is the authoritative check for the web workspace; a bare `--noEmit -p` run gives false confidence. Suggested: no code change, but agents should verify web type-safety with `npm run build -w web` (or `tsc -b`) rather than `tsc --noEmit -p web/tsconfig.json`; worth a line in `TESTING.md` or `CLAUDE.md` since the wrong invocation looks authoritative.
- **[low — non-reproducible local integration flake, likely NOT this branch]** `api/src/lineups/lineups-matches.integration.spec.ts` failed **1 test of 19** on the first run of an 11-spec local batch (`Test Suites: 1 failed, 10 passed` / `Tests: 1 failed, 146 passed`), then passed **19/19 in isolation** and **147/147 on an identical re-run of the same batch**. Honest caveat: the failing assertion's message was **not captured** — the run was grepped for summary lines only, and the failure did not recur to be re-read, so this entry cannot name the failing case. Recorded rather than dismissed per the STRICT no-dismissal rule, and flagged as *likely* (not confirmed) another datapoint in the documented cross-suite carrier family (`reference_bullmq_ioredis_test_carrier`, `reference_integration_404_socket_carrier`, and the 2026-08-24 entries above), which has the same signature: a spec that is green alone and green on re-run failing once mid-batch. Branch context: this branch touches `lineups.controller.ts` invitee guards and adds `source: 'added'` to the match-member enum, neither of which `lineups-matches.integration.spec.ts` exercises, and GitHub CI shards integration 3×3 so it never accumulates the handles that produce this. Suggested: no action on its own; if it recurs, capture the full failure output (`--verbose`, no grep) before triaging, and add it to the carrier item rather than opening a new one.
- **[med — pre-existing CI smoke failure, NOT this branch]** `scripts/smoke/calendar.smoke.spec.ts:159-195` — `Regression: ROK-1315 — calendar shows gameless events when filter chip active › gameless event remains visible after the user opens the chip and clicks "None"` fails on GitHub `smoke-test-shard (1/5)` with `expect(locator).toBeVisible() failed` / `Error: element(s) not found` on `locator('.week-event-block').filter({ hasText: 'smoke-w1|w2|w3-gameless-event-remains-visible-<ts>-1-rok-1315-variety-night-2' }).first()` (15s timeout). Fails all three attempts; each retry seeds a fresh event (`w1` → `w2` → `w3`), so it is not a stale-fixture collision — the seeded gameless event simply never renders as a week block in time. Evidence it is pre-existing: the **identical** test is the sole failure of `smoke-test-shard (1/5)` on the latest `origin/main` CI run (run `33279909851`, head `82d313fc`, i.e. ROK-1439/PR #1058 post-merge), a commit this branch already contains; PR #1059 touches no calendar, events, or week-view code. Distinct from the `navigation.smoke.spec.ts` calendar race already tracked in `reference_known_smoke_flakes` — this one does not clear on rerun. Suggested: check whether the seeded event's start lands outside the rendered week window (the retry timestamps advance, so a boundary/TZ edge is plausible) before assuming a render race; do not add a blanket retry.

### 2026-08-30 — rok-validate-ci-skip-sentinel (surfaced while fixing the run_step skip bug)

- ~~**[med — pre-existing, NOT this branch]** `scripts/test/validate-ci-integration-shards.test.sh` fails 2 of 21 cases: `AC-M10-2` asserts each integration shard runs with `NODE_OPTIONS=--max-old-space-size=3072`, but `validate-ci.sh` actually uses **4096** on all four shards. Both the structural grep and the behavioral stub assertion fail. Verified pre-existing by stashing this branch's `validate-ci.sh` change and re-running: it fails identically against the unmodified script, and the assertion concerns heap sizing, not the skip mechanism this branch touches. Either the test or the script is stale — someone changed the heap ceiling (plausibly the same 8192 bump recorded in the 2026-08-25 entry above) without updating the AC. Suggested: decide which value is intended and align the other; until then `scripts/test/run-all.sh` cannot be green.~~ **FIXED 2026-08-30** — the stale AC was aligned to 4096 (the script is correct; ROK-1268 deliberately raised 3072→4096 and the assertion was never updated), the Docker-probe test now gates on daemon availability (skips locally, HARD-FAILS under CI so coverage can't be silently lost), `scripts/**` was added to the `code` path filter, and `./scripts/test/run-all.sh` now runs in the lint job. Suite is 10/10 in ~5s.
- ~~**[info]** `scripts/test/run-all.sh` is **not wired into CI** — `.github/workflows/ci.yml` only runs `bash -n` over `scripts/*.sh`, so every behavioral shell test under `scripts/test/` is manual-only. That is why the `run_step` skip bug survived: a regression test there would not have caught it in CI regardless. Wiring the runner in is blocked on the failure above (it would make CI red on day one), so this branch adds its regression test alongside the existing ones and leaves the wiring decision to the operator. Suggested: fix the shard-heap assertion, then add `./scripts/test/run-all.sh` to the existing shell-parse job.~~ **FIXED 2026-08-30** — the stale AC was aligned to 4096 (the script is correct; ROK-1268 deliberately raised 3072→4096 and the assertion was never updated), the Docker-probe test now gates on daemon availability (skips locally, HARD-FAILS under CI so coverage can't be silently lost), `scripts/**` was added to the `code` path filter, and `./scripts/test/run-all.sh` now runs in the lint job. Suite is 10/10 in ~5s.
- **[med — latent, newly detectable]** `set -u` + `local x y` (declared, conditionally assigned, unconditionally read) is a **macOS-invisible** bug class in `scripts/*.sh`. macOS ships **bash 3.2.57**, where a declared-but-unassigned `local` is set-and-empty; **bash 4.4+** (GitHub runners, the rl-infra fleet, any Linux box) treats it as **unset**, so `set -euo pipefail` aborts the function with `x: unbound variable`. Verified both directly: `/bin/bash -c 'set -u; f(){ local a b; echo "[${b}]"; }; f'` prints `[]` on macOS and dies on `bash:5.2`. One real instance was fixed in `scripts/sync-local-to-env.sh::resolve_local_jwt_secret` (PR #1061) — it had shipped broken on Linux since 2026-06-07 and could not fail on the operator's laptop. A grep for the shape (`^\s*local +\w+ +\w+\s*$`) finds ~12 further multi-name declarations across `deploy_dev.sh`, `env-lock.sh`, `sync-local-to-env.sh` and `validate-ci.sh`; most assign unconditionally on the next line and are fine, so this needs a read, not a blanket edit. Not swept in PR #1061 — that PR fixed only the instance CI proved, and the remaining candidates are in scripts without behavioral tests, so a speculative sweep would be unverifiable. Suggested: audit the ~12 by hand for the conditional-assign shape, and prefer `local x=""` as the house style in `scripts/*.sh` since the laptop shell can never catch this.

### 2026-08-30 — rok-1441-lan-tonight-presets (surfaced during the ROK-1441 pre-push smoke run)

- **[med — pre-existing, local-state dependent, NOT this branch]** `scripts/smoke/lineup-creation.smoke.spec.ts:104-132` — `Start Lineup button on Games page › Games page shows lineup banner with countdown instead of Start Lineup when active` fails on **both** desktop and mobile against a freshly-seeded local dev DB: `Error: expect(locator).toBeVisible() failed / Locator: getByText(/remaining/i) / Expected: visible / Timeout: 10000ms / Error: element(s) not found`. Root cause is the demo seed, not the assertion: every seeded row in `community_lineups` has `phase_deadline = NULL` (verified by query — ids 1-4 are `voting` / `building` / `decided` / `archived`, all with a null deadline), and the banner resolves to id 3 `Demo: Decided Lineup`. With no `phase_deadline` there is no countdown to render, so `/remaining/i` can never match. The test's own guard cannot save it: it only POSTs a new lineup when `/lineups/banner` returns nothing, and the banner *is* present — so the seeded deadline-less lineup permanently satisfies the guard while failing the assertion. Verified pre-existing by checking out `origin/main` over `web/src` + `scripts/smoke` and re-running: it fails identically, and this branch's diff is confined to the Start-Lineup modal's preset/slider components (no banner, countdown, or `phaseDeadline` code). Note this is a **local-seed** failure — GitHub's smoke shards seed differently and this test is not among the known-failing CI specs, so the gap is in `deploy_dev.sh`'s demo data rather than in CI. Suggested: give at least one demo-seeded lineup a non-null `phase_deadline` (the `building` or `voting` row is the natural choice), or make the test's guard assert on `phaseDeadline` rather than mere banner presence so it creates a usable fixture when the seeded one is deadline-less.

### 2026-08-31 — rok-1444-nomination-target (surfaced during the ROK-1444 build + Chrome MCP gate)

- **[med — design consequence of this branch, operator decision needed]** The ROK-1444 count target can never fire on a **single-nominator lineup**. `checkBuildingQuorum` (`api/src/lineups/quorum/quorum-check.helpers.ts:54-57`) returns `solo lineup; manual advance required` when `loadQuorumGatingVoters` yields `< 2`, and that guard runs **before** the count-target branch, so the target is never evaluated. This is the pre-existing ROK-1118 safety property and was kept deliberately — but it collides with the exact scenario ROK-1444 was written for: the ROK-1441 same-day "Tonight" lineup where one keen person nominates 15 games in the first 20 minutes still sits in `building` until its deadline. Nobody will read the reason string, so it will present as "the target silently doesn't work". Suggested: decide whether a solo nominator should be allowed to trip the count target (the global `LINEUP_AUTO_ADVANCE_MIN_NOMINATIONS` floor of 4 already prevents a 1-game advance), or surface the solo state in the nominating UI so the operator knows the target is inert.
- **[low — design consequence of this branch, deliberate scoping]** `nomination_target_disarmed_at` disarms the **count** trigger only. `checkBuildingQuorum` returns ready via the submission branch *before* the count branch is consulted, so if every expected voter re-stamps `nominations_submitted_at` after a revert, the lineup re-advances despite the sticky disarm. In practice this is guarded by the ROK-1253 24h pause and requires a deliberate re-submit ritual from every voter, and it is unreachable today (see the next item), so it was scoped out rather than fixed. Recording it because the AC wording "a lineup reverted once stays manually controlled" is true only of the count trigger. Suggested: no action unless the submit flow is ever mounted; if it is, decide whether the disarm should gate both branches.
- **[med — pre-existing, NOT this branch]** The **submission-quorum advance path is dead code in the web app**. `useSubmitNominations` (`web/src/hooks/use-lineup-submit.ts:19`) is defined but mounted by no component, and `NominatingComposite` documents the omission as deliberate ("there is no 'submit' verb on this page"), so nothing ever writes the `nominations_submitted_at` stamp that `checkBuildingQuorum`'s primary branch reads. Net effect: before ROK-1444 the only *reachable* `building → voting` transitions were the time deadline and a manual operator advance; the ROK-1296 submission quorum has been inert since the U4 SubmitBar work. ROK-1444's count branch is now the only live auto-advance trigger for the building phase. Flagged rather than fixed — mounting the submit flow is a UX decision, not a bug fix. Suggested: either mount the submit affordance or retire the submission-quorum branch; leaving it half-wired means two mechanisms claim the same job and only one runs.
- **[med — pre-existing, NOT this branch, now visibly inconsistent]** `NominateModal`'s co-op fit warning is keyed off the wrong roster number. `LineupBanner.tsx:268` passes `participantCount={banner.votingEligibleCount}`, which for a **public** lineup is the entire community — 249 on the local dev env. `CoopFitHints` then compares every game's online-co-op max against 249, so effectively every co-op game in the search results carries the "too small for the group" warning and the signal conveys nothing. Verified live in the browser during the ROK-1444 Chrome MCP gate. ROK-1444 hit the identical bug on the nominated-cards surface and fixed it there by switching to Common Ground's `participantCount` (nominators + voters, or invitees + creator when private), so the two surfaces now **disagree**: the same game can be unflagged on its nomination card and flagged in the search row. Not fixed here because `NominateModal` / `CoopFitHints` is ROK-1400/1401 surface and out of ROK-1444's scope. Suggested: pass Common Ground's `participantCount` in `LineupBanner.tsx:268` too — one-line change, and it makes the two surfaces agree.
- **[low — pre-existing repo-config gap, NOT this branch]** **`web/` has no Prettier config**, so `npx prettier --write` on any web file silently falls back to Prettier defaults (2-space, double quotes) and reformats files that are hand-written with single quotes — and, for `NominationCard.tsx`/`.test.tsx`, 4-space indent. `api/` is unaffected (it has its own config and a `format` / `prettier:check` script pair). During this branch that produced ~360 lines of pure formatting churn across six files, which had to be reverted by restoring from `origin/main` and re-applying the semantic edits by hand. Nothing in lint or CI catches it, so the next agent that reaches for `prettier --write` on a web file will repeat it. Suggested: either add a `web/.prettierrc` matching the existing house style (`singleQuote: true`, and decide 2 vs 4 space — the tree is mixed) plus a `prettier:check` script, or add an explicit "do not run prettier on `web/**`" line to `CLAUDE.md`.
- **[low — local-run discipline, NOT a code defect]** Two auth integration specs failed inside an **un-caffeinated** 14-spec local batch with absurd wall times — `src/auth/refresh/refresh-token.integration.spec.ts` (307.8 s) and `src/auth/not-deactivated.guard.integration.spec.ts` (638.9 s), 59 failed tests between them. Both then passed **in isolation in 5.4 s** and the **identical batch passed 14/14 (155 tests) when re-run under `caffeinate -is`**. This branch touches no auth code. Consistent with the documented `feedback_caffeinate_long_local_runs` rule (laptop sleep mid-run), and distinct from the heap-OOM item dated 2026-08-24 further up this file. Recording it as a concrete datapoint because the failure mode is indistinguishable from a real regression until you re-run — 59 red tests in `src/auth/**` is alarming enough to derail a session. Suggested: no code change; consider having the batching helper in `validate-ci.sh` (or any agent-run batch loop) wrap itself in `caffeinate -is` on darwin so this cannot recur.
- **[low — recurrence datapoint, see the 2026-08-29 entry for this same spec]** `api/src/lineups/lineups-matches.integration.spec.ts` again failed **1 test of 19** once mid-batch (`Test Suites: 1 failed, 39 passed` / `Tests: 1 failed, 459 passed` across the 40 `src/lineups` integration specs), then passed **19/19 in isolation** and **40/40 (460 tests) on an identical re-run of the same batch**, all under `caffeinate`. Same spec and same signature as the 2026-08-29 entry, which asked that a recurrence be added to the carrier item rather than opened as a new one — logged here per the "do not edit existing entries" rule, cross-referenced instead. Honest caveat, and a repeat of the same mistake that entry called out: the failing assertion's message was **again not captured**, because the batch run was grepped for summary lines only. Suggested: before the next batch run, drop the `grep` and tee full output to a file so the next recurrence is diagnosable; this spec has now failed-once-then-passed twice across two branches and still has no captured error.
- **[med — Codex round-3 concurrence on the solo-guard item above; operator decision]** Codex independently flagged the solo-nominator gap as a correctness bug ("the create UI advertises early voting based on nomination count, so the target needs to be evaluated before, or explicitly exempted from, the solo guard"). Not actioned, because the obvious fix may be worse than the bug: `checkVotingQuorum` carries the **same** `< 2` guard, so a solo lineup that advanced `building → voting` on its count target would immediately stall in `voting` with no way to reach `decided` except a manual advance. That trades a visible stall in a phase where the operator is already watching for a less obvious one a phase later. Options, in rough order of preference: (a) leave as-is and make the modal copy honest about needing two participants; (b) exempt the count-target branch from the solo guard AND do the same for the voting quorum, so a solo lineup can actually finish; (c) block configuring a target on a lineup that has no invitees. Needs an operator call — (b) changes an existing ROK-1118 safety property in two places.

### 2026-08-31 — rok-1444-nomination-target (post-review reconciliation + rl-review findings)

- **[info — RESOLVES two bullets in the section above]** The solo-nominator gap recorded earlier ("the ROK-1444 count target can never fire on a single-nominator lineup" / "not actioned, because the obvious fix may be worse than the bug") **was actioned** — operator chose the exemption on 2026-08-31 and it shipped in `e6c77bce`. `checkBuildingQuorum` now evaluates the count target BEFORE the ≥2-voter guard when a target is configured. Recorded here rather than by editing the original bullets, per the no-edit rule. My stated objection to that fix was **wrong** and should not be reused: I claimed it would "just move the stall from building to voting", but the phase deadline is a delayed BullMQ job independent of quorum, so a solo lineup that early-advances still reaches `decided` on its voting deadline (verified: `computeTransitionDeadline` always returns a deadline for `voting`, and `applyStatusUpdate` schedules the `voting → decided` job from it). `checkVotingQuorum` deliberately keeps its own solo guard — others can still turn up to vote once voting is open.
- **[med — shipped feature gap, follow-up]** `nominationTargetPct` is **write-once**. `UpdateLineupMetadataSchema` (`packages/contract/src/lineup.schema.ts:141-149`) covers title + description only, so a target set at creation can never be corrected. Combined with the by-design permanent `nomination_target_disarmed_at`, an operator who picks 100% by mistake has no recourse short of manual advance or DB surgery. The UI compounds it: `targetSuffix` returns `''` for unset, unarmed AND disarmed — three distinct states rendered identically, so nothing explains why the promised early advance never happens. Suggested: add `nominationTargetPct` to the metadata update, and give the disarmed state its own copy ("early advance disabled after revert") rather than silence.
- **[low — micro-perf, this branch]** `checkBuildingQuorum` now runs `countNominations` + `readMinNominations` unconditionally above the solo guard, and `loadQuorumGatingVoters`' result goes unused whenever the count-target branch fires. Two extra queries on a path that runs after every nominate / unnominate / vote. Deliberate — the target branch needs both values before the guard — but worth revisiting if the lineup detail page ever gets chatty. Suggested: fetch the floor lazily, or skip the gating-voter query when a target is configured and already satisfied.
- **[low — reviewer note, not actioned]** `resolveNominationCap` was a pure pass-through to `loadEffectiveNominationCap` and has been removed; recording it only so the rename is traceable if a future diff looks for it.
- **[low — recurring CI flake, NOT this branch]** `scripts/smoke/community-lineup.smoke.spec.ts:638` — `Voting phase › match threshold slider is present in StartLineupModal` failed **3/3 attempts** on `smoke-test-shard (1/5)` (run `33439281140`, commit `e6c77bce`) with `expect(modal).toBeVisible()` / `element(s) not found` after 30s, then **passed on a plain re-run of the same commit**. Shard infra was healthy (`API is ready after 1s`, `Web server is ready after 1s`, 139 passed / 1 failed), the test passes locally in 1.4s on that exact commit, and the commit touches neither the banner, the games page nor the modal. The spec already carries a PR #754 flake comment and a 30s timeout bump for the same symptom, so this is at least the second occurrence. Distinct from the `navigation.smoke.spec.ts` calendar race in `reference_known_smoke_flakes` — this one is the ROK-1167 `?test=open-lineup-modal` effect not firing under parallel shard load. Suggested: the effect early-returns unless `isOperatorOrAdmin(user)` is true, so a slow `/auth/me` hydration would silently skip it and leave the modal closed — worth asserting the admin chrome has rendered before navigating, rather than raising the timeout again.

### 2026-09-01 — rok-1445-general-lobby-per-group-threshold (TD2/TD4 evaluated, deferred with evidence)

- **[low — TD4 VERDICT: no corruption, premise does not hold]** `api/src/discord-bot/services/voice-attendance-flush.helpers.ts:260-268` — `extractGameIds` returns `null` ("count every member regardless of game") as soon as one binding on the channel is not a `game-voice-monitor` or has a null game, which is always true for a general-lobby binding. ROK-1445's spec asked whether making N concurrent per-game events on one general-lobby channel the normal case now corrupts per-event attendance. **It does not, and cannot:** the two queries this feeds — `queryActiveEvents` (`:88-94`) and `queryActiveEventsMultiGame` — both hard-filter `eq(schema.events.isAdHoc, false)`, so this path resolves **scheduled** events only and never sees an ad-hoc event. The N-concurrent events ROK-1445 creates are all `isAdHoc = true`; their attendance is owned by `AdHocEventService` / `ad-hoc-participant.service`, not by `findActiveScheduledEvents`. No guard added — a guard here would be dead code. The residual (genuinely pre-existing, unchanged in reachability by this story) shape is: a channel carrying BOTH a general-lobby binding and a live scheduled event attributes a joiner to every active scheduled event in that channel regardless of game. Recording it so the next agent does not re-derive the analysis. Suggested: no action; if the scheduled-event case ever matters, fix it by making `extractGameIds` ignore non-game bindings instead of bailing to `null`.
- **[med — TD2 DEFERRED, backlog premise needs correcting first]** `api/src/discord-bot/listeners/voice-state-gate.handlers.ts:81` + `api/src/discord-bot/services/ad-hoc-event.service.ts:115` — the duplicate `trySuppressForScheduled` query. Deferred out of ROK-1445 after measurement, and the original ROK-1418 framing ("both queries plus the `extended_until` write execute **twice on every game-binding join**") **overstates it**: a join that lands in an existing ad-hoc event runs the query **zero** times (the listener returns via `joinExistingEvent`, and `handleVoiceJoin` short-circuits on `tryJoinExisting` before its own suppression check); a below-threshold join runs it **once**; only a join that actually reaches the spawn runs it **twice**. It is also not amplified by roster size — `handleGameSpecificGroupRoster` calls `handleVoiceJoin` per member, but members 2..N hit `tryJoinExisting` and never reach the suppression check. So the real cost is one redundant query + one redundant idempotent `extended_until` write, on spawn-path joins only. All three fixes the backlog suggests are worse than that: (a) threading the listener's result into `handleVoiceJoin` means a 7th positional boolean on a public service method called from 5+ sites; (b) a TTL memo inside `trySuppressForScheduled` adds a new staleness surface to a suppression guard; (c) dropping the listener-side check loses the `suppressed-scheduled` gate trace and arms 15-minute spawn timers that later no-op. None is a guard-sized change, and all land in `ad-hoc-event.service.ts`, which ROK-1445 otherwise barely touches. Suggested: if it is worth doing, hoist the suppression check + `extended_until` write out of `handleVoiceJoin` into an explicit `ensureNotSuppressed` step the spawn paths call once — a small service refactor with its own tests, not a fold-in. **Filed as ROK-1456 (2026-09-01, operator-approved follow-up)** — that story carries the corrected severity table, the reasons the three ROK-1418 fixes are each worse than the bug, and the ACs. Prefer it over the stale ROK-1418 entry.

### 2026-09-01 — rok-1445-general-lobby-per-group-threshold (surfaced during ROK-1445 fleet Discord smoke)

Full-suite Discord smoke on fleet env `rok-1445` (slot-1, branch `rok-1445-general-lobby-per-group-threshold`): **92 passed / 20 failed / 112 total**. Classified below. 19 of 20 match failures already documented in this file (2026-08 entries at the `[low]` fleet-env line, the `[high]` ROK-1390 line, and the `[med]` 15/112 local line); the ROK-959 one is a new observation and is recorded here.

- **[low — pre-existing, env-class] 11 × `POST /admin/test/await-processing → 500`.** Departure/cancel notification cluster (ROK-919 ×6), tentative-displacement ×2, slot-vacated, cancellation-notifications, bench-promotion. Identical signature on every one: the test-only queue-drain endpoint 500s under full-suite load. Already documented as an env-class fleet issue ("under full-suite load the env intermittently 500s `POST /admin/test/await-processing`"). NOT branch-caused: the ROK-1445 diff touches no notification, queue, or signup code — it is confined to `api/src/discord-bot/listeners/**` + `presence-game-detector.helpers.ts`. Suggested: unchanged from the existing entry (remote-env profile with longer timeouts / investigate queue-drain under load).
- **[low — pre-existing, known flake] 6 × Scheduled-Event `pollForCondition` timeouts.** SE-created-on-event-create, ROK-944 completion transition, completion-cron future-end-times, ROK-1347 reconciliation ×2, ROK-1370 lock-in. ROK-1347 ×2 are the documented rerun-flake pair; the rest share the same SE-polling shape already recorded as env-class. Branch touches no scheduled-event code.
- **[low — pre-existing, seed-class] 1 × ROK-985 attendance pipeline: `No games for ROK-985 test`.** Already documented verbatim ("the fleet env's demo seed lacks the game fixtures the test expects (passes on local/CI seeds)"). Fails in 0.0s — never reaches app code. Note the env is NOT gameless (52 usable games via `rl_db_query`); the fixture wants specific game rows the demo seed lacks.
- **[high — pre-existing, ALREADY DOCUMENTED, still failing] 1 × ROK-1390 series-linked quick-play announce.** `pollForEmbed timed out after 60000ms`. This file already records it as failing on a clean `origin/main@ef71d919` in an isolated voice-category baseline run, with the same signature. Re-confirmed here. **Still unfixed, and still invisible to GitHub CI** (`SMOKE_SKIP_VOICE_JOIN=1` — no UDP voice on runners), which means voice-join regressions have no CI gate at all. Suggested: unchanged — the existing entry's dedicated bisect story (#1006→#1015, prime suspect the #1015 binding-PATCH rewrite).
- **[RETRACTED — this was a BRANCH REGRESSION, not a pre-existing failure] 1 × ROK-959 "Sibling binding suppresses ad-hoc via channel-level check".** An earlier revision of this entry classified it as a fixture failure on the strength of (a) a `BINDING_MONITOR_REQUIRES_GAME` line in the test's own log and (b) the ROK-1445 diff to `voice-state-gate.handlers.ts` being a pure code move with `ad-hoc-event.service.ts` untouched. **That reasoning was wrong and the classification is retracted.** An isolated voice-category baseline (same test code, two fleet envs) settled it: clean `origin/main` **PASS in 2.8s**, branch **FAIL in 60.4s** (`extendedUntil not set`), reproduced twice on branch. The suppression *code* was indeed unchanged — but its *reachability* was not, and only the code path was checked. Root cause: the ROK-1445 AC9 bot filter was implemented as a wholesale early return in `handleChannelJoin` (`voice-state-join-dispatch.handlers.ts:57`, `if (isBotMember(gm)) return;`) placed before `resolveAllBindings`, so a bot's join never reached `suppressOrCheckThreshold` → `trySuppressForScheduled` and the `extended_until` write never ran. Fixed in-branch by scoping the filter to counts and rosters (AC9 as written) instead of the whole voice pipeline. **Lesson worth keeping: for a reachability regression, an unchanged-code-path argument proves nothing — only a baseline run does.** The `BINDING_MONITOR_REQUIRES_GAME` fixture noise is real but is present on `origin/main` too, so it was a red herring here.
- **[low — pre-existing, fixture-class] `BINDING_MONITOR_REQUIRES_GAME` on voice fixtures.** `[voice] Binding create failed for GamerNight / General / WoW: POST /admin/discord/bindings → 400` appears on **both** `origin/main` and the branch, while a sibling test (`Two bindings on GamerNight: gameA=16, gameB=32`) creates bindings fine — so it is fixture game-resolution, not the endpoint, and not branch-caused. It makes downstream assertions fail with misleading messages instead of failing loudly at setup. Suggested: make the voice fixtures hard-fail on binding-create and pin the games they resolve.

**Voice tests that exercise the ROK-1445 change all PASS:** voice-join attendance session, voice-leave session end, ROK-943 classification + metrics, voice members list, **multi-game channel detects second binding game event**, ROK-1352 ephemeral create→survive→destroy.

### 2026-09-01 — rok-1445-general-lobby-per-group-threshold (code-review findings, deliberately not fixed)

- **[med — pre-existing, made more visible by ROK-1445, deliberately NOT fixed here]** `api/src/discord-bot/listeners/voice-state.handlers.ts:270` — `getGameFilteredCount` is now the **only** count surface in the voice pipeline without a bot filter. ROK-1445 added one to `resolveLobbyGroups`/`humanMembers`, `shouldSpawnImmediately`, `trackChannelMembers` and every roster loop, but this one counts presence-null members **by design** (ROK-697: invisible/console raiders must count toward `minPlayers`). A Discord bot idling in a `game-voice-monitor` channel is presence-null, so it is counted: **one human + one music bot reaches `counted = 2`, clears a `minPlayers = 2` gate, and spawns a 1-human ad-hoc event on the delayed path 15 minutes later** — the exact bug class ROK-1445 was filed to kill, surviving on the game-binding path. Not fixed in this PR on three grounds: (1) AC9's approved target was the general-lobby occupancy gate, which is now filtered; (2) AC12 requires ROK-1390/1394 behaviour on fixed-game binds preserved byte-for-byte, and `counted`/`allConfirmed`/`confirmedCount` feed the degrade-to-null decision; (3) the Discord smoke harness drives voice joins **with the companion bot**, so filtering bots out of this count would make several voice smoke tests unable to reach threshold — that harness is what caught the ROK-959 regression in this same branch, and blinding it is not a trade worth making unreviewed. A comment at the call site records the reasoning. Suggested: filter on `member.user.bot` here and, in the same change, give the smoke fixtures a way to reach threshold without a bot counting (e.g. a `minPlayers: 1` fixture binding, or a test-only join endpoint) — the two must move together or voice smoke goes silent.
- **[low — perf, this branch]** ROK-1445 calls `presenceDetector.detectGames` over the whole channel on **every** lobby join (`voice-state-join.handlers.ts` `lobbyGroupSize`), every presence change (`voice-state.handlers.ts` `switchClearsThreshold`), every lobby leave (`voice-state-leave.handlers.ts` `cancelUnqualifiedLobbySpawns`) and every delayed-spawn fire. Each call is O(members) `detectGameForMember` lookups; those are cache-backed (`gameCache`, 10-min TTL) and in-memory once warm, so a cold cache costs one DB resolve per distinct activity name, not per member per join. Acceptable for realistic channel sizes and deliberately chosen over a stale cached partition — the whole bug was a threshold evaluated against the wrong set, and a memo would reintroduce staleness on the exact value the gate reads. Worth revisiting only if a very large lobby shows up in traces. Suggested: if it ever matters, memoize the partition per `(channelId, presence-version)` rather than per channel, so a presence change invalidates it.

### 2026-09-01 — rok-1449a-embed-chrome (surfaced during ROK-1459 review)

- **[low — orphaned by this branch, deliberately NOT deleted here]** `api/src/discord-bot/utils/push-content.ts:39,53` — `buildAdHocSpawnPushContent` and `buildAdHocCompletedPushContent` lost their only production callers when ROK-1459 §4 deleted `buildAdHocSpawnEmbed` / `buildAdHocUpdateEmbed` / `buildAdHocCompletedEmbed` from `discord-embed.factory.ts`. Both are now spec-only (`push-content.spec.ts:197,226`) exported helpers. They were left in place because spec §4 enumerates exactly which symbols to delete and these are not on it, and because the Quick Play family still borrows the ad-hoc push-content vocabulary until its own migration. Suggested: delete both (and their spec blocks) in slice C alongside the Quick Play family migration, so the ad-hoc push-content surface goes out in one piece rather than leaving a half-dead vocabulary behind.

### 2026-09-01 — rok-1451-lfg-intents (surfaced while writing the ROK-1451 TDD integration spec)

- **[low — pre-existing schema/DB drift, NOT this branch]** `api/src/drizzle/schema/community-lineup-matches.ts:37-45` declares `status: text(...).default('suggested')`, `thresholdMet: boolean(...).default(false)` and `voteCount: integer(...).default(0)`, but the live column has **no DB default**: `api/src/drizzle/migrations/0104_community_lineup_voting.sql:6-7` creates `"status" text NOT NULL` / `"vote_count" integer NOT NULL` with no `DEFAULT`, and the later `0113_ensure_community_lineup_tables.sql:149` restatement (which *does* carry `DEFAULT 'suggested'`) is inside a `CREATE TABLE IF NOT EXISTS` guard that never fires on a DB where 0104 already ran. Because Drizzle emits the literal `DEFAULT` keyword for `.default()` columns, any insert that omits `status` fails at runtime with `PostgresError: null value in column "status" of relation "community_lineup_matches" violates not-null constraint` — reproduced against a fresh Testcontainers DB running the full migration chain, so it is not a local-state artifact. TypeScript cannot catch it: the Drizzle-side `.default()` makes the field optional in `$inferInsert`. Not this branch — ROK-1451 only touched the table from a test fixture, which now supplies the three values explicitly. Suggested: add a migration that sets the real column defaults (`ALTER TABLE community_lineup_matches ALTER COLUMN status SET DEFAULT 'suggested'`, same for `vote_count` / `threshold_met`), or drop the `.default()` calls from the Drizzle schema so the type system forces callers to pass them.

### 2026-09-01 — rok-1451-lfg-intents (surfaced during fleet validate-ci --full)

- **[med — fleet infra, NOT this branch]** Mutagen does not preserve the executable bit when syncing the worktree to an rl-infra runner. On slot-1, **14 of the 15 scripts in `scripts/` are `-rw-r--r--`** (`fix-migration-order.sh`, `deploy_dev.sh`, `env-lock.sh`, `spec-loop.sh`, `clone-prod-to-*.sh`, `validate-ci.sh`, …) despite every one being `100755` in git — confirmed with `git ls-files -s` against `ls -l` inside the container. Anything invoked as an executable rather than through an interpreter dies with `Permission denied`. It stays invisible because `validate-ci.sh` is itself launched as `bash /workspace/scripts/validate-ci.sh`, and the only two direct sibling invocations are on rarely-taken paths. Verbatim failure: `/workspace/scripts/validate-migrations.sh: line 46: /workspace/scripts/fix-migration-order.sh: Permission denied`, surfacing as `Migration validation: FAIL` on a branch whose journal is actually in order (`bash scripts/fix-migration-order.sh --check` → `✓ 166 entries` on the same runner). Evidence this is a known-recurring class rather than a one-off: `scripts/validate-ci.sh:722` already carries a `chmod +x "$REPO_ROOT/scripts/validate-migrations.sh"` immediately before invoking it — the same workaround, applied one level up and never extended to the nested call. Only reachable when a branch adds a migration AND runs `--full` on the fleet, which is why it has gone unnoticed. Worked around this branch by invoking through `bash` (mode-independent, no working-tree mutation); the root cause is untouched. **Suggested:** set the Mutagen sync mode to preserve POSIX permissions in `rl-infra/mutagen/sync-template.yml` + `rl-infra/cli/rl`, then delete the `chmod +x` at `validate-ci.sh:722`. Also note `scripts/deploy_dev.sh` invokes `env-lock.sh` directly in five places — harmless today because deploy_dev never runs on a runner, but it is the same latent trap.

### 2026-09-01 — rok-1314-universal-game-badges (surfaced during ROK-1314 inventory re-run)

- **[med — pre-existing naming drift, NOT this branch, deliberately not reconciled here]** The concept "does the current viewer own / wishlist this game" already exists in the contract under a **different name**. `GameInterestResponseSchema` (`packages/contract/src/games.schema.ts:280`) carries `wishlistedByMe: boolean` (ROK-418) alongside `ownerCount` / `wishlistedCount`, served by the guarded `GET /games/:id/interest` and `GET /games/interest/batch` (`api/src/igdb/igdb.controller.ts:148,284`). ROK-1314 ships `currentUserOwns` / `currentUserWishlisted` on `GameDetailDto`, `CommonGroundGameDto`, `LineupEntryResponseDto` and `VetoGameCardDto` per its ACs and the operator's 2026-09-01 decision, so the codebase now has two names for one idea. Not reconciled in this branch: `wishlistedByMe` has its own consumers and renaming it is a separate, wider change than a badge-consolidation story should carry. Worth recording because ROK-1314's whole premise is eliminating exactly this kind of drift. Suggested: pick one name and migrate the other, or document `wishlistedByMe` as the single-game-interest spelling of the same flag so the next agent doesn't add a third.
- **[low — dead code removed by this branch, recorded for traceability]** `web/src/components/lineups/GameInfoBadges.tsx` had **zero consumers** anywhere in the repo (verified by grep across `web/src`, `api`, `packages`) — its exported `GameInfoBadges` was referenced only by its own definition. Its `OwnerBadge` / `DealBadge` / `PlayerBadge` had already drifted from the `CommonGroundGameCard` copies the ROK-1314 issue body names as the drift pair, but none of that drift was ever rendered. The file is deleted by this branch rather than rewired. Noting it because the ROK-1314 issue body (2026-05-17 research) presents it as a live second surface, which it has not been for some time — future inventory sweeps should verify consumers, not just definitions.

### 2026-09-02 — rok-1467-validate-ci-fleet-flags (two fleet-gate items resolved by ROK-1467)

Both items below are marked resolved by ROK-1467 (`--only-integration` / `--only-unit` / `--no-coverage` in `scripts/validate-ci.sh`, forwarded as `only_integration` / `only_unit` / `no_coverage` by `rl_validate_ci`). Recorded here per the file's convention rather than by editing the original entries.

- **[med — RESOLVED by ROK-1467]** Runner unit-step OOM blocks the whole fleet gate. `rl_validate_ci --full` on a 4 GiB slot dies inside `run_unit_tests` (`npx jest --coverage` under the ROK-1451 cgroup-derived 3072 MB heap cap), and `run_step` stops on first failure — so **integration, migration, container and e2e never ran on the fleet at all**. The 2026-08-25 fix (`test:cov` → `--max-old-space-size=8192`) and the ROK-1451 clamp each moved the ceiling but neither made the coverage run fit in 4 GiB. **Resolved:** `--no-coverage` runs jest/vitest uninstrumented at 3072 MB (verified shape: bare jest completes at the ceiling `--coverage` cannot), and `--only-integration` reaches the integration suite without depending on the unit step passing at all. Coverage thresholds are unaffected — GitHub CI still runs the instrumented suite on every PR.
- **[med — RESOLVED by ROK-1467]** Sourcing `run_integration_tests` by hand hit `ECONNREFUSED 127.0.0.1:6379`. The workaround for the item above was `source scripts/validate-ci.sh; run_integration_tests`, which bypasses flag parsing and the summary, and — because the M9 sidecar spawn is guarded on `/workspace` + `RL_SLOT` and the sharded loop lives in the same function — silently degraded to "no Redis at all" whenever the sourcing shell lacked the orchestrator env (the runner image ships `redis-tools`, not `redis-server`; see memory `reference_rl_runner_no_redis`). **Resolved:** `--only-integration` is a first-class flag, so the sidecar spawn, `REDIS_URL` export, `INTEGRATION_SHARDS` loop, backup-prereq check and summary are the exact same code path `--full` takes. Regression coverage: `scripts/test/validate-ci-only-flags.test.sh` asserts one sidecar spawn + N shards + zero build/lint/unit/e2e invocations.
### 2026-09-02 — rok-1463-lfg-page-reads (surfaced during the ROK-1463 rework verification)

- **[low — flaky, timing-class, NOT this branch]** `api/src/lfg/lfg-offers.integration.spec.ts` → `produces NO offer for a session that started before the intent (AC7b)` failed **once in 21 local runs** (`TZ=UTC npx jest --config ./jest.integration.config.js src/lfg/lfg-offers.integration.spec.ts`); the following **20 consecutive runs all passed**, so the flake-investigation protocol's reproduce step does not clear the bar and no fix was designed. Verbatim message not captured (the first run was piped through a `grep` that kept only the ✕ line); the failing assertion is `expect(offers.map((o) => o.gameId)).toEqual([fresh.id])`. Root-cause hypothesis: the test's positive control creates its Quick Play with `new Date()` from the **Node** clock immediately after `POST /lfg`, whose `lfg_intents.created_at` comes from the **Postgres** clock (`defaultNow()`); `listClearOffers` (`lfg-offers.helpers.ts:75`, via `qualifyingSession()`) requires the session to be strictly later than the intent, so any sub-millisecond ordering or host↔container clock skew drops the control row and the assertion sees `[]`. Confidently NOT caused by this branch: ROK-1463 touches `lfg-history` / `lfg-suggestions*` / `lfg-overlap*` / `lfg-query` (added `requireGame`, exported `liveIntent`) / `lfg.service` / controller / module — `lfg-offers.helpers.ts` and its spec are untouched, and the offers query inlines its own live-intent predicate rather than the exported one. Suggested: have the fixture derive the "after" instant from the created intent's own `createdAt` (`+1s`) instead of `new Date()`, so the control can never tie or lose to the DB clock.
### 2026-09-02 — chore/ci-tooling-taxes PR #1073 (surfaced during GitHub CI smoke-test-shard 5/5)

- **[low — recurring CI flake, NOT this branch]** `scripts/smoke/lineup-votes-per-player.smoke.spec.ts:114` — `create modal contains a votes-per-player slider with data-testid` (mobile) failed `toBeVisible()` on a diff that touched only `navigation.smoke.spec.ts`, `validate-ci.sh`, `ci.yml`, `web/.prettierrc`, `web/package.json` and the backlog archive. Same class as the `community-lineup.smoke.spec.ts:638` slider-modal flake already recorded (2026-08, "recurring CI flake"): a slider inside the create modal is not yet visible on the mobile project when asserted. Second spec file now exhibiting it. Cleared on `gh run rerun --failed`. Suggested: one `expect(modal).toBeVisible()` + `scrollIntoViewIfNeeded()` on the slider before the visibility assertion in both specs, or a shared `openCreateModalAndWait` helper — fix both together, do not bump the timeout.

### 2026-09-02 — rok-1463-lfg-page-reads (surfaced during fleet validate-ci --full, task 7001f0f4073b)

- **[med — fleet infra, NOT this branch]** `scripts/validate-ci.sh::run_unit_tests` on a 4 GiB runner: `npx jest --coverage` dies with `FATAL ERROR: Ineffective mark-compacts near heap limit … JavaScript heap out of memory` at ~2.95 GB against the 3072 MB cap `resolve_heap_mb` derives (75 % of the cgroup limit, ROK-1451). The same step passed on the same slot for ROK-1459 at 00:37Z; by 07:40Z the tree carried #1070 + #1071 + #1073 and the suite crossed the ceiling. The cap did its job (no exit-137 SIGKILL), but it means the fleet `--full` gate can no longer run the unit step with coverage as the suite grows. Worked around by running unit WITHOUT coverage (`NODE_OPTIONS=--max-old-space-size=3072 npx jest`) and the integration shards via the sourced `run_integration_tests` on the runner; GitHub CI (7 GB runners) remains the coverage gate. Suggested: shard the unit run like the integration run (`--shard=i/N`, coverage merged via `--coverageReporters=json` + `nyc merge`) or drop `--coverage` on the fleet path only; note `api/package.json` `test:cov` pins 8192 MB which is why the laptop path is unaffected.

### 2026-09-02 — combined/unblock-2026-09-02 (surfaced while baselining the combined dependabot branch against main)

- **[low — pre-existing on main, NOT the deps]** `web/src/hooks/use-ai-suggestions.test.ts` — `useAiSuggestions — ROK-1316 pending → poll › reports pollExhausted=true when polling caps out still pending` fails deterministically (`expected false to be true`) on a main-equivalent tree (rok-1463 worktree, no web changes) and on the combined branch alike, isolated run 2/2. GitHub `unit-tests-web` is green on main, so it is environment-sensitive (timer/poll budget under fake timers?) rather than broken outright; CLAUDE.md "never dismiss" applies. Suggested: run it under `vitest --run --reporter=verbose` with `CI=1` locally to reproduce the CI environment, then fix the fake-timer advance or the poll cap the assertion depends on.
- **[note — corrects the 2026-09-01 "VERIFIED RESOLVED" annotations at ~L788/L869]** `cd api && npx tsc --noEmit -p tsconfig.json` still reports ~38.8k `TS2304/TS2593/TS2503` jest-globals errors on main-equivalent trees (measured 2026-09-02 in two worktrees); the resolution note holds only for whatever invocation `validate-ci.sh`'s TypeScript step uses (which passes). Not a regression from any branch; the two notes should say "resolved for the validate-ci invocation, not for a bare `tsc -p api/tsconfig.json`".

### 2026-09-02 — combined/unblock-2026-09-02 (surfaced during GitHub CI on the #1063 + #1074 combination)

- **[med — dependency regression, blocks dependabot #1074]** jest 30.4.2 → 30.5.0 (dev-dependencies group #1074) makes every GitHub `integration-test-shard` die with `FATAL ERROR: Ineffective mark-compacts near heap limit` (run 33617158522; shard 3 peaked at 3.8 GB of the 4.1 GB ceiling), while main's identical shards pass on jest 30.4.2 (three consecutive runs 2026-09-02). Root cause is the pre-existing integration-suite retained-memory carrier (BullMQ/ioredis open handles across the serial shard, backlog 2026-08-24 / ROK-1268 family); the jest minor raised per-suite retention enough to tip it. Suggested: land the open-handle teardown story first (`--detectOpenHandles` + `afterAll` cleanup, or a fourth shard), then re-open #1074; alternatively split the dependabot group so jest is bumped alone and can be bisected.

### 2026-09-02 — rok-1470-dynamic-fleet-memory (runner-OOM / "serialize jest" items resolved by ROK-1470)

Marked resolved here per the file's convention rather than by editing the original entries. ROK-1470 keeps the VM at 15 GiB and shares it dynamically: every runner is `mem_limit: 6g` + `mem_reservation: 2g` (`rl-infra/docker-compose.yml`), deliberately over-subscribed, with a host-memory admission gate (`rl-infra/orchestrator/bin/_admission.sh`, `task-start --weight heavy|light`) that makes the over-subscription safe.

- **[med — RESOLVED by ROK-1470]** The manual "never two full jest runs at once" fleet rule. Four runners at `mem_limit: 4g` = 16 GiB of caps before the ~2 GiB of infra services and any env stack, on a 15 GiB VM — so the fleet was only safe if agents hand-serialized heavy work, which is unenforceable across parallel agents and silently violated whenever two of them fired `rl_validate_ci` at the same time. **Resolved:** heavy tasks (`rl_validate_ci` in any suite-running mode, `rl_env_build_image_from_runner`, and any `rl_run_on_runner` command matching jest/vitest/playwright/validate-ci/docker-build) are dispatched `--weight heavy` and wait until host `MemAvailable` >= `RL_HEAVY_TASK_MIN_FREE_MB` (5120) before launching, polling every 10s for up to 30 min. The wait is visible (`rl_status` → `heavy_running` / `heavy_waiting` / `mem_available_mb`; task log gets `[admission] waiting for memory: …`) and a give-up is explicit (`failure_reason: "admission_timeout"`, not a mystery red suite). Docs in `.claude/skills/_shared/rl-infra-fleet.md` now say the serialize rule is retired and must not be re-introduced.
- **[med — RESOLVED by ROK-1470]** The 4 GiB per-runner ceiling that forced the ROK-1451/ROK-1467 heap workarounds. Under a 4g cap `_cgroup_heap_mb` derived a 3072 MB V8 heap, which `jest --coverage` could not fit (2026-09-02 entry above) — hence `--no-coverage` / `--only-integration`. **Resolved (headroom, not a code change):** the same `resolve_heap_mb` now derives **4608 MB** under a 6g cap (6144 x 3/4), and the runner container's `NODE_OPTIONS` is pinned to the same value. `--no-coverage` / `--only-integration` keep their teeth as narrowing flags; they should no longer be *required* just to get the unit step to complete. Worth re-measuring `rl_validate_ci --full` on the new caps before deciding whether the ROK-1467 fallbacks can stop being the default advice — that measurement has NOT been taken yet and is the one open question this entry leaves behind.
### 2026-09-02 — rok-1453-lfg-chips (surfaced while writing the ROK-1453 TDD contract test)

- **[med — pre-existing, NOT this branch]** `packages/contract/src/__tests__/` is **run by no test runner**. Root `vitest.config.ts:16-19` includes only `web/src/**/*.test.{ts,tsx}` + `scripts/smoke/**/*.spec.ts`; `web/vitest.config.ts` has no `include`, so vitest defaults to the `web/` root and never reaches `packages/`; `api/jest.config.js:4` has `rootDir: 'src'` (api only). Verbatim proof: `npx vitest run packages/contract/src/__tests__/signups.schema.spec.ts` → `No test files found, exiting with code 1 … include: web/src/**/*.test.{ts,tsx}, scripts/smoke/**/*.spec.ts`. Worse, the two orphaned files are not merely idle — running them under a throwaway config gives **3 failing tests** in `lineup.schema.spec.ts` (e.g. `LineupDetailResponseSchema.parse` at :199 → `Invalid input: expected boolean, received undefined`), i.e. the contract drifted away from its own spec and nothing reported it. ROK-1453 therefore put its zod-parse assertions in `api/src/lfg/lfg-slug.integration.spec.ts` (parsing the live response body) instead of adding a fourth dead file. Suggested: add `packages/contract/src/**/*.spec.ts` to the root vitest `include` **and** fix the 3 `lineup.schema.spec.ts` failures in the same change — adding the glob alone turns them into a red CI job.

### 2026-09-02 — fleet ops (surfaced during the 1453/1461 sittings)

- **med** `rl-infra/orchestrator` (lease lifecycle) — two idle-claimed slots silently lost their lease today while work was still attached to them. **Slot 3** (agent `sdodge-4c62a526`→`sdodge-4ea8c57e`, 1466 worktree): claimed 16:04Z, `rl_run_on_runner` refused with `no slot — run claim first` at ~22:08Z and `rl_lease_status` showed `current_holder: null` while VM task `5f3046662d0e` (started 17:31Z) was still running on runner 3 and the `rl-slot-3` Mutagen session was still "Watching for changes"; re-claimed 22:10Z (`started_at 17:46:40Z` in VM time) with the task unaffected. **Slot 4** (agent `sdodge-23026449`, 1453 worktree): claimed 14:47Z, env rok-1453 spun on it and redeployed twice; at 23:08Z `rl_env_destroy rok-1453` refused with `env rok-1453 owned by slot 4, you hold slot ''` and the lease was empty — the env itself was still healthy. Both slots had no queue, no operator force-release, and the MCP server process on the laptop stayed up; in both cases the drop happened after >1 h of the MCP process being idle on that slot (the last MCP-side call for slot 4 was the deploy at ~19:02Z, for slot 3 the resync at ~21:48Z, though task 5f3046662d0e's own `run-on-runner-with-heartbeat` was emitting `[heartbeat]` lines every 30 s). Suspected cause: the gc-sweeper's missed-heartbeat window (30 min) is evaluated against the MCP claim heartbeat only, and that heartbeat stops (or is not renewed) when the laptop MCP process sits idle / the laptop sleeps, so an idle-but-owned slot is reaped even while a VM task on it is alive. Side effect: `rl_env_destroy` then needs `force`, and a competing `rl_claim` could Mutagen-overwrite `/workspace` under a running task. `Suggested:` treat a running VM task's heartbeat file as a claim heartbeat (or have `run-on-runner-with-heartbeat` touch the lease), log every gc reap with the reason in `rl_infra_logs`, and have `rl_lease_status` expose `last_reap_reason` so a dropped claim is visible instead of silent.
### 2026-09-02 — rok-1466-playwright-on-fleet (surfaced while wiring the fleet Playwright harness)

- **[med — fleet-path-only Playwright failures, reproducible across BOTH full runs]** Three specs fail on the rl-infra runner against `https://slot-N.gamernight.net` and pass on localhost and GitHub CI. Confirmed in run 1 (direct `npx playwright test`, HEAD `6b1f7e3a`) AND run 2 (through `validate-ci.sh --only-e2e`, HEAD `9dbe237e`), so they are path-specific rather than flakes — every other failure in those runs appeared in one run only. (a) `scripts/smoke/lineup-auto-advance.smoke.spec.ts:111` "badge flips Voting → Scheduling within 5 s" — fails on **both** desktop and mobile; (b) `scripts/smoke/community-lineup.smoke.spec.ts:446` nomination grid single-column — **mobile** only; (c) `scripts/smoke/events.smoke.spec.ts:282` ROK-886 action-buttons overflow menu — **mobile** only. Suspected causes, unverified: the two mobile-layout specs assert on `hidden md:grid`-style breakpoint twins, so a viewport/DPR difference on the runner image's `chromium_headless_shell` would flip which twin renders; the 5 s live-refresh assertion smells like websocket latency through the Cloudflare edge (the slot host is proxied even from the VM). Not this branch — none of the three touches the harness ROK-1466 changed, and all were failing before the auth/target fixes landed. **Suggested:** on a claimed runner, `npx playwright test scripts/smoke/community-lineup.smoke.spec.ts --project=mobile --repeat-each=3` and log `page.viewportSize()` + `window.matchMedia('(min-width: 768px)').matches` against the same spec locally; if the computed viewport matches, the cause is timing and the 5 s budget needs a deterministic wait rather than a longer one.
- **[low — runner-environment or pre-existing, NOT this branch]** `tools/mcp-rl-fleet/src/__tests__/skill-cutover.spec.ts` reports **5 failures on the fleet runner (326/331 passing)** during the tools-unit step. The identical 5 failures reproduce on a **control tree** on the same runner, so they are not caused by any ROK-1466 change — either a runner-environment difference (path/cwd assumptions, or the spec reading repo files that Mutagen excludes) or a genuinely pre-existing break that only the fleet path executes, since `run_tools_tests` is skipped by the `--static` gate most stories use. **Suggested:** run `npm test -w @raid-ledger/mcp-rl-fleet -- skill-cutover` once on the laptop. Green locally ⇒ path/env-dependent, and the spec needs its assumptions pinned to a repo-root anchor rather than cwd; red locally ⇒ a plain pre-existing failure to fix on its own branch.
- **[nit — repo-wide, pre-existing, NOT a one-liner]** Every vitest invocation in the repo logs `(node:NNN) Warning: To load an ES module, set "type": "module" ... ESM syntax in a file loaded as CommonJS (vitest.config.ts:1:1). Use a \`.mjs\` extension or set \`"type": "module"\` in the closest package.json`. Surfaced in the fleet gate's "scripts/smoke helper specs" sub-step, but it is not specific to it: **five** configs are affected — `vitest.config.ts` (root), `web/vitest.config.ts`, and `tools/{mcp-discord,mcp-env,mcp-rl-fleet}/vitest.config.ts` — because none of the owning `package.json` files declare `"type": "module"`. Cosmetic today (vitest transpiles and runs fine), but it is noise on every run and it will become an error whenever Node tightens the CJS/ESM interop path. Not fixed here: renaming only the root config leaves the other four warning, and the two clean fixes are both wider than this story — (a) rename all five to `vitest.config.mts`, updating `scripts/validate-ci.sh:749` plus the literal-string assertions in `scripts/test/validate-ci-vitest-fallback.test.sh` and `validate-ci-fleet-flag.test.sh` (`.github/workflows/ci.yml` already globs `vitest.config.*`), or (b) add `"type": "module"` to the root `package.json`, which changes module resolution for every non-workspace `.js` file in the repo. **Suggested:** (a), as one mechanical commit on its own branch.
- **[low — agent-side capability gap, blocked this story's steps 1 and 3]** The `mcp__mcp-rl-fleet__*` tool family was **not registered in the dev agent's tool set** for this session — `rl_claim({ slot: 3, worktree_path })` returned `Error: No such tool available: mcp__mcp-rl-fleet__rl_claim`. Per CLAUDE.md the operator's `rl` CLI is not an agent fallback and agent-side SSH is closed, so the two runner-side steps of ROK-1466 (capture the verbatim global-setup error on runner-3; run the full desktop+mobile suite against env `rok-1453`) could not be executed and are documented as open in `planning-artifacts/spike-ROK-1466.md`. **Suggested:** either register `mcp-rl-fleet` for dev subagents the same way `mcp-env` is, or route fleet execution through the Lead explicitly in the story brief. Related umbrella: `project_rok_1338_no_ssh_umbrella`.

### 2026-09-02 — rok-1466-playwright-on-fleet (surfaced during the ops-lead runner spike, slot 3)

- **[med — rl-infra runner image drift, NOT this branch]** The runner base image is `mcr.microsoft.com/playwright:v1.60.0-jammy`, which bakes browser builds `chromium-1223` / `chromium_headless_shell-1223` into `/ms-playwright`, while `package-lock.json` pins `@playwright/test` **1.62.1**, which requires build **1234**. Every Playwright run on a runner therefore dies in global setup with `browserType.launch: Executable doesn't exist at /ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell — Looks like Playwright was just updated to 1.62.1. current: mcr.microsoft.com/playwright:v1.60.0-jammy required: v1.62.1-jammy`. Confirmed on slot 3 (task `4ff4f1a330ce`). Worked around by `npx playwright install chromium` inside the runner (`/ms-playwright` is writable — the runner runs as root; 114.7 MiB, ~20s **per fresh runner**). This branch only makes the failure legible (`scripts/smoke/browser-preflight.ts` rewrites the stack into a one-line remedy); the drift itself is untouched and will recur on every `@playwright/test` bump. **Suggested:** pin the runner image's Playwright tag to the repo's version in `rl-infra/runner/Dockerfile` and add a CI/renovate check that fails when `package-lock.json`'s `@playwright/test` version and the image tag diverge — otherwise the next bump silently costs every fleet e2e run a 115 MiB download or a red run.
### 2026-09-02 — rok-1461-poll-lineup-chrome (surfaced during ROK-1461 slice C)

- **[low — pre-existing, blocks `npx tsc --noEmit` in `tools/test-bot`]** `tools/test-bot/src/helpers/voice.ts:6` — `error TS2307: Cannot find module '@discordjs/voice' or its corresponding type declarations.` The package IS declared (`tools/test-bot/package.json:14`, `"@discordjs/voice": "^0.19.2"`) but is absent from the installed tree (`node_modules/@discordjs/` holds builders, collection, formatters, rest, util, ws — no `voice`). Confirmed pre-existing: reproduced with the branch's changes stashed (`git stash -u`) on the ROK-1460 base, and the file is untouched by this story. Every `tools/test-bot` typecheck run therefore reports one error that has nothing to do with the diff, which trains agents to ignore that command's output. Suggested: `npm install` in `tools/test-bot` (or hoist the dep at the root) and re-run; if the omission is deliberate because voice deps are heavy/native, mark the import `// @ts-expect-error optional dep` with a comment naming the reason so the typecheck is clean either way.
- **[low — deferred by Lead decision, ROK-1461 scope]** `api/src/events/event-plans-discord.helpers.ts` — the event-plans poll is the SECOND one-shot poll surface and was deliberately left OFF the shared chrome by this story (Lead decision, spec §Scope decisions). It still hard-codes a `'Raid Ledger'` author and footer, reads `process.env` directly instead of taking a resolved `clientUrl`, uses Discord's native `poll:` primitive rather than an embed the chrome can own, and carries zero unit coverage. It is now the only Discord surface in the repo that authors as a bare constant. Suggested: fold it into the slice-D follow-up (ROK-1462, DM chrome) or its own story — either way it needs a `createChannelEmbed` pass plus a first unit spec before the "no `setAuthor`/`setFooter` outside the chrome" guard can be made an ESLint rule.

### 2026-09-02 — rok-1464-lfg-group-page (surfaced during the LFG group-page smoke run, fleet task f7b51ca97c8c)

- **[med — pre-existing, NOT this branch]** `POST /admin/test/seed-fixture-user` is not safe under concurrent identical seeds. `api/src/admin/demo-test-fixture-user.controller.ts:74` does a SELECT on the hard-coded `SMOKE_INVITEE_DISCORD_ID` (`:36`) and, on a miss, a plain INSERT at `:95` — no `ON CONFLICT`, no advisory lock, no transaction around the pair. Playwright's `desktop` and `mobile` projects are separate worker processes started together against one API, so both take the miss branch and the loser's INSERT trips `users.discord_id`'s unique constraint: verbatim `seed-fixture-user failed: 500 {"statusCode":500}` on the mobile project's first attempt, green on Playwright's retry. Any future smoke spec that needs the invitee in more than one project will hit the same first-attempt 500. The endpoint also accepts **no body**, so a spec cannot ask for a per-project identity (`-desktop` / `-mobile`) — the only spec-side mitigation available is a retry, which is what `scripts/smoke/lfg-group-page.smoke.spec.ts::seedInvitee` now does. `Suggested:` make the seed idempotent server-side — `ON CONFLICT (discord_id) DO UPDATE … RETURNING` (one statement, no read-then-write race) — and optionally accept an optional `suffix` in the body so concurrent projects can seed distinct fixture users instead of sharing one.
