# Tech Debt Backlog

Working document for reviewer findings that don't warrant immediate fixes. Appended by `/build`, `/dispatch`, and `/fix-batch` reviewer reports. Operator triages: file Linear stories for items worth doing, delete the rest.

**Why this exists:** findings used to be auto-filed as Linear `tech-debt:` stories, which created a self-perpetuating queue (reviewer flags items → Lead files story → next batch picks up the story → reviewer flags more items → loop). Findings now land here instead. Nothing in this file should be acted on by an agent without an explicit operator instruction.

## Format for new entries

Append to the bottom under a new dated section. The Lead does this as part of the batch's commits — no separate PR.

```
### 2026-05-03 — fix/batch-2026-05-02 (PR #XXX)

- **[low]** `path/to/file.ts:42` — short description.
  Suggested: one-line fix idea (optional).
- **[med]** `path/other.tsx` — description.
```

Severities: `crit` should never land here (those are auto-fixed during review or sent back to dev). Use `high` / `med` / `low` / `nit`.

## Operator workflow

1. Read new entries when reviewing the PR diff.
2. For each entry: file a Linear story manually, OR delete the entry, OR leave it for later.
3. Pruning is part of triage — old or duplicate items should be removed, not left to bloat.

## Format for skills that parse this file

A future triage skill (or operator-invoked agent) can rely on:

- Each batch is a level-3 heading: `### YYYY-MM-DD — <branch> (PR #<num>)`. Date is ISO. PR ref optional if the PR doesn't exist yet.
- Each finding is a single bullet starting with `- **[<sev>]**` where `<sev>` ∈ {`high`, `med`, `low`, `nit`}.
- File path follows in single backticks; `path:line` is preferred over `path` alone.
- Description is the rest of the bullet up to optional `Suggested:` line indented two spaces.
- The append marker (`<!-- agents append below this line -->`) divides header from entries — skills should read from there forward.

A skill's job is typically: parse → group duplicates by file path → propose Linear stories grouped by area label → present for operator approval. Never auto-file.

---

<!-- agents append below this line -->

### 2026-05-23 — fix/batch-2026-05-22 (PR pending — Phase B: ROK-1317/1333/1318)

- ~~**[med]** `web/src/hooks/use-ai-suggestions-available.ts:29` — non-admin AI surfaces silently depend on the contract that `useAiFeatures` returns `data === undefined` → `data?.aiSuggestionsEnabled !== false` evaluates as true. No test pins this. A defensible tightening to `=== true` would silently disable ✨ AI Pick badges, CommonGround banner, Nominate modal, and lineup-detail AI prefetch for every non-admin user — admin view stays fine because data is populated.~~
  ~~Suggested: add `web/src/hooks/use-ai-suggestions-available.test.ts` with two cases: (a) plugin active + `useAiFeatures = undefined` (non-admin) → returns true; (b) plugin active + `useAiFeatures = { aiSuggestionsEnabled: false }` → returns false. Pins the contract so a future tightening triggers a test failure.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 22001280).**
- **[low]** `Dockerfile.allinone:430, 545` — Both GRANT install points run under `set -e` / `ON_ERROR_STOP=1` with no DO/EXCEPTION wrapper, unlike migration 0144 which guards with `WHEN undefined_function / undefined_object / insufficient_privilege`. A future contrib bump that renames or drops the `pg_stat_statements_reset(oid, oid, bigint)` signature crashes the container at startup (docker-entrypoint dies → restart loop) instead of degrading gracefully.
  Suggested: wrap both Dockerfile GRANTs in a DO block identical to migration 0144's defensive shape.
- **[low]** `api/src/drizzle/migrations/0144_grant_pg_stat_statements_reset_execute.sql:28` — Migration is a permanent no-op in prod (raid_ledger can't GRANT on a function it doesn't own; `insufficient_privilege` is swallowed). Real prod fix lives in Dockerfile.allinone. Maintenance trap: a future maintainer might delete the Dockerfile install points thinking the migration covers them.
  Suggested: add header comment `-- INTENTIONAL NO-OP IN PROD: real GRANT lives in Dockerfile.allinone (init-db.sh + entrypoint ensure-block, run as postgres superuser). This DO-block exists for Render preview / dev environments where the migration runner IS superuser.`
- ~~**[low]** `scripts/env-lock.sh:434` — Release with a WRONG (non-matching) `--agent-id` silently falls through to branch+worktree match — wrong-id is treated identically to missing-id. Comment claims agent_id is "primary" but implementation makes it advisory. The bash integration test scenario 2 implicitly documents the fallback as intentional but the wrong-id case is not exercised separately.~~
  ~~Suggested: either (a) document agent_id as advisory in the script header (cheap), or (b) when `--agent-id` is supplied AND non-empty AND doesn't match, refuse to fall through (require explicit `--match-by branch_worktree` opt-in).~~ **RESOLVED as option (a) 2026-07-10 (fix/e2e-batch-2026-07-10).** Option (b) was implemented (0bb07f96) then REVERTED after review: the MCP wrapper persists its stamp to a single machine-global `~/.raid-ledger/mcp-agent-id` that a parallel agent's enqueue-only acquire overwrites (`tools/mcp-env/src/tools/env-lock.ts` executeAcquire persists BEFORE knowing whether it acquired), so a mismatched agent_id is routinely the rightful holder with a clobbered stamp — refusing zombies the lease until TTL. The advisory-fallback semantics are now documented in the `cmd_release` header. If (b) is ever re-attempted: first make the stamp per-worktree (or persist only on `acquired: true`), and make the refusal return `released:false`.
- **[low]** `scripts/env-lock.sh:452` — `cmd_heartbeat` is dead code: comment claims `deploy_dev.sh` sends periodic heartbeats, but zero callers exist anywhere in the repo (deploy_dev.sh, MCP wrapper, crons). Future agent might trust the comment and skip adding heartbeat calls when needed.
  Suggested: either (a) wire a `deploy_dev.sh` background heartbeat (`while sleep 300; do env-lock.sh heartbeat ...; done &`), or (b) delete `cmd_heartbeat` + its dispatch case (cheaper).
- ~~**[low]** `web/src/plugins/ai/slots/ai-plugin-content.test.tsx` (3 ROK-1000 TestChatSection tests, ~line 150+) — emit MSW warnings (`intercepted a request without a matching request handler: GET /admin/ai/features`) after ROK-1317's mock additions. Tests still pass (TanStack Query enters error state, components handle gracefully) but the warnings are noise. Cause: the ROK-1317 mock additions made the test user an admin, which now satisfies `useAiFeatures`'s `enabled` predicate (since `getAuthToken` is also present), triggering the fetch — but there's no MSW handler for `/admin/ai/features` in those tests' setup.~~
  ~~Suggested: in the 3 ROK-1000 tests' `beforeEach`, add a `server.use(http.get('/admin/ai/features', ...))` stub returning empty feature flags. Silences the warning without changing test behavior.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, fa66b1ed).**
- **[low]** `tools/mcp-env/**` workspace tests are NOT run by `scripts/validate-ci.sh` — the script invokes `npm run test:cov -w api` + `npx vitest run --coverage` from `web/` only, skipping `tools/mcp-env`, `tools/test-bot`, `tools/mcp-rl-fleet`, `tools/mcp-discord`. Surfaced when /code-review ENV-4 fix broke env-lock.test.ts (6 failures) but CI reported PASS. Caught only by the per-story reviewer running `npm run test -w tools/mcp-env` directly.
  Suggested: add `npm run test -w tools/mcp-env -w tools/test-bot` (or equivalent for tools/* workspaces with `test` scripts) to validate-ci.sh's unit test step. Each workspace's vitest run is sub-second so the additional cost is negligible.
- **[low]** Per-story reviewer is now MANDATORY in `/fix-batch` (Step 3i, post-`87f09523`) but `/build` skill's Step 4-review.md still references the old "one reviewer per batch" pattern. Surface for harmonization.
  Suggested: audit `/build`, `/bulk`, `/dispatch` SKILL.md + step files for the same "one reviewer per story" rule, OR document why those skills are intentionally batch-scoped.
- ~~**[high]** `scripts/sync-local-to-env.sh:355-360` — fleet's `rl_env_deploy` sync_settings step SSHes to the VM AS `rl-agent` (line 67 default; MCP server forces it) and runs `sudo grep` against `/srv/rl-infra/.env` to read `RL_ENV_JWT_SECRET` / `RL_PUBLIC_DOMAIN` / `RL_ADMIN_PASSWORD`. If rl-agent's SSH is hardened (per ROK-1338 PR-3 even though the actual sshd_config lockdown was deferred) OR rl-agent lacks sudo permission for `/srv/rl-infra/.env`, the grep returns empty with `BatchMode=yes` swallowing the failure → script reports `RL_ENV_JWT_SECRET missing in /srv/rl-infra/.env` even when the value IS in the file. End result: `rl_env_deploy`'s sync_settings step fails on every operator laptop where rl-agent's SSH+sudo path isn't fully provisioned. Surfaced during /fix-batch 2026-05-22 Track A (Chrome MCP for ROK-1317) — operator confirmed `RL_ENV_JWT_SECRET` matched local but deploy still reported "missing". Fix-batch fell back to local mode to complete Chrome MCP.~~
  ~~Suggested: route `/srv/rl-infra/.env` reads through an mcp-rl-fleet tool (e.g. `rl_infra_config_read` constrained to a known allowlist of safe keys) instead of in-band SSH-as-rl-agent. Goes on the **ROK-1338 no-SSH umbrella** checklist (per memory `project_rok_1338_no_ssh_umbrella` — do NOT file a separate story).~~ **FIXED 2026-06-02 (`fix/fleet-sync-settings-rl-agent-env-read`).** Root cause confirmed: the `sudo` was both unnecessary AND the failure point — `/srv/rl-infra/.env` is already directly readable by rl-agent (every orchestrator binary sources it via `_state.sh`: `set -a; . /srv/rl-infra/.env`, documented "read-only to rl-agent"). Replaced the three `sudo grep` reads with a single `read_infra_env()` that SOURCES `.env` over the existing rl-agent SSH path (no sudo) — the exact value env-spin injected as `JWT_SECRET`, so re-encrypt can't drift from what the container decrypts with. No re-opening of rl-agent sudo/SSH surface (least-privilege; it REMOVES a sudo dependency), so it stays off the ROK-1338 no-SSH umbrella as a clean win.
- ~~**[high]** `scripts/sync-local-to-env.sh:389-393` — when `REMOTE_ENV_JWT_SECRET` comes back empty, the error message ("RL_ENV_JWT_SECRET missing in /srv/rl-infra/.env") cannot distinguish "the var is actually missing" from "SSH-as-rl-agent failed and the grep silently returned empty". This sends operators on a wild goose chase setting an env var that's already set.~~
  ~~Suggested: capture the SSH command's exit code (today it's swallowed by `|| true`) and emit a differentiated hint: when SSH exit ≠ 0, print "SSH-as-rl-agent failed reaching /srv/rl-infra/.env; check VM-side rl-agent SSH+sudo provisioning"; when SSH exit = 0 but grep returned empty, print the current "RL_ENV_JWT_SECRET missing" message.~~ **FIXED 2026-06-02 (same branch).** `read_infra_env()` now sets `INFRA_ENV_READ_RC`: `0` = read SUCCEEDED (empty value ⇒ "RL_ENV_JWT_SECRET is genuinely ABSENT", exit 4); `7` = `.env` unreadable by rl-agent; `8` = truncated payload (sentinel mismatch); else = ssh transport error. Any non-zero RC ⇒ "failed to READ /srv/rl-infra/.env … remote-read failure, NOT a missing variable — do not just re-set the var", exit 6, with remote stderr surfaced. Behavioral coverage (read-failed vs genuinely-absent vs truncated): `rl-infra/orchestrator/test/sync-local-to-env-infra-read.test.sh` (extracts the fn verbatim, runs it against SSH stubs under bash 3.2).
### 2026-05-03 — rok-1067-public-shareable-lineup (ROK-1067)

- **[low]** `api/src/drizzle/migrations/0135_lineup_public_share.sql:6-14` — Migration is unsafe under rolling/blue-green deploys: ADD COLUMN nullable → backfill → SET NOT NULL has a window where old app instances can insert NULL slugs and trip the `SET NOT NULL` step. Not applicable to RL's current single-instance Synology Docker topology (Watchtower stops old container before starting new), but worth documenting if we ever move to multi-instance / k8s.
  Suggested: phased migration (separate PRs for backfill vs `SET NOT NULL`) or DB-side default that the new app code overwrites.
- **[low]** `nginx/monolith.conf.template:75-84` and `nginx/default.conf:52-61` — The existing ROK-393 `/i/:code` crawler block also lacks `proxy_set_header X-Real-IP` / `X-Forwarded-For`, so the invite-OG endpoint's rate-limit buckets all crawlers under the upstream IP too. Same class of bug as the ROK-1067 finding (fixed for `/p/lineup/:slug` in this PR); the invite block was the precedent and inherited the gap.
  Suggested: mirror the four `proxy_set_header` lines in the `/i/:code` location block.

### 2026-05-09 — rok-1225-stabilize-lineups (PR #751)

- ~~**[low]** `api/src/itad/itad-price-sync.service.ts:25` — keeps `export { extractErrorDetail }` re-export shim because `itad-price-sync.service.adversarial.spec.ts:17` still imports from the old path. CLAUDE.md frowns on re-export shims; two-line fix in a follow-up chore.~~
  ~~Suggested: update the spec import to `from '../common/pg-error.helpers'` and delete the re-export.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, d77f6fa5).**
- **[low]** `api/src/lineups/lineups-bandwagon.helpers.ts:48` — insert uses pre-check + `ConflictException` for the 409 UX path. Could be replaced with `.onConflictDoNothing().returning({ id })` and treat empty-returning as "already a member" — cleaner but a behavior change for the 409 response.
  Suggested: spike a follow-up to confirm the UX impact before touching.
- **[low]** `community_lineup_schedule_slots.match_id` and `community_lineup_schedule_votes.slot_id` FKs declared in 0113 — verify presence on prod via DB probe (this PR fixes only the empirically-missing one on `community_lineup_match_members.match_id`).
  Suggested: one-off operator psql probe; if missing, file a focused follow-up like ROK-1225.
- **[low]** Production DB likely has the same orphan rows on `community_lineup_match_members` — migration's `DELETE FROM ... WHERE NOT EXISTS` will clean them on next deploy (Watchtower 5AM); pre-deploy probe optional but informative.
  Suggested: operator-run psql `SELECT COUNT(*) FROM community_lineup_match_members m WHERE NOT EXISTS (SELECT 1 FROM community_lineup_matches WHERE id = m.match_id);` against prod before tomorrow's 5AM pull.

### 2026-05-09 — rok-1247-smoke-api-polling

- ~~**[low]** `scripts/smoke/api-helpers.ts:303-330` `pollForCondition` — JSDoc could explicitly document the truthy-vs-null caller contract so future callers don't pass a `Promise<number>` and trip on `0` (which the current `if (last)` treats as "not yet ready"). Implicit-but-correct today; one-line addition would prevent confusion.~~
  ~~Suggested: add `@example` showing the `data?.foo?.length ? data : null` pattern.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 12618dce).**
- **[low]** `scripts/smoke/api-helpers.ts` `pollBackups` / `pollLogs` — currently resolve on any non-null object (`data ? data : null`). Acceptable today because tests assert "table OR empty-state" panels. If a future test seeds a row and asserts on its presence, those polls must be tightened to require `total > 0`.
  Suggested: tighten when a row-asserting test is added; not in scope for this story.
- **[nit]** Test-infra observation: ROK-1209's worktree acquired the env-lock 4 minutes into ROK-1247's lease (PID 0 / heartbeat-based liveness — bug-prone). Re-acquiring with `priority: operator` recovered, but suggests the env-lock heartbeat semantics may need a refresh while a Playwright suite is running.
  Suggested: investigate adding a heartbeat refresh inside `npx playwright test` (or the deploy_dev wrapper) to prevent mid-run preemption when the lease holder is busy but not actively pinging the MCP. (Addressed by ROK-1209 commit `chore(env-lock): re-anchor deploy_dev.sh lease to API PID` — re-anchors lease to long-lived API server PID at end of `start_dev()`.)

### 2026-05-11 — rok-1253-grace-and-pause (ROK-1253, PR pending)

- **[med]** `api/src/lineups/lineup-auto-advance-grace.integration.spec.ts:863-920` REWORK-4 — test asserts the failed-grace catch path (clear `pendingAdvanceAt` + cancel job + no deadlock) but does **not** deterministically reproduce the replacement-job race that Codex round 3 P2 closed. REWORK-4 calls `processGraceAdvance()` in isolation; it never injects a competing vote/`maybeAutoAdvance` between the cancel and the clear inside the catch block, so it would also pass with the old buggy `clear → cancel` ordering. Runtime fix at `lineup-phase.processor.ts:175-176` is correct + reviewable from the code; the integration suite just doesn't prove the race window is closed.
  Suggested: add a competing-mutation race test using either (a) a test-only hook in the catch block (`process.env.ROK_1253_RACE_HOOK` await pattern), or (b) `Promise.race` orchestration where REWORK-4 fires a vote on the same lineup mid-failure and asserts a fresh `lineup-grace-<id>` job survives. Defer until someone adds the test infra to inject hooks cleanly — flaky race tests are worse than the gap.
- **[low]** `api/src/lineups/queue/lineup-phase.processor.ts:142-178` — `runGraceTransition`'s catch swallows ALL errors with the same hygiene path (cancel + clear). Right thing for `ConflictException` and `TIEBREAKER_REQUIRED`; questionable for unrelated runtime exceptions (DB outage, etc.) where retrying might be preferable to giving up the claim. Pragmatically the BullMQ retry semantics already handle transient failures upstream (`attempts: 3, backoff: exponential`), so the catch only fires after retries are exhausted — acceptable.
  Suggested: only revisit if real-world flakes show grace-windows being lost to transient DB hiccups.

### 2026-05-11 — fix/batch-2026-05-11 (ROK-1259 review)

- ~~**[low]** `web/src/components/notifications/NotificationItem.test.tsx:60-75` — Test fixtures use `type: 'lineup_vote_reminder'` (etc.) but the backend dispatches lineup reminders with `type: 'community_lineup'` and `payload.subtype: 'lineup_vote_reminder'` (see `api/src/lineups/lineup-reminder-dispatch.helpers.ts:30-36`). The click handler is payload-driven only, so the tests still verify navigation correctly — but the test fixture's `type` field misrepresents the wire shape.~~
  ~~Suggested: switch fixtures to `type: 'community_lineup'` + `payload.subtype: 'lineup_vote_reminder'`, OR add an inline comment noting that `handleClick` ignores `notification.type` and only uses `payload`.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 724b452f).**
- ~~**[low]** `api/src/lineups/lineup-reminder-dispatch.helpers.ts:52` — `lineup_scheduling_reminder` is dispatched with `{ subtype, matchId }` only (no `lineupId`). After ROK-1259's fallback, these clicks still no-op because neither the matchId+lineupId branch nor the lineupId-only branch matches. AC3 of ROK-1259 assumed the backend sends both; the test fixtures reflect the spec's expectation rather than the wire shape.~~
  ~~Suggested: add `lineupId` to the scheduling-reminder dispatch payload in a follow-up so users can actually click through. Pre-existing dispatch gap, not a ROK-1259 regression.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 18a13526).**

### 2026-05-11 — rok-1260-deactivate-discord-leavers (ROK-1260, PR pending)

- **[low]** `api/src/notifications/discord-notification.service.ts` (399 lines, over the 300-line STRICT limit but `warn`-level). ROK-1260 added `deactivateUser()` (~30 lines of ModuleRef-based DI plumbing — required to break a 3-way Notification↔Users↔Events cycle that crashed boot via `RosterNotificationBufferService`) plus `isUserDeactivated()` (~15 lines for Codex P2). The actual deactivation orchestration is already extracted to `discord-notification-deactivate.helpers.ts` (106 lines); only the DI shim remained inline because it needs access to the service's injected `moduleRef`. Architect POST_REVIEW: suggested extracting a `DiscordDeactivationService` that owns the cross-module ModuleRef calls, leaving the notification service lean (~330 lines).
  Suggested: new `DiscordDeactivationService` provider in `notifications.module.ts`; move `deactivateUser` and `isUserDeactivated` over; `DiscordNotificationService.deactivateUser` becomes a 1-line passthrough OR is dropped entirely and the processor calls `DiscordDeactivationService` directly. Architect estimated ~70-line drop on the main service.
- **[low]** `api/src/discord-bot/listeners/pug-invite.listener.ts:90-95` — same gateway-reconnect listener leak Codex P3 flagged on `guild-member-add.listener.ts` (fixed for ROK-1260 in commit `74912008`). `handleBotDisconnected()` clears the registration flags but never calls `client.off(Events.GuildMemberAdd, this.boundGuildMemberAddHandler)` or `client.off(Events.InteractionCreate, this.boundInteractionHandler)`. On every reconnect, pug-invite stacks another handler. Not bundled with ROK-1260 because pug-invite has 2 listeners + an interaction surface — bigger blast radius than the new file. Operator's call whether to fix in a follow-up chore or leave for the next batch.
  Suggested: mirror the `client.off()` call pattern from `guild-member-add.listener.ts:60-67`. Two lines per handler. Verify with a Discord-bot reconnect smoke (if there is one — there may not be coverage for the reconnect cycle today).

### 2026-05-12 — rok-1068-lineup-prelaunch-validation (ROK-1068)

Findings from pre-launch validation of the Community Lineup feature (33 runbook ACs + 35 original Linear sub-ACs cross-checked). Runbook landed at `docs/runbooks/lineup-prelaunch-validation.md`. Chrome MCP + Playwright + companion bot all driven against deployed dev env.

**Coverage gaps (uncovered by smoke + integration + Chrome MCP):**

- **[low]** `api/src/lineups/lineup-notification-channel.helpers.ts` lifecycle dispatcher when `channel_bindings.lineup` is null — no smoke or integration coverage exercises the no-bound-channel path. Every test fixture uses a channel-bound community. Regression would be silent in CI; only surfaces when an operator self-hosts without binding a channel before starting their first lineup. AC 33 in the ROK-1068 runbook is explicitly logged as a gap because DEMO_MODE does not expose channel-unbind.
  Suggested: either (a) integration test that asserts `LineupNotificationService.dispatch*` returns gracefully and emits the `channel-unbound` log line when `channelBindings.lineup === null`, or (b) companion-bot smoke that creates a lineup in a guild with no bound channel and `assertConditionNeverMet` on any channel embed for the full lineup lifecycle.

**Phase F (2026-05-12) covered the following three runbook ACs that had originally landed here; the entries are removed because the ACs are now covered:**

- AC 34 (Steam-unlinked-nominator warning) — Chrome MCP drive of `SteamNudgeBanner` against the live env (existing vitest `SteamNudgeBanner.test.tsx` covers copy + visibility logic).
- AC 35 (vote-at-deadline race) — `api/src/lineups/lineup-deadline-vote-race.integration.spec.ts`.
- AC 36 (abort-with/without-reason variance) — `abortReasonVarianceWalkthrough` test in `tools/test-bot/src/smoke/tests/lineup-abort.test.ts`.

### 2026-05-12 — fix/batch-2026-05-12b (ROK-1237 + ROK-1271 + ROK-1267 review)

- ~~**[low]** `web/src/lib/api/fetch-api.ts:50` — inline structural type for `schema` parameter (`{ safeParse: ... }`) duplicates Zod's interface and types `result.error.issues` as `unknown[]`. Consider importing `ZodType<T>` from `zod` so the generic narrows automatically and Sentry's `issues` extra carries proper Zod issue typing for future debugging.~~
  ~~Suggested: change to `schema?: ZodType<T>` and let TypeScript infer. Two-line change. Defer until the next API-layer touch.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 3e7a4518).**
- ~~**[low]** `api/src/admin/games-dedup-audit.helpers.ts:106-189` — `buildDirectCountQueries` is 84 lines (function-length `warn` rule is 30). It's a flat list of 16 count queries; a small `countDirect(table, col)` helper would cut it to ~20 lines and remove the brittle "order must match destructure" coupling at `computeBlastRadiusForId:220-241`. Mediated by the new unit-spec assertion on all 17 fields (committed in `fa16e2a1`) — destructure swap is now test-detectable.~~
  ~~Suggested: refactor in a follow-up when ROK-1270 Phase 1 extends `BlastRadiusRow` (the merge migration will likely add more counts).~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, bf650fc2).**
- **[low]** `api/src/admin/games-dedup-audit.service.ts:99-101` — outer `Promise.all(dupIds.map(...))` × inner `Promise.all([...17 queries])` = up to 17N concurrent DB queries. Fine for Phase 0 (operator-only, prod has <100K games), but if a large guild ends up with N > 20 dup ids the endpoint could spike Postgres `max_connections`.
  Suggested: wrap the outer `Promise.all` with `pLimit(8)` (or similar) if telemetry shows N > 20 on prod. Currently no metrics; defer.
- **[low]** `packages/contract/src/roster.schema.ts:66` — `signupStatus` is now `.optional()` after referencing canonical `SignupStatusSchema`. Widening the contract to accept `undefined` from the server. Acceptable for forward-compat, but call sites (`RosterSlot.tsx:73-78`, `EventDetailRoster`) compare `===` against literals, so undefined falls through to default treatment. Confirm this is the intended fallback for legacy/missing payloads, or tighten back to non-optional.
  Suggested: review the read-side query helpers — if signupStatus is always present in practice, drop `.optional()`. Spike before next contract refactor.

### 2026-05-12 — rok-1270-games-dedup-audit (ROK-1270 Phase 1)

- ~~**[low]** `api/src/admin/games-dedup-unique-conflicts.helpers.ts:79-118` — `countCompositeConflicts` and `countSingleColumnConflict` use `sql.raw` with template-literal interpolation of `tableName`, `otherCols`, `input.canonicalId`, and `input.dupIds.join(',')`. Safe today: all interpolations are compile-time string literals or typed `number` / `number[]` values from `games.id` (typed FK column). The pattern is fragile — if `UniqueConflictInput` ever accepted external/user input (helper reused outside the admin surface, or a future bulk-import flow), the SQL-injection guarantee silently regresses.~~
  ~~Suggested: refactor to drizzle `sql` template with `${}` placeholders for the dynamic-arity case, OR add a runtime assertion at the helper entry: `Number.isInteger(input.canonicalId) && input.dupIds.every(Number.isInteger)`.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 7a68f91c).** Took the runtime-assertion option (`assertIntegerIds` at helper entry + unit coverage), not the drizzle-template refactor.
- **[nit]** `api/src/admin/games-dedup-extra-counts.helpers.ts:21` — doc comment references `extendedBlastRadiusKeys` which doesn't exist as an export in this file. The lockstep contract is actually documented via inline comments on each Promise in the array. Minor doc drift from an earlier draft.
  Suggested: edit the comment to drop the dangling reference.
- ~~**[nit]** `api/src/drizzle/schema/games-dedup-audit.ts:35-37` — `uniqueConflicts` is typed `jsonb.$type<Record<string, number>>()`. Importing `UniqueConflictCounts` from `games-dedup-unique-conflicts.helpers.ts` would tighten the schema-level type to match the helper's output shape. No functional bug; consumers cast at read time today.~~
  ~~Suggested: tighten on the next touch to either file.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, f8fdf61c).**

### 2026-05-12 — rok-1158-csp-security-headers (ROK-1158)

- **[low]** `nginx/monolith.conf.template:44-54` and `nginx/default.conf:35-45` — `proxy_hide_header` strips 7 helmet headers (CSP/HSTS/XCTO/XFO/Referrer/Permissions/XXP) but six additional helmet 8.1 defaults pass through to the public surface: `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Resource-Policy: cross-origin` (override, intentional), `Origin-Agent-Cluster: ?1`, `X-DNS-Prefetch-Control: off`, `X-Download-Options: noopen`, `X-Permitted-Cross-Domain-Policies: none`. All are security-positive (no conflict with nginx's set), but the in-code comment "nginx owns the authoritative public-surface header set" is overstated.
  Suggested: either lock helmet posture explicitly in `api/src/main.helpers.ts::buildHelmetOptions` (`crossOriginOpenerPolicy: false`, `originAgentCluster: false`, `xDnsPrefetchControl: false`, `xDownloadOptions: false`, `xPermittedCrossDomainPolicies: false`), or extend the `proxy_hide_header` list in both nginx files to cover them. Either choice gives the operator one source of truth for the public-surface header posture.
- **[low]** `nginx/monolith.conf.template:38-39` and `nginx/default.conf:29-30` — no comment near the server-scope `include /etc/nginx/snippets/security-headers.conf` warning future devs that any NEW `location` block with its own `add_header` must re-include the snippet (otherwise all 6 security headers are silently dropped on that path). The static-asset block carries the warning; server scope does not.
  Suggested: prepend the include with a one-line comment "NOTE: new locations with custom add_header must re-include this snippet — nginx kills inheritance on the first child add_header. See architect-ROK-1158.md §2."
- **[info]** `nginx/snippets/security-headers.conf:7` — HSTS `max-age=31536000; includeSubDomains` pins browsers to HTTPS for 1 year at the prod host (raid.gamernight.net) and all subdomains. Operator should confirm DNS lists no HTTP-only `*.raid.gamernight.net` subdomain (legacy admin tools, image hosts, etc.) before deploy. Operator informed in `planning-artifacts/review-ROK-1158.md` §8; non-blocking.

### 2026-05-13 — rok-1279-prod-clone (ROK-1279) — Codex review findings (non-blocking)

- ~~**[med]** `api/src/backup/backup.controller.ts:54` — `fs.createReadStream(absPath).pipe(res)` has no `.on('error', ...)` handler. TOCTOU between `existsSync` and stream open: if the file is deleted between resolution and stream open (race with rotation cron or operator DELETE), the read stream emits `error` after headers are already sent → client sees 200 + truncated body, Node may surface an unhandled error. Low practical risk (rotation cron only deletes >30d files; admin DELETE flow is operator-only) but cheap to harden.~~
  ~~Suggested: attach `stream.on('error', err => { this.logger.warn(...); res.destroy(err); })` before the pipe. ~3 lines.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, b6d13751).**
- ~~**[low]** `scripts/clone-prod-to-local.sh:165` — `prod_get_safe` glob `[[ "$pth" != "$PROD_GET_ALLOWED_PREFIX"/*/download ]]` is looser than strict-whitelist intent. Bash `[[ ]]` glob `*` matches `/`, so a path like `/admin/backups/a/b/c/download` passes the bash check. Server-side AdminGuard + path-traversal regex still block any real damage, but cosmetic strictness would close the gap.~~
  ~~Suggested: replace bash glob with regex check: `[[ ! "$pth" =~ ^/admin/backups/(daily|migration)/[A-Za-z0-9_.-]+/download$ ]]` and the exact list-endpoint match. ~3 lines.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 0b461555).**
- ~~**[low]** Two backup-path helpers exist for similar intent: `BackupService.getBackupFilePath` (new in ROK-1279; 404-only on traversal) and `BackupService.resolveBackupPath` (existing; uses 400 for traversal — feeds restore/delete). Unify on the 404-only behavior to reduce info leak surface across all path-validated routes. Non-urgent; both are admin-only.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, efa1d1ce).**

### 2026-05-13 — scheduling-banner queryKey mismatch (surfaced during ROK-1235 review)

- **[low]** `web/src/hooks/use-standalone-poll.ts:29` invalidates `queryClient.invalidateQueries({ queryKey: ['scheduling-banner'] })` but the actual key declared in `web/src/lib/hooks/use-scheduling.ts:28` is `['scheduling', 'banner']` (two-element tuple). TanStack Query matches by prefix array equality, so the invalidation never matches anything → after creating a standalone poll, the events-page scheduling banner is not invalidated and the user must reload or wait for `staleTime` to refresh. Out of scope for ROK-1235 (which fixed the 400-route-shadow); pre-existing.
  Suggested: change line 29 to `queryKey: ['scheduling', 'banner']`. One-line fix. Confirm by triggering "create standalone poll" and watching network for the immediate `/scheduling/banner` refetch.

### 2026-05-13 — full-suite integration test flakes (surfaced during fix/batch-2026-05-13 CI gate)

`./scripts/validate-ci.sh --full` against the batch base produced 2 failures out of 1009 integration tests, neither in any file changed by the batch (24 file diff, neither file present in diff). Both pass 3/3 cleanly in isolation via `./scripts/spec-loop.sh`. Cross-suite carrier flake, NOT caused by this batch.

Both findings from this section are now tracked in Linear: BullMQ socket-leak carrier in [[ROK-1268]] (cross-batch evidence appended); auth-state leak in [[ROK-1321]] (new story, distinct cluster from socket-leak).

### 2026-05-13 — fix/batch-2026-05-13 reviewer findings (medium/low/nitpick — non-blocking)

Findings from senior-code-review on the batch diff. Critical + high were fixed inline (commit c90f98af). Operator deferred the [med] participantCount=1 UX call to ship-as-is.

- ~~**[low]** ROK-1235 / `api/src/lineups/scheduling/scheduling-banner.integration.spec.ts:42` — Empty-body assertion `expect(res.text === '' || res.body === null || res.body === '').toBe(true)` is fragile. Supertest returns `res.body = {}` for an empty 200, not `null`. Test passes today because of Nest config coincidence; intent is "body is empty/null-ish."~~
  ~~Suggested: rewrite to `const bodyEmpty = res.text === '' || res.text === 'null' || Object.keys(res.body ?? {}).length === 0; expect(bodyEmpty).toBe(true);`.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, d9474c95).**
- ~~**[low]** ROK-1235 / `api/src/lineups/scheduling/scheduling.controller.ts:38` — `SchedulingController` still declares `@Controller('lineups')` with parametric routes (`:lineupId/schedule/...`). If a future author adds a literal-segment route there (e.g. `@Get('archive')`), the same Nest route-shadow bug recurs because `LineupsController` registers first with `@Get(':id') + ParseIntPipe`.~~
  ~~Suggested: add a one-line guard comment at the top of `SchedulingController` ("literal-segment routes are forbidden on this controller — only `:lineupId/schedule/...` patterns"), OR migrate the remaining 8 scheduling routes to a `/scheduling/lineups/:lineupId/...` prefix in a future cleanup story.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 62872f81).** Guard-comment option only; routes were not migrated.
- **[low]** ROK-1207 / `web/src/lib/lineup-aborted.ts:26-33` — `useMemo` keyed on `data` (full timeline response). Any timeline update (vote, nomination, phase change) re-scans every activity entry. O(n) per refetch — acceptable today (logs are small).
  Suggested: switch to `.find(e => e.action === 'lineup_aborted')` working backwards from end of array if logs ever grow >50 entries. Verify newest-tends-to-be-last ordering first.
- ~~**[low]** ROK-1255 / `web/src/components/lineups/CommonGroundFilters.tsx:110-117` — `didInitPlayersRef` is set to `true` even when `filters.maxPlayers` was already set (the early-return after marking the ref consumed). Behavior is correct ("capture intent once per known participantCount") but reads like a bug to future maintainers — the name implies "we've successfully auto-set" rather than "we've seen a non-zero count."~~
  ~~Suggested: rename ref to `participantCountSeenRef` (or `intentCapturedRef`). Mechanical clarification, no behavior change.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 73ab9aef).**
- ~~**[nit]** ROK-1235 / `api/src/lineups/scheduling/scheduling-banner.controller.ts:1-8` — Block comment explains the historical bug verbosely. Trim to one line ("Banner endpoint moved out of /lineups/* to avoid ParseIntPipe shadow — see ROK-1235") once the fix has aged. Doc-only.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, dce91dda).**
- ~~**[nit]** ROK-1207 / `web/src/components/lineups/LineupAbortedBanner.tsx:23-26` — `role="status"` with `aria-live="polite"` is correct for live-updating regions, but the banner is painted once on render. Screen readers will announce on first render (intended), but `aria-live` implies subsequent updates that don't happen.~~
  ~~Suggested: drop `aria-live="polite"` (let `role="status"` carry the polite announcement), or switch to `role="alert"` for a stronger announcement. UX/a11y polish.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, a9eca365).** Dropped `aria-live` and kept `role="status"` (first suggested option, not `role="alert"`).

### 2026-05-14 — ROK-1281 (prod incident postmortem)

- ~~**[high]** Sentry never captured the prod migration error. Three gaps: (1) `api/src/scripts/run-migrations.ts` doesn't import `instrument.ts`; (2) the runner uses sync `process.exit(1)` without `Sentry.flush(2000)`; (3) the ACTUAL prod path was an inline `node -e` block in `api/scripts/docker-entrypoint.sh:26-73`, which had no Sentry import at all.~~
  ~~Fixed in ROK-1281 by routing all boot-time migrations through the new instrumented runner. The legacy `run-migrations.ts` (still used by `backup.service.ts` for restore flows) should follow up — it still has the gap but isn't on the deploy critical path.~~ **Folded into ROK-1343, shipped PR #841 — legacy `run-migrations.ts` now Sentry-instrumented (`reportMigrationFailure` with `restore-migration` tag + `Sentry.flush(2000)` before exit + first-import guard + `require.main === module`).**
- ~~**[med]** Prod recovery required manually marking 0140 as applied in `drizzle.__drizzle_migrations` before the new image could boot. This pattern (insert hash row to skip a failed migration) is not documented anywhere — operator had to derive it. Add a `scripts/recover-stuck-migration.sh` runbook.~~
  ~~Suggested: a script that takes a migration tag, looks up its hash from `meta/_journal.json`, inserts the journal row, and prints next-steps. Idempotent.~~ **Folded into ROK-1343, shipped PR #841 — `scripts/recover-stuck-migration.sh` ships with `--dry-run`/`--help`, idempotent INSERT (uses journal `when` for `created_at`), shell-injection hardened (Codex security review).**

(The `run-migrations.ts` divergence bullet that originally lived here is now tracked in [[ROK-1322]].)

### 2026-05-14 — fix/rok-1283 (ROK-1283 follow-up)

- ~~**[low]** `api/scripts/seed-games.ts` — sibling of `seed-igdb-games.ts` audited as part of ROK-1283. Same shape (`ON CONFLICT (slug) DO NOTHING`), same NULL-distinct vulnerability in principle. Practically dormant in prod: its slugs match `seed-igdb-games.ts` AND post-ROK-1278 cleanup left no name dups for this seed's names to collide with. Was NOT fixed in ROK-1283 because the file already exceeds `max-lines` (302/300) on `origin/main` and applying the same fix would push it further over; CI lint would fail. Apply the name-dedup guard the next time this file is touched, alongside breaking up the `GAMES_SEED` constant (move to a sibling `seed-games.data.ts`) to bring the file back under 300 effective lines.~~
  Suggested: extract `GAMES_SEED` to `api/scripts/seed-games.data.ts` (no logic, just data) and apply the same `findGameByNormalizedName` pre-check pattern used in `seed-igdb-games.ts::upsertSeedGames`. **ATTEMPTED 2026-07-10 (fix/e2e-batch-2026-07-10, b19f1225) and REVERTED after review found the naive port is net-harmful.** Three hazards the next attempt must solve: (1) `findGameByNormalizedName`'s ILIKE candidate select has no ORDER BY, so with both the canonical seeded row and a NULL-igdb_id name-dupe present it can return the dupe → `updateGameRow` sets the seed igdbId on it → `games.igdb_id` UNIQUE violation → seeder exits 1 and docker-entrypoint swallows it ("Game seeding skipped") — remaining seed entries silently never land on prod boot; prefer the row already holding the seed igdb_id (or slug) before any name-match. (2) The name-match update branch clobbers operator-customized fields (shortName, colorHex, hasRoles/hasSpecs, maxCharactersPerUser) on every boot for rows the slug-targeted upsert never touched — merge must be insert-only or field-preserving. (3) While touching the file, add the STRICT boot-script Sentry instrumentation (import instrument first, captureException + flush(2000) before exit 1) — it's missing today.

### 2026-05-14 — fix/batch-2026-05-14 (PR pending — ROK-1282 + ROK-1283)

- ~~**[low]** `api/src/users/guild-reconciliation.service.ts:107-109` — dead-code `rows.filter((r): r is { id: number; discordId: string } => r.discordId !== null)`. SQL already guarantees `isNotNull(discordId)` so the filter never drops a row.~~
  ~~Suggested: drop the filter and tighten the return type via a non-null assertion or Drizzle column-level non-null inference.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, 078d5e46).**
- **[low]** `api/src/users/guild-reconciliation.service.ts:113-123` — `deactivateGap` runs deactivations sequentially. Fine at current scale (~5–10 leavers per run on a 500-user guild), but a post-outage run with hundreds of leavers serialises per-row cascades (cancel signups + admin notification).
  Suggested: keep sequential for now (audit-trail simplicity); revisit if a single run ever exceeds ~50 deactivations.
- **[low]** `api/src/users/guild-reconciliation.service.ts:93-106` — `loadActiveDbUsers` has no index on `deactivated_at` and full-scans `users` daily. Acceptable at current user volume; index becomes relevant past several thousand rows.
  Suggested: revisit if cron duration exceeds ~1s on prod.

### 2026-05-14 — fix/batch-2026-05-14 (post-Codex reviewer pass)

- ~~**[low]** `api/src/common/testing/test-app.ts:122` vs `api/src/common/testing/integration-setup.ts:64` — inconsistent CI gating. `test-app.ts` uses strict `process.env.CI === 'true'` after the post-clone audit fix; `integration-setup.ts` uses loose `!process.env.CI`. Both behave correctly under GitHub Actions (`CI=true`) but a developer setting `CI=1` locally would get DATABASE_URL deleted by integration-setup yet ignored anyway by test-app (Testcontainers fires).~~
  ~~Suggested: align both to `process.env.CI === 'true'`. ~5-line change in `integration-setup.ts`.~~ **FIXED 2026-07-10 (fix/e2e-batch-2026-07-10, f0b6ee5f).**
- **[low]** `api/scripts/seed-igdb-games.ts:181-194` — extra DB roundtrip via `rowExistsWithIgdbId` per orphan-match row. Acceptable at boot-time (~hundreds of rows, single-threaded). Could fold into a single SQL CTE if seed counts grow into the thousands.
  Suggested: no action unless seed perf becomes a bottleneck.
- **[low]** `api/src/users/guild-reconciliation.service.integration.spec.ts:137-146` — the "Discord API errors bubble up" test calls `runReconciliation()` directly, not via `CronJobService.executeWithTracking`. Confirms the throw bubbles but doesn't assert the cron wrapper records a failed run vs. healthy heartbeat (which is the actual P2 motivation). Integration with `executeWithTracking` is likely already covered by existing cron-service tests but not explicitly asserted in this regression spec.
  Suggested: add one spec that mocks `cronJobService.executeWithTracking` and asserts `failedRun` was recorded when `listAllGuildMemberIds` throws.

### 2026-05-16 — security-review 2026-05-14 PR 3 defense-in-depth (Group C deferred from ROK-1292)

ROK-1292 closed 2026-05-16. PR 1 (Group A — 4 docker-compose / allinone JWT-guard items + BANNED_SECRETS symmetry + CI negative-path test) shipped as PR #795 + #796. PR 2 (Group B — drop SVG from branding `ALLOWED_TYPES` + boot-time legacy `logo.svg` eviction + `bestEffortInit` classification) shipped as PR #797. Group C below is the remaining 10 app-code defense-in-depth items + 2 sibling/completeness findings, originally planned as PR 3. Operator decision to leave PR 3 in the backlog rather than carry an open Linear story for it — items will be triaged into a future fix-batch or build when convenient. Source reports preserved at `planning-artifacts/security-review-7bec0fb2-fullrepo.md` + `planning-artifacts/security-review-7bec0fb2-summary.md`.

**Group C — App-code defense-in-depth:**

_(The four `[med]` items — ai-providers validation, CLIENT_URL header poisoning, magic-link token-in-URL, encryption salt — were promoted to **ROK-1366** on 2026-06-18 via /readlogs triage. The `[low]` defense-in-depth items below remain in the backlog.)_

- **[low]** `api/src/steam/steam-auth.controller.ts:233-255` + `api/src/steam/steam-http.util.ts:49-81` — Steam-OpenID nonce replay protection currently relies on Steam (`mode=check_authentication`). We don't cache `openid.response_nonce` ourselves. Replay risk low (only re-links the same Steam ID to the same user); layered-defense story missing.
  Suggested: after successful link, `redis.setex(`steam-openid:${nonce}`, 86400, '1')` and reject if already present.
- **[low]** `api/src/auth/jwt.strategy.ts:28` — `process.env.JWT_SECRET!` non-null assertion at construction. If env var missing in prod, passport-jwt receives `undefined` as `secretOrKey` and may either crash at boot or accept tokens signed with an empty key.
  Suggested: throw at module init if `JWT_SECRET` missing or < 32 chars (fail-closed).
- **[low]** `api/src/auth/auth.controller.ts:48-67` (`exchangeCode`) — `redis.get` then `redis.del` is not atomic. Two simultaneous requests with the same code could both retrieve, both `del`, both succeed. Single-use semantics claimed in code comment but not enforced.
  Suggested: replace with `redis.getdel(key)` (Redis 6.2+) or wrap in `MULTI/EXEC`.
- **[low]** `api/src/csp-report/csp-report.controller.ts:14-32` — Logs full CSP report payload to structured logger. 64kb body limit caps abuse but an attacker producing CSP violations can spam logs with arbitrary content reaching the configured log sink.
  Suggested: cap and redact (`JSON.stringify(report).slice(0, 4096)`), or rely on Sentry tags + sampling and skip the second log line.
- **[low]** `api/src/plugins/wow-common/blizzard-equipment.helpers.ts:25-52` (`fetchSingleIconUrl`) — Sends `Bearer ${token}` Authorization header to a URL pulled from parent Blizzard JSON response (`equipped_items[].media.key.href`) without host validation. If a future Blizzard endpoint returns a non-Blizzard URL (or MITM injects one), bearer leaks to third-party host.
  Suggested: assert `new URL(mediaHref).hostname.endsWith('blizzard.com') || .endsWith('battle.net')` before fetch.
- **[low]** `api/src/version/version-check.service.ts:144` → `web/src/components/admin/UpdateBanner.tsx:48` — `latestReleaseUrl` flows from GitHub's `body.html_url` through `app_settings` to a JSX `<a href={href}>`. Zod's `z.string().url()` allows `javascript:` URLs (uses URL constructor which accepts them), so contract doesn't block scheme-based XSS. Trusted source today; defense-in-depth one-liner.
  Suggested: validate `href.startsWith('https://github.com/')` either at storage step in `storeVersionCheckResults` (reject otherwise, store empty) OR at render time in `BannerContent`.

**Sibling/completeness findings (surfaced by `/validate` + `/code-review` over PR 1):**

- **[low]** `docker-compose.test.yml` (sibling to `docker-compose.yml`) binds `5433:5432` for the test Postgres — same wide-open `0.0.0.0` exposure as the main compose file before PR 1. Out of scope for the PR1 framing but the obvious sibling to harden.
  Suggested: change to `"127.0.0.1:5433:5432"`. Same risk profile, same one-line fix.
- **[nit]** `api/scripts/reencrypt-settings.ts:116` — recovery-instructions help text bakes the literal `raid-ledger-default-secret-change-in-production` into a `console.log` example. Not in `BANNED_SECRETS` scope (it's documentation), but if the literal naming is ever rotated this is a 4th replication site to update (alongside `Dockerfile.allinone`, `docker-entrypoint.sh`, `encryption.util.ts`).
  Suggested: leave for now; flag if anyone proposes renaming the legacy literal.

### 2026-05-16 — rok-1294-journey-hero (surfaced while fixing the ROK-1292 PR 2 branding regression)

The ROK-1292 PR 2 regression was patched in this PR (commit `55475fb1`) by adding a `RAID_LEDGER_BRANDING_DIR` env override and sandboxing the integration spec to `os.tmpdir()`. Two related foot-guns remain in the codebase — not blocking, worth filing.

- **[med]** `api/src/users/avatar.service.ts:44` and `api/src/main.ts:53-58` — same `process.cwd()/uploads/avatars` anti-pattern as the pre-fix branding code. Currently SAFE because no existing avatar spec writes/unlinks against that dir. If a future avatar integration spec copies the pre-fix branding pattern (`path.join(process.cwd(), 'uploads', 'avatars')` + `unlinkSync` in `afterEach`), it will wipe the operator's real local avatar webps in main repo `api/uploads/avatars/`.
  Suggested: harden defensively — either (a) rename the existing `AVATAR_UPLOAD_DIR` override to match the new `RAID_LEDGER_*` convention and document both, OR (b) make the dev default for both branding + avatars resolve to a non-`process.cwd()`-anchored path so `npm run test -w api` cwd assumptions can't collide with the dev-served dir.
- **[low]** Root-cause-vs-symptom: the production `getBrandingDir()` still returns `process.cwd()/uploads/branding` in dev. Any future spec / seed / script that doesn't honor `RAID_LEDGER_BRANDING_DIR` can re-introduce the wipe. A real fix would either anchor the dev path off something non-`process.cwd()`-relative (e.g. a fixed `~/.raid-ledger/dev-uploads/` location) OR refuse destructive writes when `NODE_ENV !== 'production'` and the target path matches the static-asset-served dir.
  Suggested: defer until/unless another team member's spec re-trips the same bug. The env-var override + new regression test (added in this PR) is the minimum-effective fix.

### 2026-05-16 — fix/batch-2026-05-16 (surfaced by code review of ROK-1258 + ROK-1306)

Reviewer (sonnet, devedup-rl:reviewer) verdict PASS WITH NOTES on the batch diff. No critical/high findings. Browser validation also clean (see `planning-artifacts/chrome-mcp-summary-fix-batch-2026-05-16.md`). The 7 low items below are nits/cleanup; none block ship.

- **[low]** `api/src/lineups/lineups-matching.helpers.ts:36,76,106` — Comments claim `decided`/`scheduled`/`archived` match rows are preserved, but `communityLineupMatches.status` enum is `suggested|scheduling|scheduled|archived` (no `decided`). The word `decided` here refers to the lineup status, not the match status. Code is correct (wipe only deletes `suggested`/`scheduling`); the wording is misleading for the next reader.
  Suggested: drop `decided` from the preserved-list in the three comments.
- **[low]** `api/src/lineups/quorum/quorum-voters.helpers.ts:62` — `Date.now() < deadline.getTime()` is TZ-safe because `phaseDeadline`/`votingDeadline` are `timestamp` columns (UTC epoch) and both sides compare as ms-since-epoch. Worth a one-line note so the next reader doesn't worry.
  Suggested: add `// UTC ms vs UTC ms — TZ-safe`.
- **[low]** `packages/contract/src/lineup.schema.ts:241` ↔ `web/src/pages/lineup-detail-page.tsx:274` — `stillWaitingOnVoters` is non-optional in the contract, but the page reads it with `?.length ?? 0`. Either tighten the frontend (`.length > 0`) or mark the contract field `.default([])` for forward-compat with cached older responses.
  Suggested: drop the `?.` in the page since the contract guarantees the array.
- **[low]** `api/src/lineups/lineups-response.helpers.ts:154-176` — `loadStillWaitingOnVoters` runs an extra `GROUP BY userId` aggregate on every `GET /lineups/:id` when status=voting+private. Selective (filtered by `lineupId`, backed by `uq_lineup_vote_user_game`'s leading column) so not a hotspot today, but it's serial after the parallel batch.
  Suggested: fold into the existing `Promise.all` batch in `buildDetailResponse`.

### 2026-05-16 — rok-1272-smoke-shard (surfaced during actionlint sweep)

`actionlint .github/workflows/ci.yml` reports 3 pre-existing shellcheck warnings, all unrelated to the ROK-1272 timeout/retry/cache edits. Reproduces on `origin/main` verbatim. Documenting per the STRICT pre-existing-failures rule; not in-scope to fix here.

- **[nit]** `.github/workflows/ci.yml:274` — SC2034 from shellcheck: `i appears unused. Verify use (or export if used externally)`. The `for i in $(seq 1 60); do ... done` API-readiness wait loop uses `$i` only inside the success branch (`exit 0` before the echo runs), so on the failing path `i` is genuinely unused. Cosmetic; the loop is correct.
  Suggested: rename `i` to `_` (bash convention for unused loop var) or echo `$i` in the final timeout message (`echo "API failed to start within ${i}s"`).
- **[nit]** `.github/workflows/ci.yml:416` — SC2034 same pattern as line 274, this time on the Postgres-readiness wait loop (`for i in $(seq 1 30)`). Same fix.
- **[nit]** `.github/workflows/ci.yml:742` — SC2126 from shellcheck: `Consider using 'grep -c' instead of 'grep|wc -l'`. Stylistic; behavior identical.
  Suggested: replace `grep ... | wc -l` with `grep -c ...` (single fork instead of two).
- **[low]** `api/src/lineups/lineups-response.helpers.ts:161` — `lineup.maxVotesPerPlayer ?? 3` duplicates the `3` default that already lives in `mapLineupCore` (line 104).
  Suggested: extract `DEFAULT_MAX_VOTES_PER_PLAYER` or read the mapped value.
- **[low]** `api/src/lineups/lineups.service.ts:370` — `maybeAutoAdvance` fires on every invitee removal even when status is `decided`/`archived` (cheap internal no-op, but a wasted round-trip).
  Suggested: short-circuit on lineup status before the call, or document that `maybeAutoAdvance` already gates by status.
- **[low]** `api/src/lineups/quorum/quorum-voters.helpers.ts:65-72` — `findDistinctVoters`/`findDistinctNominators` re-queried serially after `loadPrivateExpectedVoters`. Two sequential round-trips where one combined query would do.
  Suggested: run roster + participant fetch in `Promise.all`.

### 2026-05-16 — rok-1296-submit-bar (surfaced during ROK-1296 validate-ci.sh sweep)

_2026-07-17 groom: the bullets below were flagged verbatim in BOTH 2026-05-16 batches (fix/batch-2026-05-16b + rok-1296-submit-bar); duplicates deduped into this section and the emptied sibling section removed._

- ~~**[low]** `web/src/components/calendar/calendar-filter-chip.tsx:11-15` — `formatChipLabel` ambiguous for "0 selected" state.~~ **RESOLVED in this same batch.** Codex pre-push surfaced that this was actually a P2 correctness bug, not a [low] cosmetic — `CalendarView.tsx:92-93` filters out ALL events when `selectedGames` is defined but empty, so the chip's "Filter: All games" was the literal opposite of reality. Fixed by adding an explicit `selectedCount === 0 → "Filter: No games"` branch; test in `calendar-page.test.tsx:247` updated to assert the corrected label.
- **[low]** `web/src/components/calendar/calendar-styles.css:614-631` — new `.calendar-filter-chip` rule lives only in the dark-theme block; no light-theme override under `:is([data-scheme="light"], ...)`. On light themes (`light`, `quest-log`, `sky`, `dawn`, `holy`, `celestial`) the chip renders with dark slate colors on a light surface — readable but visually inconsistent with the sibling `.sidebar-action-btn` which DOES have a light override at line 1390.
  Suggested: add a 4-line light override mirroring `.sidebar-action-btn` (background `rgba(226, 232, 240, 0.6)`, border `rgba(203, 213, 225, 0.5)`, color `#334155`).
- **[low]** `web/src/pages/calendar-page.tsx:108,164-170` — `CalendarSidebar` still declares `toggleGame`, `selectAllGames`, `deselectAllGames`, `likedSlugs` props even though only `allKnownGames`, `selectedGames`, `onShowFilterModal` are consumed. Unused props forwarded via `{...filterProps}` keep TS happy but over-promise the type contract.
  Suggested: narrow `CalendarSidebar`'s prop type to the 3 it consumes.
- **[low]** `web/src/pages/calendar-page.tsx:113-117` — `GameItem` is locally re-declared with `{ slug, name, coverUrl }` even though `GameInfo` is already exported from `../stores/game-filter-store` (used by `calendar-filter-chip.tsx`). Same shape; duplication invites drift.
  Suggested: import `GameInfo` from `../stores/game-filter-store` and replace local `GameItem`.
- **[nit]** `api/src/admin/games-dedup-union-find.helpers.ts:173-178` — sort uses `Number(a) - Number(b)` for `igdb`/`steam`. Currently safe (columns are `integer`), but a future schema change loosening the column type would produce `NaN` ordering.
  Suggested: add a one-line comment noting numeric strings are guaranteed by the integer column type.
- **[nit]** `api/src/admin/games-dedup-audit.service.spec.ts:172-178,431-434` — `.orderBy()` thenable extension is structurally identical in two mock blocks. Will rot if the load query gains another chain step.
  Suggested: extract a `mockLoadGameRowsChain(loadRows)` helper when next touching this file.

### 2026-05-17 — pre-existing Steam-link cosmetic + dev-env collision (surfaced during ROK-1307 Chrome MCP)

Operator hit `ConflictException: This Steam account is already linked to another user` during a Link Steam test on the ROK-1307 deploy. **Not introduced by ROK-1307** — the Link flow is untouched by this story. Two surfaces:

- **[low]** Pre-existing cosmetic bug in `web/src/pages/profile/identity-sections.tsx:153` (or wherever the Link Steam button is wired). `<button onClick={linkSteam}>` binds the React `MouseEvent` SyntheticEvent as the function's first argument — and `linkSteam(returnTo?: string)` then `encodeURIComponent`s the SyntheticEvent object, producing `?returnTo=%5Bobject%20Object%5D` in the URL. The backend allowlist (`SteamAuthController.RETURN_TO_ALLOWLIST`) silently normalizes to `/profile`, so no functional impact, but the URL is wrong and confusing in logs. Confirmed in `api.log` 2026-05-17 18:50:04/44 entries.
  Suggested: wrap the handler — `onClick={() => linkSteam()}` — or change `linkSteam`'s signature to accept `unknown` and reject non-strings. The Discord link button at `:64` has the same shape and likely the same issue.
- **[dev-env-only, NOT tech-debt]** Clone-prod-to-local DB carries the operator's prod `users.steam_id` on the `roknua` user row (id=1). When the operator authenticates as the local `admin@local` user (id=115) and tries to Link Steam through OpenID with their real Steam ID, the `users_steam_id_unique` constraint correctly throws Conflict. Solution for dev testing: `UPDATE users SET steam_id = NULL WHERE id = 1;` before attempting the link, or test as `roknua` directly. Documenting in case other agents are confused by the same symptom.

### 2026-05-17 — fix/batch-2026-05-17 (surfaced during ROK-1312 rebase onto origin/main)

ROK-1312 was generated with migration index 0141 against `origin/main` at `8c7e1f20` (pre-ROK-1296). Concurrent merge of PR #804 added `0141_late_wrecking_crew.sql` (community_lineup_user_submissions) before this batch landed. Manually renumbered to `0142_feedback_client_logs` during rebase and updated `_journal.json` to keep both entries. Migration applies correctly (`drizzle-kit migrate` is content-driven, not snapshot-driven), but the `0142_snapshot.json` was emitted by drizzle-kit assuming the pre-ROK-1296 base — so it lacks origin's `community_lineup_user_submissions` table.

- **[low]** `api/src/drizzle/migrations/meta/0142_snapshot.json` — snapshot is missing `community_lineup_user_submissions` (added by 0141_late_wrecking_crew). Doesn't affect `drizzle-kit migrate` or runtime. Will surface on the next `db:generate` against the `feedback` schema (and possibly produce spurious deltas for unrelated tables). Self-resolves after one clean `db:generate` cycle against a DB that includes both 0141 + 0142.
  Suggested: next agent touching `feedback` schema OR running `db:generate` should re-emit `0142_snapshot.json` from a freshly-migrated DB. No urgency — runtime + CI both green.

### 2026-05-17 — fix/batch-2026-05-17 (surfaced during reviewer pass)

- **[low]** `api/src/feedback/feedback.controller.ts:81` — `attachSlowQueryContext` receives untrimmed `clientLogs` from `parsed.data` instead of `truncatedClientLogs`. Functionally harmless (the method only uses it as a truthiness gate; Zod already caps at 50_000), but semantically inconsistent with line 75 which correctly persists the trimmed value. ROK-1312 reviewer surfaced.
  Suggested: pass `truncatedClientLogs` for consistency, OR rename the parameter to `hasClientLogs: boolean` to make the gate intent explicit.
### 2026-05-17 — env-lock semantics: `deploy_dev.sh --down` is the real release, not `env_lock_release` (surfaced during ROK-1307 backup)

When `deploy_dev.sh --ci --rebuild` runs, it reasserts the env lock under its own PID with TTL 240min, **replacing** any MCP-acquired entry. After that point, calling `mcp__mcp-env__env_lock_release` from the original acquirer is a no-op — it returns `was_holder: false` because the deploy-script-anchored entry doesn't match the caller. The lock stays held until the deploy-anchored PID dies, the 240min TTL expires, OR `deploy_dev.sh --down` is run.

This bit ROK-1307: the lock was held for ~43 minutes after the operator gave the code-review verdict (the chrome-mcp playbook says release IMMEDIATELY after writing the summary). A queued agent (ROK-1299) waited the whole time. The release call at 23:05 returned `was_holder: false` and I didn't notice the signal — assumed "call returned" meant "released."

- **[low]** `.claude/skills/_shared/chrome-mcp-e2e.md` "Step 8: Release env lock" — add a note that after `deploy_dev.sh`-based deploys, `env_lock_release` is insufficient on its own; callers must `deploy_dev.sh --down` to release the script-anchored lock. Or check `was_holder: true` before treating release as successful. Tracked alongside [[ROK-1318]] (the `tools/mcp-env` semantics fix).

### 2026-05-17 — rok-1299 migration-drift incident (converted to Linear stories)

After rebasing rok-1299-decided-composite onto an updated origin/main (which had landed migration 0141 from ROK-1296), `./scripts/deploy_dev.sh --ci --rebuild` did **not** apply 0140 / 0141 to the local DB. The API came up healthy on the new code, but `community_lineup_user_submissions` (created by 0141) did not exist. Every `POST /lineups/:id/vote` then 500'd. Manual `node scripts/reconcile-migrations.mjs` made it worse — `Mode: trust (schemaRestored=true)` silently marked 0140 / 0141 + two older migrations as `trusted` without probing. Recovery required direct `docker exec psql` to apply `0141_late_wrecking_crew.sql` by hand.

All three findings from this incident are now tracked in Linear:

- [[ROK-1319]] (High) — reconcile-migrations trust-mode bug (cause + test coverage AC)
- [[ROK-1320]] (High) — deploy_dev.sh missing-migration drift probe (failure surface)
- [[ROK-1322]] (Med) — align legacy `run-migrations.ts` with `run-migrations-with-sentry.ts` (related boot-time / restore-time divergence from ROK-1281 postmortem)

### 2026-05-17 — fix/batch-2026-05-17 (surfaced during reviewer pass on ROK-1315)

- **[low]** `web/src/components/calendar/calendar-view.utils.test.ts:1-78` — 7 vitest cases cover gameless/game-having × undefined/empty/non-empty Set, but no case exercises `event.game` defined with `slug === ''` (empty string slug). Predicate treats it as gameless via `!event.game?.slug`, which is the intended behavior, but it's untested.
  Suggested: add one case `eventWithGame('')` + non-empty Set → expect `true`, locking in the empty-string-slug branch.
- **[low]** `scripts/smoke/calendar.smoke.spec.ts:159-223` — Regression test creates event via `apiPost` BEFORE entering the `try` block; if `apiPost` partially succeeds but throws, the event leaks (no `world.cleanup()` prefix-sweep covers `/events` rows).
  Suggested: either move `apiPost` inside `try` with `event?.id` guard in `finally`, or rely on `world.prefix` + a `/admin/test/reset-scheduled-events` cleanup hook.

### 2026-05-19 — rok-1297-nominating-composite (surfaced during /push validate-ci --full)

- **[low]** `scripts/smoke/navigation.smoke.spec.ts:22` — desktop: `Navigation (desktop) › nav links navigate to correct pages` flake. Not touched by ROK-1297 (only the lineup detail page surface).
  Suggested: rerun isolated 5×; if reproducible, audit nav-link rendering against header/footer changes in recent batches.
- **[low]** `scripts/smoke/notifications.smoke.spec.ts:20` — mobile: `Notifications › dropdown opens and shows content` flake. Notifications dropdown not visible at 7s on mobile. Not touched by ROK-1297.
  Suggested: bump the open-animation timeout or assert against an inner element instead of the container.

### 2026-05-20 — rok-1331-m5a-orchestrator (surfaced during M5a sweeper integration)

- **[high — FIXED in this batch]** `rl-infra/gc-sweeper/sweep.sh:122-125` (DEAD_SLOTS), `:149-152` (HOARDED_SLOTS), `:172-175` (STALE_WAITERS) — three jq filters in M1's sweeper were missing a closing paren on the `((($cutoff - ($tol|tonumber)) | tostring) > (.X | strptime(...) | mktime | tostring))` expression. The `2>/dev/null || true` guard swallowed jq's compile error → the dead-claim, hoarded-slot, AND stale-waiter reapers have been silent no-ops since M1 (a8380f1a) landed. Reproducible locally — every jq emits `syntax error, unexpected end of file, expecting ';' or ')'`. Fix is one extra `)` on each filter before the `| .slot`/`| .agent_id` outdent. **Folded into M5a's sweep.sh edits since the M5a sweeper-pin-safety legacy-row test required HOARDED_SLOTS to actually fire.** Operator: confirm with `bash -x` against the fleet that the reapers run as expected post-merge.

### 2026-05-21 — rok-1331-rl-infra-task-execution (surfaced during gap-fix cross-check, fresh Lead)

- **[low]** `rl-infra/cli/rl` `cmd_validate_ci` (~line 534) — when `rl validate-ci` exits non-zero, the CLI does NOT auto-release the slot. The heartbeat background process (started by `cmd_claim`) keeps the slot alive until gc-sweeper's `CLAIM_HEARTBEAT_TIMEOUT_SECONDS=300` (5min) reaps it OR the agent calls `rl release` explicitly. Slot is recoverable, just not immediate. **By design** (operator may want to ssh in and debug after failure, OR run a follow-up `rl run-on-runner ...` to inspect state), but UX is confusing: agents that ran a one-shot `validate-ci` expect cleanup and don't realize the heartbeat daemon is still alive. Surfaced 2026-05-21: Probe 1 attempt 11 exited 1; slot 1 stayed claimed for ~17 min before fresh Lead noticed and manually released.
  Suggested: document in `rl --help` / cli docstring that the slot persists across `rl validate-ci` exit codes and the agent owns the release call. OR add a `--release-on-exit` flag (opt-in) that traps EXIT and calls `cmd_release`. OR add a "Last validate-ci on slot N: FAILED, slot still held" line to dashboard fleet-health summary.

### 2026-05-21 — rok-1338-mcp-readonly-surface (surfaced during PR-1 dogfood probes)

- **[med]** `rl-infra/orchestrator/bin/env-spin` — failure rollback is incomplete. When env-spin aborts after the Postgres container is created but before the allinone is healthy (e.g., docker pull failure on a non-existent image tag when `skip_build:true`), the PG container (`rl-env-<slug>-pg`) is left running with no compensating cleanup. Operator's PR-1 round-2 probe reproduced this: Phase B with `skip_build:true` + a never-built image tag left `rl-env-rok1338-envspin-probe-2-pg` orphaned after env-spin exit 125. Today's PR-1 defensive-trap wrap surfaces the failure as JSON, but the trap fires AFTER docker compose has already started the PG side car.
  Suggested: add a cleanup branch to the EXIT trap in env-spin that runs `docker rm -f rl-env-<slug>-pg rl-env-<slug>-allinone 2>/dev/null || true` before exiting on failure, OR move the Postgres start AFTER the image-pull-and-validate step so a missing image fails fast before the PG container exists, OR document explicitly that env-destroy is the required cleanup after any env-spin failure. Operator already runs env-destroy as part of dogfood; just needs to be explicit in the runbook.

- **[high — FIXED in this batch]** rl-infra fleet — Mutagen worktree sync did not block claim from returning, causing downstream commands to hang. PR-1 round-3 probe Phase A reproduced: `rl_claim` returned slot 2 at 20:38:35 against branch `rok-1338-mcp-readonly-surface` (HEAD `4d8666ec`), but `rl_status` 6 minutes later showed `runner-2.last_sync_at = 2026-05-21T20:01:51Z` (37 min stale) and `runner-2.worktree_head = 61d348b` (a sibling branch's HEAD, NOT this claim's HEAD). `build_image` hung the full 6 min without progress before operator-cancel. **Root cause:** `cmd_claim` in `rl-infra/cli/rl` called `ensure_mutagen_sync` (which only registers the session via `mutagen sync create`) but did NOT call `flush_mutagen` to block until the initial sync round actually transferred files. Downstream `rl_env_build_image_from_runner` ran `docker build /workspace` against a runner whose /workspace hadn't synced yet → silent hang. **Fixed: added `flush_mutagen "$SLOT"` immediately after `ensure_mutagen_sync` in `cmd_claim`** (commit b8e392f6). This bug was invisible from the MCP layer before PR-1's new `rl_status.last_sync_at` + `runners[].worktree_head` fields — those were literally how the operator detected it during dogfood. The same PR that adds the diagnostic visibility also fixes the underlying race.

### 2026-05-21 — rok-1338-mcp-readonly-surface (codex + devedup-rl reviewer findings — non-blocking)

- **[low]** `rl-infra/orchestrator/bin/status:62-83` — `augment_runner_sync` runs `find` + `docker exec git rev-parse` per runner on EVERY `rl_status` call. Currently 2 runners so cost is ~200ms; on a busy 4-runner fleet could add 1-3s per call. Codex P2: gate the augmentation on whether the slot is claimed (skip the docker-exec when claimed=false; just emit null for unclaimed slots). Already returns null on missing worktree dir — this is a perf optimization, not a correctness fix.
  Suggested: `if (jq -r '.claimed' <<<"$claim_for_slot") = "true"; then augment; else echo "{...,last_sync_at:null,worktree_head:null}"; fi`.

- **[low]** `tools/mcp-rl-fleet/src/tools/task-inspect.ts:29-30`, `infra-logs.ts:64-85` — both new tool files inline their own `execFileP` helper + `sshUser()/sshHost()` getters. Same pattern as `task.ts`. Codex P2: pull these into `exec.ts` to dedupe ~50 lines. Non-blocking.
  Suggested: export `execFileP` + `sshConnectArgs(remote)` from `exec.ts`, replace local copies in task-inspect / infra-logs (and the original task.ts).

- **[low — FIXED in this batch]** `rl-infra/orchestrator/bin/env-spin` idempotent path — only ONE `LAST_PHASE="idempotent_path_entered"` marker vs 12 in the new-env path. devedup-rl reviewer recommended 2-3 more; commit efc42850 added: `idempotent_state_mutated`, `idempotent_traefik_written`, `idempotent_bootstrap_done`.

- **[nit]** `tools/mcp-rl-fleet/src/tools/{task-inspect,infra-logs}.ts` — neither accepts `worktree_path`. Intentional (read-only, no claim/sync dependency), but devedup-rl reviewer recommends a 1-line comment noting that's intentional so a future maintainer doesn't add it for parity with claim/env_spin.

### 2026-05-21 — rok-1338-mcp-readonly-surface follow-ups (operator-side, surfaced during PR-1 dogfood)

- **[med]** `/srv/rl-infra/traefik/conf.d` directory permissions — the dir was provisioned with `drwxr-xr-x rl rl-fleet` (mode 755); rl-agent (in rl-fleet group) couldn't create new env-*.yml files there, which silently broke EVERY env-spin since something reset the perms ~02:41Z 2026-05-21. ROK-1338 PR-1 dogfood surfaced this when LAST_PHASE markers narrowed env-spin's failure to the traefik write block; operator ran `sudo chmod 2775 /srv/rl-infra/traefik/conf.d && sudo chown :rl-fleet ...` manually to unstick it. The fix held for PR-1's final probe, but the chmod isn't persisted in VM provisioning — next reprovision (or whatever reset the perms originally) will reintroduce the silent break.
  Suggested: persist `drwxrwsr-x rl rl-fleet` (mode 2775, matches the state dir's setgid + group-write pattern) in whatever VM-provisioning script created `/srv/rl-infra/traefik/conf.d` originally. ROK-1338 PR-1's defensive wrap on `cat > env-${SLUG}.yml` will now catch a future regression with an actionable hint, so this is "make it permanent" rather than "the bug is invisible again."

### 2026-05-21 — rok-1337-test-plan-v2 (surfaced during dev TDD pass)

- **[low]** `rl-infra/dashboard/test/app-cleanup.test.js:130` `AC-M6b-21 listener-attach comment explains GC + replaceChildren semantics` — pre-existing source-grep test still fails on this branch's base (confirmed via `git stash && node --test test/app-cleanup.test.js`). The test asserts the comment block above the `passBtn.addEventListener('click', ...)` line in `public/app.js` mentions both `GC|garbage|gc handles` AND `replaceChildren|discard|prior nodes`. The actual comment in M6b's landed state mentions `replaceChildren scroll-jumps` (matches the second clause) but NOT GC. Not caused by ROK-1337 (the dev branch only adds tester-page modules, leaves the operator-route `renderStep` comment unchanged).
  Suggested: rewrite the comment block at `app.js`'s `passBtn.addEventListener('click', onVerdict('pass'));` site to explicitly say "GC handles orphaned listeners after replaceChildren" so the source-grep test passes. Belongs in a small docs/comment chore PR.

### 2026-05-21 — rok-1338-pr2-interactive-tools (surfaced during rl_db_query dev TDD pass)

- **[med]** `tools/mcp-rl-fleet` NUL-byte header-guard family — the CR/LF/NUL validator didn't reject `\0`, and the AC6 spec exercised it via `process.env`, which Node truncates at the first NUL (unsatisfiable as written). Flagged across 2026-05-20, 2026-05-21 ×2: rok-1331-m6a, rok-1331 Session-4 dogfood, rok-1338-pr2-interactive-tools. Instances: `tools/mcp-rl-fleet/src/tools/test-plan.ts:149`, `tools/mcp-rl-fleet/src/tools/__tests__/test-plan.spec.ts:77-112`. Last surfaced 2026-05-21. Tracked in ROK-1360 (shipped PR #884); the 2026-06-27 reconciliation verifies the `/[\r\n\x00]/` guard and the rewritten direct-helper test on main. Consolidated 2026-07-17.

### 2026-05-22 — rok-1338-pr3-ssh-lockdown (reviewer follow-ups for next cycle)

- **[low]** `tools/mcp-rl-fleet/src/exec.ts:492-501` — `UNREACHABLE_RE` would false-positive on psql-side `Connection refused` if a future write-mode `rl_db_query` (or similar) emits psql stderr that reaches the classifier. Today's tools' postgres-specific matchers run first in `db-query.ts::classifyError`, so this is latent.
  Suggested: anchor unreachable patterns with an `ssh:` prefix where feasible — `(?:ssh:\s+connect to host \S+ port \d+:\s+)?(?:Connection refused|Connection timed out|...)`. OR add a `psql:` head-exclusion in `classifySshFailure`. Reviewer flagged 2026-05-22; defer to a hardening cycle.
- **[low]** `tools/mcp-rl-fleet/src/exec.ts:475-486` — `DENIED_RE` could theoretically match `permission denied (some context)` postgres errors. Real-world postgres uses `permission denied for <object>` (no parens) so collision is unlikely; consumer-side classifier ordering keeps it safe today.
  Suggested: tighten to require a publickey/password/gssapi/hostbased context inside the parens — `\bPermission denied \([^)]*(?:publickey|password|gssapi|hostbased|keyboard-interactive)`. Reviewer flagged 2026-05-22.
- **[low]** `tools/mcp-rl-fleet/src/tools/__tests__/task.spec.ts` — `executeWait` watch-side classifier (inside the wait loop, not the probe-side branch) has no direct test. Probe-side is tested; in-loop is covered indirectly.
  Suggested: add `execFileOk(probe-success), execFileOk(status-running), execFileFail(255, 'Permission denied (publickey)')` test asserting immediate `ssh_denied` return.
- **[nit]** `tools/mcp-rl-fleet/src/tools/db-url.ts` — `psql_exec_cmd` alias kept for one cycle (`@deprecated`); needs a removal tracked.
  Suggested: add to the next cycle's removal list. Look for callers via grep before dropping.

### 2026-05-22 — rok-1335-recruitment-reminder-stale-channel (surfaced during validate-migrations.sh — RESOLVED in-PR, see below)

- **[RESOLVED, was high]** `scripts/validate-migrations.sh` failed because `npx drizzle-kit migrate` exits 1 silently against a fresh Postgres container — confirmed pre-existing on `origin/main` (a861b11d) via worktree-stashed checkout test. The drizzle-kit CLI swallows its own error via its spinner UX (hanji library); no message reaches stdout/stderr even with `NO_COLOR=1 TERM=dumb`, `script -qec`, or `--enable-source-maps`. Likely related to the broken snapshot chain documented in the 2026-05-17 entry above (0140 missing snapshot; 0141 + 0142 both `prevId: eb6febd0…` = 0139's id). Runtime migrations were NOT affected: `api/scripts/run-migrations-with-sentry.ts` uses Drizzle's programmatic migrator and applies cleanly; integration tests on the fleet pass 285/285 against a freshly-migrated DB; raw psql in journal order applies all 144 migrations cleanly. Only the standalone `drizzle-kit migrate` CLI was broken.
  **Resolution (ROK-1335 commit 9eb6a3e3):** rewrote `scripts/validate-migrations.sh` to apply `*.sql` files via `psql -v ON_ERROR_STOP=1` in `_journal.json` order. Bypasses drizzle-kit's broken CLI UX entirely; matches the behavior of `api/scripts/run-migrations-with-sentry.ts` (which is what production uses at boot). Surfaces real Postgres errors directly when a migration is broken, so future migration regressions become diagnosable instead of silent.
  **Follow-up (not blocking, low):** the underlying snapshot-chain corruption (0140 missing snapshot; 0141/0142 share `prevId`) is unresolved and still breaks `drizzle-kit generate`. Next agent that successfully re-emits a snapshot from a freshly-migrated DB should also write a `0140_snapshot.json` from 0139's state + 0140's SQL, OR delete the orphan snapshots so drizzle-kit doesn't try to follow them. Tracked under the 2026-05-17 entry above.

### 2026-05-22 — rok-1335-recruitment-reminder-stale-channel (devedup-rl reviewer pass — operator-triage)

- **[low]** `api/src/notifications/notification.service.ts:58-59` injects `ChannelResolverService | null` with `@Inject(...)`, while `recruitment-reminder.service.ts:49` (added in this PR) uses the plain type. Both live in the same module under the same `forwardRef`. Inconsistent. Worth reconciling — probably drop the `| null` from NotificationService since the forwardRef resolves at runtime in this project's NestJS setup.
- **[nit]** `recruitment-reminder.service.ts::postChannelBump` is now 31 raw lines / ~24 counted (under the 30-line `max-lines-per-function` warn threshold). If another tweak lands here, extract the log-formatting ternary (`(rebound from …)` branch) to a helper to stay clear of the limit.
- **[nit]** Single-guild assumption baked into the new "(rebound from CHANNEL)" log line. Not a problem today; flag if/when multi-guild work starts so we add guild scoping at the log layer.

### 2026-05-22 — rok-1343-drizzle-kit-bypass (devedup-rl reviewer low-severity nits — non-blocking)

- **[low]** Sentry context tag taxonomy — `api/src/scripts/run-migrations.ts:60` uses `{ tags: { context: 'restore-migration' } }` while the canonical boot-time runner at `api/scripts/run-migrations-with-sentry.ts:206` uses `{ tags: { context: 'boot.migration' } }`. Both are intentional and discriminate the two call sites in Sentry searches, but the project has no documented naming convention for `<phase>` tag values. ROK-1343 M4 explicitly specified `restore-migration` (hyphenated) so the spec is satisfied; this is a project-wide tag convention nit, not a ROK-1343 issue.
  Suggested: if/when a third boot-time script appears (e.g. seed-igdb-games Sentry instrumentation lands per ROK-1322 follow-ups), standardize on the dotted form (`restore.migration`, `boot.migration`, `seed.migration`) and add a one-liner to CLAUDE.md "Migration Generation Rules" naming the convention.

- **[low]** `scripts/recover-stuck-migration.sh:241-246` `ensure_table_sql` — the embedded `CREATE TABLE IF NOT EXISTS drizzle.__drizzle_migrations` schema (`id serial PRIMARY KEY, hash text NOT NULL, created_at bigint`) is verified to match `drizzle-orm/postgres-js/migrator.js` as of ROK-1343, but the dependency is implicit. If upstream changes the shape, this script will diverge silently on fresh-DB recovery (the common case of an already-existing table short-circuits via `IF NOT EXISTS`).
  Suggested: add a comment above `ensure_table_sql` pointing at `node_modules/drizzle-orm/postgres-js/migrator.js` so the next reader knows where to re-verify. Optional; impact bounded to the fresh-DB recovery path.

### 2026-05-23 — fix/batch-2026-05-22 (surfaced during ROK-1318)

- **[low]** `tools/mcp-env/src/tools/service-status.ts:160-164` — TS2339 "Property 'queue/holder/free' does not exist" — 6 errors. `executeLockStatus()` returns a union `LockState | { error, raw }`, and the `if ('error' in lockState && typeof lockState.error === 'string')` narrowing check at line 150 doesn't exclude the success arm because the optional `LockState.error` field would also satisfy `'error' in lockState`. TS therefore can't narrow the else branch to the success type. Pre-existing on origin/main — file is untouched by ROK-1318's diff (which only edits `env-lock.ts`, `env-lock.test.ts`, and adds `env-lock.bash.integration.test.ts`).
  Suggested: tighten `executeStatus`'s return type so the error envelope is discriminated (e.g. add a literal `kind: 'error'` field), OR refactor `fetchLockSummary` to use a runtime type guard that TS understands (`typeof lockState === 'object' && 'error' in lockState && typeof (lockState as { error?: unknown }).error === 'string'` returning `lockState is { error: string; raw: string }`). Either avoids the union-narrowing failure cleanly.

### 2026-06-02 — fix/batch-2026-06-02 (per-story reviewer findings, all PASS WITH NOTES — non-blocking)

ROK-1345 (roster slot race):
- **[low]** `api/src/events/signups-roster-slot.helpers.ts:61` — `insertRosterSlotWithRetry` 3-attempt cap is tuned for the typical N=2 race; N>3 simultaneous racers can still surface a 23505 to the caller. Within AC scope (test validates 2 concurrent). Suggested: add a one-line doc note that the cap is a design choice, not an N-racer guarantee.
- **[nit]** `api/src/events/signups-flow.helpers.ts` `assignDirectSlot` — redundant `autoBench:true` param passed to the retry helper when `slotRole` is already forced to `'bench'`. Harmless.
- **[nit]** `api/src/events/signups-roster.integration.spec.ts` — `occupant2`/`latecomer2` user naming smell (suffix hints at collision-avoidance with another test); `afterEach` reset makes it safe.

ROK-1328 (cron jobCache FK self-heal):
- **[low]** `api/src/cron-jobs/cron-job.service.ts` `runTracked` — after an FK self-heal retry against the fresh job id, `maybePrune` is still called with the original stale `job.id`, so the prune counter increments under the stale id and `pruneExecutions(staleId)` is a no-op. Minor off-by-N in the prune cadence for the fresh row; no data loss. Suggested: rebind the job ref before `maybePrune`.
- **[low]** `api/src/cron-jobs/cron-job.fk-recovery.integration.spec.ts` — test 1's `countExecutions(staleId) == 0` assertion is vacuous (cascade delete already removed rows); the real proof is `.resolves.toBeUndefined()`. Suggested: add a clarifying comment or assert on the fresh id instead.
- **[nit]** `api/src/cron-jobs/cron-job.helpers.ts` `recordSkippedTrigger` (manual-trigger path) bypasses the `insertExecutionRow` FK self-heal. Not in AC-1 scope; the trigger path receives a freshly-queried job row (not cache), so FK risk is low. Worth a follow-up if the trigger path ever develops the same stale-cache exposure.

ROK-1319 (reconcile-migrations trust-mode probe):
- **[low]** `scripts/reconcile-migrations.mjs:41` — `RL_MIGRATIONS_DIR` env override controls which journal/SQL dir is read. Test-scoped + commented; no psql injection (postgres.js parameterized). Acceptable; flag if it ever escapes test usage.
- **[nit]** `scripts/test/reconcile-migrations-trust-probe.test.sh:153,162,187` — `psql_exec` inside `$(...)` without an explicit error guard; functionally correct (empty var → comparison fails → `fail()`), just not explicit.
- **[nit]** same test — does not assert alpha/beta were NOT re-processed (skipped). The AC-3 "in sync" assertion covers it; an explicit `grep -v ran` would add depth.
### 2026-06-02 — fix/batch-2026-06-02-api (ROK-1320 + ROK-1112 + ROK-1136 reviewer findings)

Per-story reviewers ran clean except for ROK-1112, whose two actionable findings (N+1 voter-tz lookup; unescaped `]` in chat-tree markdown links) were FIXED inline on the batch (commit `47e3eafd`). Remaining non-actionable items deferred here. ROK-1320 + ROK-1136 reviews were clean (nits only, no action).

- **[low]** `api/src/discord-bot/ai-chat/tree/signups.tree.ts` (and the `staticLeaf` path in `events.tree.ts`) — a populated bullet-list of signups/events is returned in the `emptyMessage` field of `leaf(null, list, deps)` to deliberately short-circuit the LLM (`resolveContent` bypasses the model when `emptyMessage` is set). Functionally correct + intended, but the field name reads as "the message shown when empty," which will mislead future maintainers. Suggested: rename the leaf field (e.g. `staticBody`) or add a one-line comment at the leaf call sites documenting the deliberate reuse.
- **[nit]** `api/src/lineups/standalone-poll/standalone-poll.notify.spec.ts` — exercises the private `notifyVoters` via a cast. Acceptable (driving the public `recordVote` path would need much heavier DB mocking); noted only.
- **[note]** Reviewer-rok-1112 also flagged `recruitment-reminder.service.ts:169` as a possible missing-`timeZone` DM and the two service files as possibly over the 300-line ESLint cap. BOTH verified false: `resolveRecruitmentContext` already passes `timeZone: defaultTimezone`, and the runner `Lint (all): PASS` confirms no `max-lines` error. No action.

### 2026-06-02 — rok-1300-scheduling-composite (surfaced during ROK-1300 fleet deploy)

- **[low]** `rl_env_deploy` (mcp-rl-fleet) intermittently aborts at the `env_spin` phase with `exit_code 125` / `env_spin_aborted_unexpectedly` after `docker_run_pg_pending`, even though the sibling Postgres + allinone containers DO come up healthy moments later (verified via `rl_env_list`). The very next idempotent `rl_env_deploy` on the same slug succeeded fully (build cached ~10s, `env_spin` ok, `sync_settings` ok, admin seeded). Looks like a race in env-spin's pg-readiness check returning rc=125 before the container is observed running. Pre-existing fleet-side flake — not caused by ROK-1300's web/contract/api diff. Suggested: make the `docker_run_pg_pending` readiness probe in `env-spin` retry/wait for the container to report running before treating a transient `docker` rc=125 as fatal; meanwhile callers should retry the idempotent deploy once on `env_spin_aborted_unexpectedly`. NOTE: the ROK-1301 `RL_ENV_JWT_SECRET`-missing blocker documented above appears RESOLVED — `sync_settings` succeeded and `admin@local` seeded on the retry.

### 2026-06-03 — batch/2026-06-03 (surfaced during /bulk worktree setup)

- **[low]** `api/src/discord-bot/processors/embed-sync.processor.ts:239`, `api/src/events/event-plans.controller.ts:38`, `api/src/scripts/bootstrap-admin-linked-user.spec.ts:70,77` — these 3 files on `origin/main` (b73c0db9) are not lint-clean: `npm run lint -w api` (which runs `eslint --fix` per `api/package.json:21`) rewrites them on EVERY lint invocation — prettier line-wrapping in two files plus removal of a redundant `as z.output<T>` cast (`@typescript-eslint/no-unnecessary-type-assertion` auto-fix) in `event-plans.controller.ts`. Verified pre-existing: drift appeared identically in the main repo working tree before this batch AND in a freshly-created `batch/2026-06-03` worktree after a single lint run; not caused by any batch story (none touch these files). Every agent that lints `api` re-discovers these as "mystery modified files" (matches memory `feedback_lint_fix_worktree_drift`). Suggested: commit the 3-file auto-fix output once on main (`chore: lint-fix drift`) so lint runs become no-ops on a clean tree.

### 2026-06-06 — ROK-1329 AC-6 fleet verification (surfaced during ac6-sentry env deploy on slot 2)

- **[med]** `rl_env_deploy` / env-spin — RECURRENCE++ of the `env_spin_aborted_unexpectedly` flake (2026-06-02 ROK-1300 `[low]` entry, now med): env_spin aborted AFTER `allinone_running` but BEFORE writing the Traefik route file, leaving a healthy-but-unreachable env (all URLs 502, no `env-<slug>@file` router). Worse, the **idempotent re-spin path cannot self-heal**: once the containers exist, every re-run (`rl_env_deploy skip_build`, `rl_env_spin`) aborts at phase `idempotent_state_mutated` and never reaches route registration — manual `rl_env_destroy` + full re-spin (or hand-writing the route file) is the only recovery. Suggested: make env_spin's route-write idempotent and ordered BEFORE the `idempotent_state_mutated` early-exit, so a re-spin on an existing env always (re)writes the route file; treat "containers exist but no route file" as resumable, not fatal.
- **[note]** AC-6 deploy also confirmed two fixes working live: `SENTRY_ENVIRONMENT=fleet-slot-2` present on the spun container (PR #872 env-spin change, post rl-infra redeploy `b3febd38`), and the synthetic 500 landed in Sentry env `fleet-slot-2` with NO GitHub issue created (narrowed alert rule, ROK-1329 AC-5).

### 2026-06-06 — fleet-bug fix session (root-cause corrections to the two entries above)

- **[resolution]** The `env_spin_aborted_unexpectedly` recurrence from the ROK-1329 AC-6 entry was NOT a latent env-spin race — root cause was a **perms regression introduced that same evening** by deploying rl-infra with SETUP.md's bare `rsync -avh` (replicates laptop dir perms, stripping group-write+setgid from `traefik/conf.d`, so rl-agent could no longer create route files). The silent abort was an unguarded `exec 9>"$SLOT_LOCK_FILE"` dying under `set -e` BEFORE the (already-guarded) route write could fail loud. Fixed: VM perms restored to the canonical `2775 rl:rl-fleet`; `exec 9>` guarded in both env-spin paths; idempotent route write got the same fail-loud wrap as the fresh path; new `rl-infra/deploy.sh` (content-only rsync + perms re-assert + sweeper rebuild + sha stamp) replaces the raw rsync in SETUP.md. Dogfooded live: fresh spin on slot 1 wrote its own route (rl-agent:rl-fleet), slot URL 200, destroy cleaned up fully.

### 2026-06-06 — fix/rok-1347 (surfaced during ROK-1347 smoke-test authoring)

- **[note]** `tools/test-bot` `npx tsc --noEmit` reports `src/helpers/voice.ts(6,8): Cannot find module '@discordjs/voice'` — a missing optional dependency, pre-existing and unrelated to ROK-1347 (voice.ts untouched). Noted only.

### 2026-06-06 — fix/batch-2026-06-06 (surfaced during fix-batch local --full gate)

- **[med]** Fleet slot-hostname routing stale: `rl_env_deploy` for slug `fixbatch` returned `slot_url: null` + "Discord OAuth not available on this env — another env owns the slot rule", yet `rl_env_list` shows fixbatch is the ONLY env and it sits on slot 1; `https://slot-1.gamernight.net` answers **502** while the per-slug `https://fixbatchtest.gamernight.net` is healthy. A leftover Traefik slot-1 route file from a previously-destroyed env appears to both (a) block new deploys from claiming the slot rule and (b) dead-end the slot hostname. Per CLAUDE.md the slot URL is the canonical tester link (Discord OAuth only works there), so any env hitting this can't do Discord-login testing. Pre-existing fleet-side; not caused by the batch. Suggested: env-destroy (and the gc-sweeper) must delete the slot route file when the owning env goes away, and env-spin should treat a slot rule whose backend container no longer exists as reclaimable instead of "owned".
- **[low]** `api/src/discord-bot/services/scheduled-event.recovery.ts:170-176` — `reconcileBoundIds` issues up to 25 sequential per-row UPDATEs per recovery invocation (reviewer [low], fix/batch-2026-06-06). Operator-triggered + rate-limited, so no production risk; a single bulk `UPDATE ... WHERE id IN (...)` would be cleaner. Suggested: batch the UPDATE when next touching the recovery helper.
- **[nit]** Unknown SPA routes (e.g. `/lineups/1` — real route is `/community-lineup/:id`) render a fully blank body with header/nav and no 404 fallback. Surfaced during Chrome MCP gate. Suggested: add a catch-all NotFound route in `web/src/app-routes.tsx`.
- **[nit]** `packages/contract/src/lineup.schema.ts` — `votingEligibleCount` uses `.int().positive()` (banner :~324, detail :289 from ROK-1298). Reviewer suggested `.min(1)` for explicitness; semantically identical for integers, so no code change made. The real guard is `computeVotingEligibleCount`'s `Math.max(1, ...)` floor. Suggested: switch both to `.min(1)` next time the schema is touched.
- **[low]** `tools/test-bot/src/smoke/tests/` — first-ever Discord smoke runs against a FLEET env (API_URL=https://fixbatchtest.gamernight.net/api) surfaced two env-class issues, neither a code regression: (1) ROK-985 attendance-pipeline test fails deterministically with `No games for ROK-985 test` — the fleet env's demo seed lacks the game fixtures the test expects (passes on local/CI seeds); (2) under full-suite load the env intermittently 500s `POST /admin/test/await-processing` and SE-creation polls exceed 60s (6 such flakes in run 2, zero in run 1; endpoint probes 200 reliably when idle). Suite is otherwise green: 98/103 and 95/103 across two runs, every failure except ROK-985 passing in the adjacent run. Suggested: give the smoke fixture a seed-games bootstrap (or skip-with-reason when games are absent), and consider a remote-env profile with longer default timeoutMs.

### 2026-06-06 — rok-1354-discord-error-classifier (surfaced during fleet validate-ci --full)

- **[low]** rl-infra slot lifecycle — a slot claim auto-released by the gc-sweeper (missed heartbeats after a finished `rl_validate_ci` task) strands the claim's child envs: re-claiming gets a DIFFERENT slot and `rl_env_deploy` for the same slug then fails `slug_owned_by_other_slot` (env `rok-1354` was owned by unclaimed slot 2 while the new claim sat on slot 1). Recovery this run: `rl_env_destroy force=true` + `rl_env_spin` from the already-built image. Suggested: either let a re-claim by the same `agent_id` inherit envs stranded on unclaimed slots, or have `rl_env_deploy` treat a slug owned by an UNCLAIMED slot as reclaimable.
- **[low]** `api/src/notifications/discord-notification.processor.ts:103` — `process()` is now 51 counted lines vs the 30-line `max-lines-per-function` WARN (devedup-rl reviewer, ROK-1354). Warn-only, not a CI failure. Suggested: extract the catch-branch dispatch (`permanent-deactivate` / `permanent-prefs-only` / transient) into a private helper next time the processor is touched.
### 2026-06-03 — rok-1346-lineup-participants (fleet Playwright gate, 3-layer dig)

- **[med — FIXED]** Fleet env **Discord OAuth login was broken** — "Continue with Discord" on `https://slot-1.gamernight.net` completed Discord auth but the backend redirected to the **internal** hostname `http://rl-env-rok-1346-allinone/auth/success?code=…` (unreachable from the browser). Root cause: `rl-infra/orchestrator/bin/env-spin` set `DISCORD_CALLBACK_URL` to the slot host but **never set `CLIENT_URL`** on the env container, so `discord-auth.controller.ts`'s `getClientUrl()` (`CLIENT_URL || getOriginUrl(req)`) fell through to `getOriginUrl(req)` which builds the URL from the `Host` header the API receives (the internal container name). **Fix (committed in `rl-infra/orchestrator/bin/env-spin`, ROK-1346):** add `-e "CLIENT_URL=https://${SLOT_HOST}"` to `PUBLIC_URL_FLAG` alongside the existing `DISCORD_CALLBACK_URL`. Applied live on the VM + committed to the rl-infra source; verified `CLIENT_URL=https://slot-1.gamernight.net` on a fresh `env_spin`. NOTE: takes effect only on a **fresh container** — an idempotent redeploy that reuses the running container won't pick up new `-e` vars (`rl_env_destroy` + redeploy, or the container-recreate fix in the stale-build entries). Username login was the interim workaround.

### 2026-06-03 — rok-1346-lineup-participants (reviewer findings, not auto-fixed)

- **[med]** (Codex `[P2]`) `rl-infra/orchestrator/bin/env-spin:351` — `CLIENT_URL=https://${SLOT_HOST}` is correct for the common one-env-per-slot case and for the slot OWNER (OAuth only works for the owner anyway), but when two envs share a slot, a NON-owner env's `CLIENT_URL` points at `slot-N.<domain>` (the owner env), so its bot share-links / notification URLs / CORS can target the older env. Consistent with the pre-existing `DISCORD_CALLBACK_URL` slot-host limitation. Suggested: determine slot-rule ownership BEFORE the `docker run` and set `CLIENT_URL` to the per-slug `PUBLIC_URL` for non-owner envs while keeping the slot host only for the owner (and for `DISCORD_CALLBACK_URL`). Edge case; not fixed inline.
- **[low]** `api/src/lineups/lineups-participants.helpers.ts` — the pure `deriveRole`/`deriveStatus`/dedup internals are unexported and covered only via the integration spec (union+dedup, voted>nominated>waiting, role precedence, deactivated exclusion all asserted there). Spec Test-Plan called for a dedicated unit test of the derivation helper. Spec-letter gap, not a behavior gap. Suggested (optional): export the pure helpers + add a fast unit test.
- **[low]** `api/src/lineups/lineups-participants.helpers.ts:164-168` — `loadRosterSources` runs the nominator + voter `selectDistinct` scans for PRIVATE lineups too (needed so a private invitee who voted resolves `status: voted`). Two extra distinct scans per private-roster fetch; negligible at current scale. No change required.
- **[low — pre-existing, NOT this diff]** `rl-infra/dashboard/test/app-cleanup.test.js` `AC-M6b-21` (a comment-content assertion on `public/app.js`) fails on origin/main; neither file is touched by ROK-1346. Surfaced while running the dashboard suite for the new pruner test (80/81 pass; the 1 failure is this). Pre-existing; documenting per the "document pre-existing failures" rule.

### 2026-06-07 — rok-1346-lineup-participants (pre-push local Playwright full-suite run)

- **[low — FIXED in-branch]** `api/src/admin/demo-test-fixture-user.controller.ts` — `seed-fixture-user`'s SELECT-or-INSERT reused a fixture row WITHOUT clearing `deactivated_at`; the daily guild-membership cron (ROK-1260) deactivates the synthetic `smoke-invitee-fixture-001` user (no real Discord membership), after which `addInvitees`' `activeUsersFilter` 404s `Unknown user id(s)` and every private-lineup smoke fixture fails on long-lived envs (fresh-DB CI never sees it). Fixed in this branch: the idempotent repair UPDATE now also sets `deactivatedAt: null`.

### 2026-06-07 — fix/batch-2026-06-07 (backlog reconciliation — rl-infra/fleet open-hitter sweep)

Reconciliation pass over the rl-infra/fleet entries above, done while assembling this batch. Append-only; entries below record which earlier items are RESOLVED on main or now tracked in Linear, so the next `/readlogs` doesn't re-discover them. Operator: when convenient, strike the originals.

- **[resolution]** gc-sweeper PID-namespace false-orphan bug (2026-05-21 rok-1331 [high]) — ALREADY FIXED on main by ROK-1336 (#833): orphan detection now uses the per-task pidfile + mtime contract written by the supervisor (see `rl-infra/gc-sweeper/sweep.sh` ~line 139 comment), not `kill -0`; `state::mutate` chmods 664 so post-mutate JSON stays dashboard-readable. The "pick one of 4 fix paths" operator decision in the original entry is moot (option 3, pidfile, shipped).
- **[resolution]** `validate-ci.sh::perf_emit_local` silent-failure diagnostic (2026-05-21 rok-1331 [high]) — the exact suggested fix is ALREADY on main: emission errors now append to `${PERF_LOG_LOCAL}.errors` (validate-ci.sh ~lines 191-200) instead of being swallowed. Underlying runner-side root cause still unconfirmed — the next fleet run that drops events will leave a `.errors` file saying why. No further code change needed until that evidence lands.
- **[tracked]** Fleet admin@local password rotation (2026-06-03 [high], 2026-06-02 ROK-1300 [med]) — root-cause remainder (first rebind-from-placeholder still rotates despite PR #863's guard) now tracked as **ROK-1356**, in this batch. (#883 has since set RL_ADMIN_PASSWORD on the VM — the operational fix; ROK-1356 remains the code-side completion so the returned admin_password is trustworthy even without it.)
- **[tracked]** env-spin stale-image reuse on redeploy (2026-06-02 ROK-1300 [high]) + orphan-PG rollback (2026-05-21 rok-1338 [med]) + unclaimed-slot slug strand (2026-06-06 rok-1354 [low]) — now tracked as **ROK-1357**, in this batch.
- **[tracked]** `rl_env_sync_from_local` false "public schema is empty" probe (2026-06-06 fix/batch [med]) — now tracked as **ROK-1358**, in this batch.
- **[tracked]** runner missing tools/test-bot deps for Discord smoke (2026-06-06 rok-1354 [med]) — now tracked as **ROK-1359**, in this batch.
- **[tracked]** mcp-rl-fleet NUL-byte header guard + unsatisfiable test (2026-05-20/-21 [med]) + `UNREACHABLE_RE`/`DENIED_RE` tightening + executeWait watch-side test (2026-05-22 [low] ×3) — now tracked as **ROK-1360**, in this batch.
- **[tracked]** orchestrator test suite macOS/BSD coreutils failures — GNU `timeout` (2026-05-20 m5a/m5b, 2026-06-02 [med]), `flock` (2026-05-20 m6a [low]), `test_task_list.sh` 12/13 (2026-05-21 rok-1336 [med], 2026-06-02 [med]) — now tracked as **ROK-1361**, in this batch.
- **[note]** Slot-hostname routing stale / leftover Traefik slot route (2026-06-06 fix/batch [med]) — likely addressed by #877 (TTL-reap now removes the route file); will be verified during this batch's fleet dogfood rather than re-fixed blind.

### 2026-06-07 — fix/batch-2026-06-07 (per-story reviewer findings — all PASS/SHIP, low/nit only)

Six per-story reviewers (sonnet), zero critical/high. The one actionable dogfood finding (bare `rl_env_spin` comparing against the default `:latest`) was FIXED in-batch (`ab8c4198`, IMAGE_EXPLICIT guard + test ac6.3b) — not listed here.

- **[low]** `rl-infra/orchestrator/bin/env-spin` (~line 150) — `OWNING_SLOT_CLAIMED` uses `jq --argjson s "$EXISTING_SLOT"` with `|| echo "0"`; a non-numeric `rl.slot` docker label (corrupt container) makes jq fail silently → falls back to 0 → owning slot treated as UNCLAIMED → reclaim allowed. Theoretical (labels are always written as integers by env-spin itself). Suggested: validate `EXISTING_SLOT` is numeric before the query; refuse-with-error otherwise.
- **[low]** `api/scripts/bootstrap-admin.ts` — pre-existing (NOT this batch): `credAlreadyBound && !resetMode` guard fires before `ADMIN_PASSWORD` is considered, so a bound credential + `ADMIN_PASSWORD` WITHOUT `RESET_PASSWORD=true` silently ignores the fixed password. env-spin always pairs both flags, so no live impact. Suggested: log a hint ("ADMIN_PASSWORD ignored without RESET_PASSWORD") in that state.
- **[low]** `rl-infra/cli/rl::install_runner_deps` — the new test-bot prefetch uses `npm ci` only (no `npm install` fallback, unlike validate-ci.sh's two-step). Lockfile drift on the runner fails silently at claim time and is recovered by validate-ci.sh's fallback at smoke time. By design (best-effort prefetch); documenting the asymmetry.
- **[low]** `rl-infra/orchestrator/test/test_helpers.sh::rl_timeout` — pure-bash fallback (macOS-no-coreutils only) kills `$cmd_pid`, not the process group; grandchildren of the timed command could orphan if the watchdog fires. Theoretical for current callers. Suggested: `kill -- -$pid` with `set -m`, or document the limitation.
- **[nit]** `rl-infra/orchestrator/test/test_env_destroy_m6a.sh::m6a_teardown` — doesn't restore PATH after prepending the fake-flock stub dir. Benign (AC12 is the file's last test; run-tests.sh isolates files in subshells). Suggested: PATH save/restore in setup/teardown for hygiene.
- **[nit]** `tools/mcp-rl-fleet/src/exec.ts` — pre-existing: `kex_exchange_identification: Connection closed by remote host` (no `port 22` suffix) is unclassified by `UNREACHABLE_RE`'s closed-by branch; falls through to the generic exit-255 path. Same behavior before ROK-1360. Suggested: add a `kex_exchange_identification` branch if consistent classification is wanted.

### 2026-06-07 — rok-1353-refresh-tokens (fleet validate-ci --full runs)

- **[low]** fleet dashboard API — `https://fleet.gamernight.net/api/tasks/<id>/log` returns **502** from the laptop (and `/api/tasks/<id>` 404s), so agent-side curl polling of task progress is impossible; only the MCP `rl_task_status`/`rl_task_logs` path works. Cost this build: two 45-60 min Monitor watches sat silent past task completion. Suggested: expose the task status/log routes through the public Traefik vhost (read-only), or document the MCP-only contract in rl-infra README.

### 2026-06-07 — rok-1353-refresh-tokens (reviewer findings, not auto-fixed)

- **[low]** `api/src/auth/refresh/refresh-token.service.ts:130-149` — `mintChild` INSERTs the child row and UPDATEs `parent.replaced_by` in two statements, not a transaction; a crash between them leaves `replaced_by` NULL. Harmless today (`replaced_by` is audit-only lineage; family revoke keys on `family_id`). Suggested: wrap in `db.transaction` next touch.
- **[low]** `api/src/plugins/discord/discord-auth.helpers.ts:139-150` — `isSilentOAuthState` parses the OAuth `state` without HMAC verification; used only to choose the failure-redirect target (server-derived URL, both branches benign), never grants auth. Suggested: verify the signature there too for defense-in-depth.
- **[nit]** `api/src/plugins/discord/discord-auth.helpers.ts:126` — `returnTo` is signed into the silent state but never consumed server-side (callback always redirects `/auth/success?code=`). Dead weight: drop it or validate-and-use it.
- **[nit]** localStorage/sessionStorage key strings duplicated across `web/src/lib/api/refresh-client.ts:14`, `web/src/hooks/use-auth.ts:8`, and hardcoded in `web/src/pages/auth-success-page.tsx:70-71` (`raid_ledger_token`, silent-guard key). Drift would silently break transparent refresh. Suggested: extract shared consts module.

### 2026-06-07 — rok-1353-refresh-tokens (security-review P2/P3 remainder, not fixed in-branch)

- **[low]** Silent re-auth drops `returnTo`: it's signed into the OAuth state (`discord-auth.helpers.ts:126`) but the callback never forwards it, so a successful silent re-auth lands on `/calendar` instead of the page that triggered recovery. Companion to the earlier "returnTo dead weight" nit — either consume it in the callback redirect (validate same-origin path) or drop it from the state. UX-only; security reviewed both ways as benign.

### 2026-06-07 — rok-1351-dual-binding (surfaced during TDD test authoring)

- **[low — tooling drift, NOT this branch]** `api/package.json:28` — `test:integration` script + the ROK-1351 dev brief both invoke Jest with `--testPathPattern=<x>`, but the installed Jest rejects it: `Option "testPathPattern" was replaced by "--testPathPatterns"` (note the trailing `s`; new flag is CLI-only). Pre-existing: a Jest major bump renamed the flag and no call site / doc was updated. Workaround used: `npm run test:integration -w api -- --testPathPatterns=channel-bindings`. Suggested: sweep skills/docs/CLAUDE.md references from `--testPathPattern` to `--testPathPatterns`, or pin a Jest version that still accepts the old form.
- **[low — pre-existing, NOT this branch]** `tools/test-bot/src/helpers/voice.ts:6` — `npx tsc --noEmit` in `tools/test-bot` errors `TS2307: Cannot find module '@discordjs/voice'`. The package IS in `tools/test-bot/package.json` deps but is not installed in this worktree's `node_modules` (`ls node_modules/@discordjs/voice` → No such file). Not in my diff. Suggested: `npm install` in the worktree (or document that test-bot tsc needs a full install); the smoke runner uses `tsx` at runtime so this only bites a standalone tsc gate.
- **[low]** `tools/test-bot/scripts/no-sleep-lint.sh` false-positives on the literal `sleep()` token inside comments (JSDoc at `tools/test-bot/src/smoke/tests/lineup-abort.test.ts:26`). Flagged across 2026-05-14, 2026-05-15, 2026-06-06, 2026-06-07 ×2: fix/rok-1243, rok-1036-allinone-privilege-drop, fix/rok-1347, rok-1350-se-rename, rok-1351-dual-binding. Instances: `tools/test-bot/scripts/no-sleep-lint.sh:13`, `tools/test-bot/src/smoke/tests/lineup-abort.test.ts:26`. Last surfaced 2026-06-07. RESOLVED per the 2026-06-27 reconciliation (comment reworded + lint exits clean, PR #910). Consolidated 2026-07-17.

### 2026-06-07 — rok-1362-bounded-waits (surfaced during ROK-1362 build)

- **[med]** `tools/mcp-rl-fleet/src/exec.ts:854` — `npx tsc --noEmit` (workspace `tools/mcp-rl-fleet`) fails with TS2345/TS7006 on `worktreePathSchema`'s `.refine(check, (val) => ({message}))` second-arg function form: "Argument of type '(val: any) => { message: string }' is not assignable to parameter of type 'string | {...} | undefined'" + "Parameter 'val' implicitly has an 'any' type". Pre-existing on origin/main (file untouched by ROK-1362). Not caught by GitHub CI because the `lint`/typecheck jobs do NOT run tsc over `tools/mcp-rl-fleet/**` (the workspace `lint` script is `echo`, and there is no eslint/tsc job covering tools/). Likely a zod (^3.24) × TypeScript (^6.0.3) overload-resolution mismatch on the legacy `.refine` errorMap-function form. Suggested: switch to `.superRefine((val, ctx) => ctx.addIssue({code:'custom', message}))`, or pass the message as a plain string / `{ message }` object form.
- **[low]** `tools/mcp-rl-fleet/src/tools/task.ts` — 576 raw lines, already over the repo-wide 300-line `max-lines` cap BEFORE ROK-1362 (the cap is not mechanically enforced on this workspace — no eslint config covers `tools/mcp-rl-fleet/**`). ROK-1362 deliberately keeps task.ts net-roughly-flat by extracting shared schemas to `task-schemas.ts` and the laptop registry to `local-task.ts` rather than inlining; a full <300 refactor of task.ts is out of scope per the ROK-1362 spec. Suggested: split the four executors (status/wait/cancel/list) into sibling modules in a dedicated tech-debt pass.
- **[low]** CI gap (not a regression): no GitHub workflow runs the `tools/mcp-rl-fleet` Vitest suite — the fleet MCP behavioral tests are the gate for fleet-tooling changes but execute only locally. ROK-1362 adds a `tools-tests` job to `.github/workflows/ci.yml` to close this. Flagging for awareness in case the operator wants it scoped/named differently.
### 2026-06-07 — rok-1316-suggestions-pregen-swr (surfaced during ROK-1316 SWR rework)

- **[note]** `web/src/components/lineups/PersonalSuggestionsRow.tsx` + `NominateModal.tsx` (personalize UI) [low] — the per-user "Suggested for you" personalize path was gutted by ROK-1297 (per-user LLM dispatch removed) and is now fully served by base suggestions after ROK-1316 (the `?personalize=me` query short-circuits to base, zero LLM). The components still render (no contract break) but the "personalize" affordance is cosmetic — it shows the same base suggestions as the Common Ground grid. Suggested: remove PersonalSuggestionsRow + the NominateModal personalize wiring in a follow-up cleanup story (explicitly out of scope for ROK-1316 per operator scope-lock — tech-debt note only).

### 2026-06-18 — fix/batch-2026-06-18 (ROK-1181 / ROK-1182 / ROK-1206)

All three stories shipped with reviewer PASS and no critical/high/med/low findings. The nits below are cosmetic; recorded for triage only.

- **[nit]** `web/src/plugins/wow/lib/wow-era.ts` (ROK-1182) — `WOW_SLUG_TABLE` uses ALL_CAPS, a convention this codebase generally reserves for *exported* constants; this table is module-private. Intent is clear; non-blocking. Suggested: rename to `wowSlugTable` if aligning with local convention matters.
- **[nit]** `api/src/drizzle/timestamp-utils.spec.ts` (ROK-1206) — no test case covers the two-digit `+00` offset, which is the exact format `findEventStartTime` (`lower(duration)::text`) yields and which `parseTimestampUtc` intentionally does NOT handle (the regex requires `±HH:?MM`). A negative test would make the intentional gap explicit rather than implicit. The behavior is documented in a code comment. Suggested: add a test asserting `parseTimestampUtc` is not safe for two-digit offsets.
- **[nit]** `web/src/components/scheduling/duration-picker.tsx` (ROK-1206) — props typed via inline object literal rather than a named interface, a minor style divergence from some sibling components. Not a standards violation.
- **[nit — pre-existing, NOT introduced by ROK-1181]** `api/src/characters/characters-import.helpers.ts` `requireExistingForMerge` throws `ConflictException("... already exists")` when the post-unique-violation lookup returns no row; a `NotFoundException` would be semantically more precise. Message is byte-for-byte identical to pre-refactor code. Suggested: revisit if/when this path is touched again.

### 2026-06-18 — fix/batch-2026-06-18 (flake-fix follow-ups)

The recurring smoke flake set (community-lineup banner, create-event, navigation, onboarding, tiebreaker beforeAll timeouts) was fixed this batch with deterministic waits + local `retries:1`/`timeout:60s`/`expect:10s` headroom (commit `fix(test): deterministic waits...`). Two residual items remain:

- **[high — pre-existing deterministic, NOT a flake]** `scripts/smoke/profile-avatar-prefs.smoke.spec.ts:112` — "timezone section renders with select" asserts `toHaveValue('auto')` but the admin's saved timezone is `America/New_York`, failing on every attempt (incl. retries). Root cause is test-state/seed: the admin user's `timezone` pref is not `'auto'` in the local/cloned DB (likely set by demo seed, settings-sync from prod, or a sibling profile test that doesn't reset). Unrelated to this batch's diff. Suggested: reset the admin timezone to `'auto'` in a `beforeAll` (via `/admin/test/*`), or assert the default more robustly (accept a resolved IANA zone), or fix the seed to leave timezone unset.

### 2026-06-07 — prod recovery runbook (surfaced during ROK-1347/ROK-1355 execution)

- **[low]** `POST /admin/scheduled-events/recover-orphans?dryRun=false` — every bulk-delete invocation against prod 504'd at the nginx/proxy 60s timeout while SUCCEEDING server-side (25 Discord deletes ≈ 60-90s with rate-limit pacing). Operators must re-poll with a dry-run to learn the outcome; the response body is lost. Suggested: stream/early-return a 202 with a progress handle, lower RECOVERY_DELETE_BATCH to ~15, or raise the proxy timeout for /admin/* routes.

### 2026-06-26 — origin/main red (surfaced during /unblock-prs queue triage)

`origin/main` @ `8bbc4501` (and `6098fffc`) has a red CI workflow + red Discord Smoke workflow. Confirmed pre-existing — none of the open PRs in the unblock queue (#904–#909 deps, #911 search, #913 voice-bind, #914 vote-button) touch the failing surfaces. Combined deps branch (`combined/unblock-2026-06-26`) is green locally on all of these (build + tsc + lint + 6292 api unit + 3552 web unit).

- **[med]** `tools/mcp-rl-fleet/src/tools/__tests__/test-plan-v2.spec.ts` — `ROK-1337 AC2 — executeWait watches the slug directory, not a single file > watches /srv/rl-infra/state/test-plans/{slug}/ (trailing slash, directory)` fails: `AssertionError: expected null not to be null` (1 failed / 301 passed in the `tools-tests` job). Distinct from the 2026-05-21 `app-cleanup.test.js:130` test-plan entry above — this is the `executeWait` directory-watch assertion. Not touched by any queued PR. Suggested: read `test-plan-v2.ts::executeWait`'s watcher setup against AC2 — the watched-path capture returns null where the spec expects the slug-dir string (likely an fs.watch mock wiring or path-normalization regression).
- **[med]** `scripts/smoke/lineup-creation.smoke.spec.ts:275` (`Phase countdown display › detail page shows full countdown timer`) — `getByText(/remaining|countdown|time left/i)` not visible (10s timeout) on `smoke-test-shard (2/5)`, desktop. Same run logs `Reset-to-seed returned 403: Only available in DEMO_MODE — falling back to demo/install`, i.e. the seed reset isn't applying, so the countdown-bearing lineup state isn't present. Overlaps the reset-to-seed seeding flake class already noted (2026-06-07 / line ~706). Suggested: ensure the smoke harness runs with DEMO_MODE so `/admin/test/reset-to-seed` is authorized instead of silently falling back, OR make the countdown assertion seed-independent.
- **[low]** Discord Smoke Tests workflow `discord-smoke` job red on `8bbc4501`. Same recurring cold-start/timeout flake class documented 2026-06-07 (line ~882); recording recurrence on main only. Suggested: track recurrence rate; gate behind a warmed-bot health check if it persists.

### 2026-06-27 — fix/rok-1364-drizzle-snapshots (surfaced during ROK-1364 documented-bug batch)

- **[low]** `api/src/drizzle/migrations/meta/` — **38 historical migration snapshots are missing** and one ancient prevId chain break exists at `0102_lineup_phase_scheduling` (prevId references `0101_reg` but the prior snapshot id is `b207bece…`). Missing tags (all ≤ 0137, predate ROK-1364's 0144–0146): `0011, 0040, 0054–0057, 0062, 0074, 0080–0088, 0092–0095, 0097, 0104–0106, 0108–0111, 0113, 0118, 0120, 0121, 0131, 0133, 0134, 0136, 0137`. Harmless today — `drizzle-kit generate`/`check` only diff against the HEAD snapshot (now contiguous after the ROK-1364 fix), and the runtime migrator + `validate-migrations.sh` never read snapshots (they replay `.sql` in journal order). Surfaced by the new `migration-snapshots.spec.ts` guard, which is therefore scoped to the HEAD invariant only. Suggested: a one-shot backfill (regenerate each missing snapshot by replaying the chain) if we ever want `drizzle-kit check` to validate the full history; low priority.

---

## Reconciliation 2026-06-27 (verify-first sweep — 12 agents)

A 12-agent pass verified all 293 backlog entries against current `main` (tsc clean; PRs through #919). **95 entries (~32%) are RESOLVED** and can be archived; the remaining ~198 are genuine open items (mostly unactioned low/nit code-quality suggestions + real flakes, conservatively kept). Approx line + evidence below — safe to strike/remove the cited entries.

### High confidence — shipped PRs / tsc-clean / already-struck (26)

- **~L61** — fleet sync_settings sudo-grep reads /srv/rl-infra/.env via rl-agent — _already struck; read_infra_env() present sync-local-to-env.sh:487 + test sync-local-to-env-infra-read.test.sh exists (fix 2026-06-02)_
- **~L63** — sync error can't distinguish missing-var from SSH-read-failure — _already struck; INFRA_ENV_READ_RC differentiation present in read_infra_env() + same test file (fix 2026-06-02)_
- **~L196** — migration 0140 audit prepopulation bug (prod outage) — _fixed in ROK-1281 run-migrations-with-sentry.ts (text self-states fix)_
- **~L417** — ROK-1319 reconcile trust-mode marks unapplied as applied — _reconcile-migrations.mjs now probeEffectExists-gated (#869)_
- **~L418** — ROK-1320 deploy_dev.sh migration-drift probe — _shipped PR#869_
- **~L437** — ROK-1316 lineup suggestions block on Gemini cache miss — _shipped PR#888 (commit a56be725)_
- **~L440** — ROK-1319 reconcile trust-mode bug (dup of L417) — _probe fix in reconcile-migrations.mjs (#869)_
- **~L441** — ROK-1320 deploy_dev.sh drift probe (dup of L418) — _shipped PR#869_
- **~L468** — games-lookup.integration.spec:306 TS2307 missing controller — _import removed, controller present, tsc clean_
- **~L484** — validate-ci.sh run_typecheck masks api tsc failure — _fixed with `_
- **~L526** — common-ground spec missing itadLowestPrice (TS2741) — _tsc-clean + fixtures added at spec L60/L100 on origin/main_
- **~L531** — invite-page test "/join event/i" button not found — _invite-page.test.tsx passes 18/18 now; renderer emits "Join Event" (invite-character-step.tsx)_
- **~L576** — gc-sweeper jq filters missing closing paren (silent no-op reapers) — _filters present + reaper loops fire; folded into M5a, merged #832_
- **~L596** — gc-sweeper orphan kill -0 wrong PID namespace — _switched to per-task pidfile + chmod 664; ROK-1336 #833 (61d348be)_
- **~L622** — Mutagen sync didn't block claim return (silent hang) — _flush_mutagen added in cmd_claim; merged #834 (rl:287)_
- **~L667** — validate-migrations.sh drizzle-kit silent-exit — _already labeled [RESOLVED] in-file — commit 9eb6a3e3 (archive whole block)_
- **~L742** — rl_env_deploy served stale build (fix/batch-2026-06-02-ui) — _already-marked RESOLVED; sync-guard shipped PR #868 (6e78107e)_
- **~L747** — rok-fleet-stale-build-sync-guard — _entry marked [high — FIXED]; PR #868 merged, sync-guard.ts present_
- **~L815** — fleet admin@local 401 blocker (RL_ADMIN_PASSWORD) — _already marked RESOLVED; VM env set, confirmed_
- **~L817** — Playwright runner image v1.50 vs project 1.60 — _rl-infra/runner/Dockerfile now `v1.60.0-jammy` (commit 8fdfa260)_
- **~L838** — gc-sweeper PID-namespace false-orphan bug — _ROK-1336 PR #833 (pidfile contract shipped)_
- **~L839** — validate-ci perf_emit_local silent-failure diagnostic — _fix on main (.errors append, validate-ci.sh ~191-200)_
- **~L840** — fleet admin@local password rotation (ROK-1356) — _ROK-1356 shipped PR #884 (MERGED)_
- **~L841** — env-spin stale-image reuse on redeploy (ROK-1357) — _shipped PR #884_
- **~L903** — rl-infra runner Playwright v1.50 vs required v1.60 image lag — _PR #890 / 8fdfa260; runner/Dockerfile now FROM playwright:v1.60.0-jammy_
- **~L932** — onboarding Steam selector strict-mode collision — _scripts/smoke/onboarding.smoke.spec.ts:370 now `{ name: 'Steam', exact: true }`_

### Medium confidence (35)

- **~L342** — community-insights operator-role 401 on GET endpoints — _controller now class-level `@Roles('operator')` (community-insights.controller.ts:46) covers all GET endpoints + CI green on main_
- **~L374** — lineup-confirmation-pills invitee hero/checkmark selectors — _spec rewritten for new hero by ROK-1297 PR #823 (`/nominate a game/i` markup)_
- **~L376** — events smoke attendance light-mode theme tokens — _deterministic Playwright check, CI green on main (required 5-shard suite blocks merge)_
- **~L378** — onboarding game-time compact GameTimeGrid viewport — _smoke-flake hardening PR #900 (2026-06-18) touched spec_
- **~L379** — community-lineup banner visible on mobile — _reworked + flake-hardened by PR #900 / #871 / #854_
- **~L380** — lineup-carryover auto-populate from prior decided matches — _deterministic Playwright check, CI green on main_
- **~L438** — ROK-1317 useAiFeatures polls 403 for non-admin — _shipped PR#843_
- **~L439** — ROK-1318 env_lock_release no-ops after re-anchor — _shipped PR#843_
- **~L462** — games-dedup-audit.integration.spec:386 TS2769 — _api tsc (spec-inclusive tsconfig.json) clean, 0 errors_
- **~L464** — games-dedup-audit.service.spec:443 TS2502 — _api tsc clean_
- **~L466** — games-dedup-merge.integration.spec TS2352 x3 — _api tsc clean_
- **~L470** — lineup-deadline-vote-race.integration.spec:186 TS2345 — _api tsc clean_
- **~L472** — lineup-notification.service.private-visibility.spec TS2556 — _file deleted + tsc clean_
- **~L593** — NUL-byte header-injection guard regex broken — _impl regex now /[\r\n\x00]/ at test-plan.ts:149_
- **~L609** — test_task_list.sh 12 of 13 fail on main — _now 15 pass 0 fail; fixed in #884 (ROK-1356..1361)_
- **~L619** — env-spin failure rollback leaves orphaned PG container — _EXIT trap docker rm PG+APP added (ROK-1357, env-spin:35-73)_
- **~L638** — .deployed_sha never written by deploy script — _deploy.sh:47 now ssh-writes /srv/rl-infra/.deployed_sha_
- **~L641** — traefik conf.d perms not persisted in provisioning — _deploy.sh:37-38 chgrp rl-fleet + chmod 2775_
- **~L735** — rl-infra VM missing RL_ENV_JWT_SECRET blocks sync_settings/admin-seed — _doc self-reports resolved at L766 (sync_settings ok + admin@local seeded on retry)_
- **~L753** — orchestrator lease-status AC9 `timeout: command not found` on macOS — _test now wraps with `rl_timeout` helper (lease-status.test.sh:139), bare `timeout` gone_
- **~L767** — fleet Playwright --only-e2e 401 admin@local random pw — _ROK-1368 commit 61722b66 threads re-seeded ADMIN_PASSWORD into rl_validate_ci --only-e2e_
- **~L781** — env_spin aborted leaving healthy-but-unreachable env — _resolution L787 + PR #880 (85782bd7): exec 9> guards, idempotent route fail-loud_
- **~L782** — Mutagen sync-probe re-wedge (sentinel-deletion race) — _resolution L788 + PR #880; sync-guard.ts finally-flush block (L332/L340) present_
- **~L799** — rl_env_sync_from_local "public schema is empty" wrong probe — _regression test added: sync-local-to-env-host-resolve.test.sh covers exact exit-3 host-resolve bug_
- **~L800** — fleet slot route file stale blocks deploy/dead-ends slot host — _PR #877 (ec0aac82) TTL-reap removes env Traefik route file; PR #880 safe deploy_
- **~L809** — runner missing tools/test-bot deps for Discord smoke — _ROK-1359 shipped PR #884 (7827627f)_
- **~L818** — fleet env Discord OAuth internal-hostname redirect — _already FIXED; env-spin CLIENT_URL committed, verified_
- **~L842** — rl_env_sync_from_local false empty-schema probe (ROK-1358) — _shipped PR #884_
- **~L843** — runner test-bot deps tracked (ROK-1359) — _shipped PR #884_
- **~L844** — mcp-rl-fleet NUL-byte/regex guards (ROK-1360) — _shipped PR #884_
- **~L845** — orchestrator macOS/BSD coreutils failures (ROK-1361) — _shipped PR #884_
- **~L869** — rl_test_plan_wait/rl_task_wait long-block visibility hang — _ROK-1362 bounded waits + 120s cap, PR #889_
- **~L907** — exec.ts:854 worktreePathSchema .refine TS2345/TS7006 — _`npx tsc --noEmit` in tools/mcp-rl-fleet now EXIT=0_
- **~L917** — LineupPhaseQueueService registered in two modules — _single provider exported by LineupsModule, standalone-poll imports it (ROK-1206)_
- **~L954** — test-plan-v2 executeWait directory-watch assertion fails — _`vitest run test-plan-v2.spec.ts` now 14/14 pass_

### Low confidence (28)

- **~L167** — use-standalone-poll queryKey mismatch ['scheduling-banner'] — _line 29 now uses correct `['scheduling','banner']` tuple; PR #910 "fix(low-hanging): banner refresh"_
- **~L224** — seed-igdb per-row rowExistsWithIgdbId extra DB roundtrip — _function gone, replaced by batched IN query (only in comments now)_
- **~L280** — lineups-matching `decided` preserved-list comment clarified — _comment rewritten at lineups-matching.helpers.ts:36 — now frames `decided` as the lineup-row status, preserved list is only `scheduled`/`archived`; file refactored (mtime May 31)_
- **~L284** — stillWaitingOnVoters frontend drops optional-chaining — _page now reads `lineup.stillWaitingOnVoters.length > 0` directly (lineup-detail-page.tsx:234), contract field non-optional (lineup.schema.ts:260) — exactly the suggested fix_
- **~L396** — 0142_snapshot.json missing community_lineup_user_submissions — _snapshot now contains the table (8 refs in api/src/drizzle/migrations/meta/0142_snapshot.json)_
- **~L401** — feedback.controller attachSlowQueryContext untrimmed clientLogs — _now passes `truncatedClientLogs` (feedback.controller.ts), shipped via PR #910_
- **~L409** — chrome-mcp env_lock_release no-op after deploy re-anchor (doc note) — _root-fixed by ROK-1318 PR#843, release now matches via agent_id_
- **~L489** — vitest MSW leak invite/admin-dashboard cross-suite flake — _setup.ts has afterEach resetHandlers + admin-dashboard-page.test.tsx deleted + invite passes 18/18_
- **~L536** — use-lineup-submit:193 isError bare-expect race flake — _waitFor wrap applied on origin/main (L196) with TECH-DEBT-2026-05-19 comment_
- **~L563** — lease-status.test.sh bare `timeout` not on macOS — _now uses rl_timeout wrapper (test_helpers.sh:137, gtimeout-detect + pure-bash fallback) + inotifywait skip guard_
- **~L568** — NUL-byte header test unsatisfiable via process.env — _test now calls validateCurlHeaderValueForTest helper directly; tsc-clean_
- **~L571** — env-destroy AC12 flock test fails on macOS — _test rewritten to force flock-failure deterministically on both platforms_
- **~L580** — heartbeat-emitter timeout test fails on macOS — _now uses portable rl_timeout shim instead of bare timeout_
- **~L588** — rl validate-ci doesn't auto-release slot on exit — _--release-on-exit opt-in flag added (ROK-1336, rl:641-659)_
- **~L604** — itadLowestPrice missing-in-type TS errors slip CI gate — _tsc-clean + field now present in spec (lines 60,100)_
- **~L629** — task-inspect/infra-logs inline execFileP duplication — _both now import execFileP from '../exec.js'_
- **~L632** — env-spin idempotent path missing LAST_PHASE markers — _three markers present (env-spin:319,396,456), commit efc42850_
- **~L651** — test-plan.spec.ts AC6 NUL-byte header test — _rewritten to validateCurlHeaderValueForTest().toThrow, fn correct — PR #833/#842_
- **~L705** — community-lineup banner-visibility smoke flake — _tests renamed + beforeEach lineup precondition + deep-link — PR #900 smoke-flake hardening_
- **~L707** — bootstrap-admin FK violation on community_lineups — _idempotent upsert (onConflictDoUpdate) — PR #863 on main_
- **~L792** — no-sleep-lint false-positive on JSDoc `sleep()` comment — _lint now exits 0 ("No sleep() calls found"); offending comment gone from lineup-abort.test.ts_
- **~L793** — test-bot tsc `Cannot find module @discordjs/voice` — _dep added: tools/test-bot/package.json:14 `@discordjs/voice ^0.19.2`_
- **~L810** — gc-sweeper auto-release strands child envs — _ROK-1357 shipped PR #884_
- **~L830** — seed-fixture-user not clearing deactivated_at — _already FIXED in-branch (deactivatedAt: null)_
- **~L887** — lineup-abort no-sleep lint false-positive on `sleep()` comment — _comment reworded to "never a fixed delay" — sleep() token gone_
- **~L900** — api/package.json test:integration uses dead --testPathPattern flag — _script now `jest --config ./jest.integration.config.js --runInBand`, flag removed_
- **~L901** — test-bot voice.ts TS2307 @discordjs/voice not installed — _package now present in tools/test-bot/node_modules (env/install issue)_
- **~L909** — no GitHub job runs mcp-rl-fleet Vitest suite — _ci.yml:197 tools-tests job runs `npm test -w @raid-ledger/mcp-rl-fleet`_

### Nit — comment rewrites / lint-nit fixes / already-struck (6)

- **~L92** — env-lock mid-run preemption / heartbeat re-anchor (ROK-1209) — _entry's own inline note: fixed by ROK-1209 "re-anchor deploy_dev.sh lease to API PID" commit_
- **~L146** — games-dedup doc comment references nonexistent extendedBlastRadiusKeys — _`grep extendedBlastRadiusKeys` GONE everywhere; comment rewritten to lockstep buildDirectCountQueries note_
- **~L232** — no-sleep-lint flags lineup-abort.test.ts:26 comment — _comment rewritten + lint clean, PR #910 (6098fffc)_
- **~L237** — no-sleep-lint nit duplicate (still-unresolved note) — _same fix, lint now "OK: No sleep() calls", PR #910_
- **~L316** — games-dedup-union-find numeric-sort comment added — _explanatory comment present at games-dedup-union-find.helpers.ts:172 (`// igdb/steam ids are numeric strings → sort numerically; names → lex.`)_
- **~L902** — dup lineup-abort.test.ts:26 no-sleep lint comment — _same fix as L887 — `sleep(` token removed from comment_

### 2026-06-28 — CI Docker-registry flake (surfaced during ROK-1352 PR #922)

- ~~**[med — CI infra flake, NOT code]** GitHub Actions jobs that start service containers (`integration-test-shard`, `smoke-test-shard`, `discord-smoke`) intermittently fail at the Docker image-pull step before any test runs: `Error response from daemon: Get "https://registry-1.docker.io/v2/": context deadline exceeded` → `##[error]Docker pull failed with exit code 1`. Hit `integration-test-shard (1/3, 1)` on PR #922 (run 28329347625), failing the job in ~1m; recovered on `gh run rerun --failed`. Cause is Docker Hub registry timeout / unauthenticated-pull rate limiting on the runner — unrelated to any branch diff, but it red-flags PRs and forces manual re-runs (defeats walk-away auto-merge).~~ **RESOLVED 2026-06-28 (PR #925 + #926):** mirror workflow (`mirror-ci-images.yml`) pushes `pgvector`/`redis` to `ghcr.io/sjdodge123/rl-ci-*`; all 6 service containers now pull from GHCR via `GITHUB_TOKEN` `credentials` (no Docker Hub rate limit). Validated green on push-to-main.

### 2026-06-28 — chore/test-infra-batch (surfaced during ROK-1145 MCP-server refactor)

- **[low — pre-existing, NOT this branch; tsc not CI-gated for tools/]** The `tools/` MCP-server workspaces carry TypeScript errors that NO gate catches (validate-ci.sh + GitHub run `tsc` only on `api`/`web`; tools packages run via `tsx` and their `lint` script just echoes). Confirmed present on `origin/main` before the ROK-1145 work (relocated verbatim, net count unchanged): (1) `tools/mcp-env/src/tools/service-status.ts:160-164` — 6× TS2339 `Property queue/free/holder does not exist` (a parse-result union isn't narrowed before property access); (2) `tools/mcp-discord/src/register-tools.ts` (was `index.ts`) — 7× TS2769 overload mismatch on `server.tool()` registrations (the `withCdpErrorHandling` return type lacks the SDK `CallToolResult` `[x:string]:unknown` index signature); (3) `tools/mcp-discord/src/index.test.ts:183` TS2339 `isError` + `cdp.ts` playwright/implicit-any errors. None break CI today. Suggested: if the operator wants tools type-safety gated, add a `tsc --noEmit` per tools workspace to the new `tools-test-shard` matrix — but that would surface these as failures, so fix (1)+(2) first (narrow the union in service-status; widen the tool-handler return type or cast to `CallToolResult`). Deferred — documenting per the STRICT pre-existing-failure rule; not fixing in this batch (out of scope, would expand the diff).

### 2026-07-01 — fix/rok-1367-raw-fetch-401-refresh (surfaced during full `vitest run --coverage` gate)

- **[med — pre-existing load flake, NOT this branch; already tracked ROK-1285/ROK-1290]** `web/src/hooks/use-ai-suggestions.test.ts` — one of the real-timer poll-cap tests (`reports pollExhausted=true …` / `resets the poll budget …`, 55s–70s `waitFor` budgets) times out under full-coverage load: `Error: Test timed out in 70000ms` at `Timeout.checkRealTimersCallback` (@testing-library/dom wait-for). Reproduced twice this session (full suite: that file ran 644s; isolated re-run: 1024s for 6 tests — an absurd duration proving CPU/event-loop starvation, not logic). File is UNCHANGED by this branch (`git diff --name-only origin/main..HEAD` does not list it) and this branch's `getAiSuggestions` change is byte-identical for the 200/503 payloads these poll tests use (no 401 → no refresh probe). Passed green when run in a lighter batch earlier the same session. This is the known ROK-1285 (raised `asyncUtilTimeout` for this exact file) / ROK-1290 deep-carrier poll flake — GitHub CI runs sharded + randomized so it lands in a lighter shard and passes. Suggested: convert the two poll-cap tests to fake timers (`vi.useFakeTimers()` + `advanceTimersByTimeAsync`) so the 45s/55s budgets don't depend on wall-clock under load — tracked in ROK-1285/ROK-1290. Not fixing here (out of scope, test not touched by this story).

### 2026-07-02 — fix/rok-1370-poll-start-rescheduling (surfaced during fleet validate-ci)

- **[med]** `api/**` (dozens of files, e.g. `api/src/auth/auth.controller.ts`, `api/src/admin/demo-data-gen-taste-profile.ts`, `api/src/discord-bot/services/discord-embed.factory.ts`) — `npm run prettier:check -w api` fails on a FRESH `npm ci` install: PR #940 (2026-06-30 combined dependency bumps) bumped prettier 3.8.4 → 3.9.3 in the lockfile and 3.9.3 reformats existing files (union-type wrapping etc.). Pre-existing on origin/main: main-repo checkout with pre-#940 node_modules (prettier 3.8.4) passes with 0 warnings on identical file content; the fleet runner passed earlier tonight on cached node_modules and failed after a force-resync triggered fresh `npm ci`. GitHub CI never catches it because the lint job runs `eslint --fix` (silently reformats its own checkout, exits 0) — only `validate-ci.sh`'s `prettier:check` is strict. Blocks the fleet lint gate for every branch validated on a freshly-provisioned runner from now on.
  Suggested: one-shot `npx prettier --write "{src,apps,libs,test}/**/*.ts"` in api (+ web check) as a standalone `chore(format):` PR, immediately after the current in-flight PRs land to minimize rebase conflicts.

### 2026-07-02 — fix/rok-1391-se-lockin-race (surfaced during local Discord-smoke AC gate, overnight batch)

- **[med — pre-existing smoke-suite design, NOT this branch]** `tools/test-bot/src/smoke/` flow category + `/admin/test/{enable,disable}-scheduled-events` — flow tests run at `SMOKE_CONCURRENCY=5` (config.ts:31) against ONE global `scheduledEventsEnabled` toggle; every SE test's `finally` disables it, killing sibling tests' in-flight SE creates (create path reads the toggle at execution). Verbatim log signature: interleaved `POST /admin/test/disable-scheduled-events` + zero `DISCORD | scheduledEvents.create` for the victim's event. Historically masked by the dev reconciler healing missing SEs: when the `ScheduledEventReconciliation_reconcileMissing` cron_jobs row is ABSENT (fresh DB — e.g. CI shards), smoke's `pauseReconciliation` cannot stick (`executeWithTracking` runs untracked, cron-job.service.ts:210-213) and the reconciler heals every ~70s; tonight's dev DB HAS the row → pause sticks → 3-4 SE-dependent flow tests (ROK-944 completion, completion-future, ROK-1347 reconciliation, ROK-1370 pair) fail per parallel run on ANY branch. Confirmed pre-existing: completion-future + 1347-reconciliation PASS serially on this branch. Suggested: per-test SE-toggle scoping (e.g. fixture refcount so disable only lands when no sibling holds enable), or serialize the SE-dependent subset like the `voice` category.
- **[med — pre-existing, NOT this branch]** serial workaround is non-viable: `SMOKE_CONCURRENCY=1 SMOKE_CATEGORY=flow` takes ~50 min → smoke ApiClient JWT expires mid-run (last 4 tests fail `POST … 401`) and Discord rate-limit debt accumulates (guild `scheduledEvents.fetch` 118ms → 807ms → >5s across runs; one undici `Connect Timeout … 162.159.136.232:443`). Suggested: token refresh in the smoke ApiClient (or re-login per test file) before any serialization work.
- **[low — pre-existing, already noted in session notes 2026-07-02]** `Multiple users sign up -> roster updates correctly` — failed all 3 modes tonight (parallel 61s pollForEmbed timeout ×1, serial 82s undici connect-timeout variant ×1; also 2/2 on 2026-07-02 pre-batch). Untouched by this branch. Suggested: next flake pass; likely embed-sync poll budget vs Discord latency, not logic.
- **[high — pre-existing, MAIN-reproduced 2026-07-02, NOT this branch]** reschedule-poll lock-in embed re-render is carried by AMBIENT embed-sync traffic, not the lock-in itself — `POST /scheduling-polls/:id/complete` → UPDATED re-emit correctly recreates the SE (fresh time verified) but produces NO embed re-render: `discord_event_messages.embed_state` stays `rescheduling` until some OTHER event's signup/edit enqueues an embed sync whose level-triggered processor heals it. Deterministic under isolation: `SMOKE_NAME_FILTER="ROK-1370" SMOKE_CONCURRENCY=1` fails `waitForLiveEmbed` on EVERY attempt on BOTH `fix/rok-1391-se-lockin-race` AND `main@89529dfb` (api.log evidence: 1391 branch event 82 4:34:53→4:35:54 stuck; main event 82 5:13:47→5:29:25 stuck, retries included). Masked in CI + parallel local runs by sibling-test embed traffic — which is why #955's smoke passed. User impact: a real lock-in on a quiet server leaves the event card stuck on "RESCHEDULING" until the next unrelated embed touch (or ~15-min reconcile-adjacent traffic). Suggested: make the lock-in path enqueue an explicit embed sync for the linked event (mirror the RESCHEDULING teardown's `updateEmbedState` with a symmetric restore), or have `completeStandalonePoll` enqueue `embed-sync` directly. Candidate Linear story drafted in the 2026-07-03 morning report; operator files.

### 2026-07-02 — fix/rok-1389-series-voice-channel + fix/rok-1390-quickplay-fallback (reviewer LOWs, overnight batch)

- **[low — pre-existing, outside ROK-1389's 8-site inventory]** `api/src/events/pug-invite.service.ts:151,253` + `api/src/discord-bot/services/voice-attendance-snapshot.helpers.ts` snapshot path — still call the raw 3-arg `resolveVoiceChannelForEvent`/tier resolver without the ROK-1389 override guard, so a PUG-invite Join-Voice link and the start-of-event snapshot don't honor a voice `notificationChannelOverride` while reminders/website/embeds/SE now do. Behavior unchanged from pre-1389; operator to decide whether to fold PUG-invite into the unification. Suggested: one-line delegation each to `resolveVoiceChannelHonoringOverride` in a follow-up.
- **[low]** `api/src/discord-bot/listeners/voice-state.listener.ts:128` — the ROK-1389 WARN-only binding heal report re-fires on every Discord READY (gateway reconnects), not once per boot. Harmless (read-only WARNs) but noisy on flappy connections. Suggested: `firstReadyOnly` gate flag.

### 2026-07-06 — dispatch batch ROK-1365 + ROK-1392 (surfaced during dispatch)

- **[med — pre-existing CI coverage gap, NOT this branch]** The GitHub `discord-smoke` job's path filter (`.github/workflows/*` Discord-smoke `paths:` set — discord-bot/**, notifications/**, events/signups*, events/event-lifecycle*, admin/demo-test*, tools/test-bot/src/smoke/**) and the mirrored local detector in `scripts/validate-ci.sh` do NOT include `api/src/lineups/standalone-poll/**`. But standalone-poll enqueues an embed-sync at reschedule-poll lock-in (ROK-1392) → it directly drives Discord embed behavior. Net: an embed-affecting change under `lineups/standalone-poll/**` gets its GitHub `discord-smoke` job path-filtered to SKIPPED, so GitHub provides NO Discord behavioral coverage for it. ROK-1392 was safe because the companion-bot smoke was run LOCALLY (GREEN ×2, the `ROK-1370: lock-in restores the live embed` acceptance test), but a future embed-affecting lineup change could merge with zero Discord CI signal. Suggested: add `api/src/lineups/standalone-poll/**` (and audit sibling lineup embed paths) to both the GitHub discord-smoke `paths:` filter and the `validate-ci.sh` Discord-smoke trigger list, so embed-affecting lineup changes auto-trigger the Discord smoke on push.

### 2026-07-06 — feat/rok-313 (surfaced during ROK-313 full local gate)

- **[med — pre-existing local-env, NOT this branch]** `npm run test:integration -w api` (the FULL local integration suite) SIGABRTs (exit 134) at global setup — 0 test suites run, native V8/libuv crash dump, no ROK-313 code loaded yet. Reproduces run-alone (not concurrency). ROK-313's own integration specs pass 9/9 in isolation (`test:integration -- users-moderation.integration`). Class of the known socket-carrier / OOM local flakiness (`reference_bullmq_ioredis_test_carrier`, `reference_integration_404_socket_carrier`), but here the crash is at Testcontainers global setup rather than mid-suite. GitHub CI runs integration SHARDED so it never hits the monolithic single-process accumulation. Suggested: shard the local `test:integration` invocation (or raise `--max-old-space-size` in the jest.integration runner) so the full suite is runnable locally; low priority since CI shards.
- **[low — pre-existing local-env, NOT this branch]** `./scripts/validate-migrations.sh` fails locally at migration `0000` with `psql: connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed: FATAL: database "raid_ledger" does not exist` — the script's psql hits the laptop's default local socket (PGHOST/PGDATABASE from the shell profile) instead of the temp container's TCP port. Passes cleanly on the fleet runner (`Applied 153 migration(s) cleanly`). Local-shell-env artifact, not a script/migration bug. Suggested: `validate-migrations.sh` should `unset PGHOST PGDATABASE PGPORT` (or set them explicitly to the temp container) before its psql loop so a developer's ambient PG env can't hijack the connection.
- **[note]** The prettier 3.9.3 vs 3.8.4 (#940) drift documented 2026-07-02 recurred on this branch's fresh-`npm install` worktree (local `prettier:check` flags ~20 pre-existing files; api-workspace prettier is the CI-authoritative context). ROK-313's own api files were formatted to 3.9.3 in-branch. No new action — the standing `chore(format):` suggestion stands.

### 2026-07-13 — fix/scheduling-poll-voter-membership (surfaced during adversarial review of the voter-membership fix)

- **[low]** `api/src/lineups/scheduling/scheduling.controller.ts:152` — `DELETE .../votes` (retractAllVotes) and the poll-page GET remain ungated for private lineups after the vote/suggest surface gained `assertCallerMayVote` (fixed in this same branch — Codex security P2). Retract only deletes the caller's own votes (self-cleanup, harmless) and the GET is read-only, but for full parity with nominate/game-vote the whole private-lineup scheduling surface could run the participation check. Suggested: add `assertCallerMayVote` to retract + consider gating `getSchedulePoll` for private lineups.
- **[low]** `api/src/lineups/lineups-bandwagon.helpers.ts:71-91` `checkAutoPromote` — schedule-vote enrollment grows the member set of `suggested` matches but never triggers the promote check itself; the NEXT bandwagon join computes its threshold percentage against the enlarged member count and can promote earlier than the pre-fix math would have. Behaviorally defensible (slot votes are a demand signal) but undocumented. Suggested: either call the promote check after vote-enrollment on `suggested` matches too (consistent), or note the semantic in the helper docstring.
- **[low]** `api/src/lineups/standalone-poll/standalone-poll-reminder.service.ts:159-173` + `lineup-reminder-target.helpers.ts` — sticky vote-enrollment means a user who un-votes (or retracts all votes) stays a match member and becomes a standing "you haven't voted" reminder-cron target with no leave-poll mechanism. Matches from-match semantics (member sets never shrink) but is new for standalone polls. Suggested: consider excluding users whose LAST action was retract-all, or add an explicit leave/mute affordance if operators report DM complaints.

### 2026-07-15 — fix/running-late-followups (surfaced during high-effort code review of the running-late/delay follow-ups)

- **[low — defense in depth]** `formatEpoch` (`api/src/notifications/format-helpers.ts:19-29`) renders ANY residual `<t:` token that reaches `buildPlaintextContent` in the server process TZ (no `timeZone` option), and `stripDiscordMarkup` (:39) discards the style char so `:R` (relative "in N hours") collapses to an absolute duplicate. Even after the two fixes above, any future notification type that stores `<t:>` markup inherits the bug. Suggested: thread the recipient timezone into the DM job data and have `formatEpoch` render in it (and render `:R` as a real relative delta). Note: `tools/test-bot/src/helpers/discord-markup.ts` mirrors `format-helpers.ts` and must stay in sync.
- **[low — product/observability]** The new `running_late` notification type is default-on across inApp/push/discord and has no toggle row in the web `NotificationPreferencesSection` (`web/src/components/profile/NotificationPreferencesSection.tsx`) — same as the shipped `event_delayed` precedent, so users cannot opt out of either. It also sets no `expiresAt`, so a late notice lingers in the in-app list after the event. Suggested (if operators report noise): add prefs rows for `event_delayed` + `running_late` and set `expiresAt = event end` on both.

### 2026-07-15 — rok-1394-nonseries-zero-presence-guard (surfaced during fleet validate-ci run on runner-1)

- **[med]** Fleet runners run validate-ci against stale `/workspace/node_modules` relative to the Mutagen-synced `package-lock.json`, producing phantom failures — missing newly-added deps at build; version-drifted eslint/prettier reformatting files unrelated to the branch diff. Flagged across 2026-06-07, 2026-07-15: rok-1353-refresh-tokens, rok-1394-nonseries-zero-presence-guard. Instances: `scripts/validate-ci.sh::run_lint` (mutating `eslint --fix` before `prettier:check` — check-only-gate half shipped with ROK-1404 in fix/batch-2026-07-17), runner `/workspace/node_modules` never refreshed on lockfile change (ROK-1404 AC3, open). Last surfaced 2026-07-17 (fleet tasks 88dd18de5e4c/0c0e69d49eff, per the fix/batch-2026-07-17 entry below). Tracked in ROK-1404 (In Progress). Consolidated 2026-07-17.
  Suggested: have `rl_validate_ci` run `npm ci` (workspace root + tools/test-bot) when the synced `package-lock.json` hash differs from the runner's last-install stamp; keep the lint gate check-only.

### 2026-07-17 — fix/batch-2026-07-17 (PR pending)

- **[RESOLVED, was med]** `.github/workflows/discord-smoke.yml` + `scripts/validate-ci.sh:305` discord-smoke filter gap for `api/src/lineups/standalone-poll/**` (2026-07-06 entry) — both filter locations extended in this batch; CLAUDE.md trigger lists updated to match.
- **[RESOLVED, was low]** `scripts/validate-migrations.sh` "database raid_ledger does not exist" startup race (2026-07-06 entry) — **diagnosis correction:** the suggested `unset PGHOST/PGDATABASE/PGPORT` was a misdiagnosis (psql runs INSIDE the container via docker exec; laptop env can't hijack it). Actual mechanism: `pg_isready` succeeds against initdb's temporary socket-only bootstrap server before `POSTGRES_DB` exists. Fixed in this batch: readiness poll now runs `SELECT 1` against `raid_ledger` over TCP (`-h 127.0.0.1`) — the bootstrap server is socket-only, so TCP success guarantees the final server.
- **[half-RESOLVED, was med]** 2026-07-15 `run_lint --fix` order-dependence entry — the check-only gate half shipped in this batch (ROK-1404), including the forced one-shot prettier 3.9.4 reformat of 32 files (laptop node_modules carried prettier 3.8.4 vs lockfile 3.9.4 — the drift can point EITHER direction: this time the runner was lockfile-correct and the laptop was stale). The fleet lockfile-exact-refresh guard half (ROK-1404 AC3) remains open; this batch hit it live (fleet tasks 88dd18de5e4c/0c0e69d49eff: 42 phantom prettier errors until `npm ci` on both ends).
- **[med]** `tools/mcp-rl-fleet` `rl_env_spin` — hung >30 min with NO response, NO task record (`rl_task_list` empty of it), and NO env-registry entry; the MCP idle timeout (1899s) aborted the call and the env never materialized. Violates the ROK-1362 every-blocking-wait-caps-at-120s design — spin appears to run its build/deploy synchronously inside the tool handler instead of dispatching a task like `rl_env_deploy` does. Bit this batch's Chrome-MCP gate (fell back to local deploy_dev). ROK-1338 no-SSH umbrella adjacent.
  Suggested: make `rl_env_spin` async-by-default returning `{task_id}` (laptop or VM task), polled via `rl_task_status` like deploy/clone-prod.

### 2026-07-18 — fix/batch-2026-07-17-b (PR #994)

- **[RESOLVED, was med]** `tools/mcp-rl-fleet/src/tools/__tests__/test-plan-v2.spec.ts` "ROK-1337 AC2 — executeWait watches the slug directory" red (2026-06-08 entry, ~L584) — **diagnosis correction:** neither an fs.watch mock wiring issue nor a path-normalization regression (the product code watches the correct slug directory). Actual mechanism: `vi.resetModules()` in `beforeEach` wipes the `resolveProxmoxHost` memo, so every test re-paid a REAL `dns.promises.lookup('rl-infra.lan')` inside the AC2 test's 100ms fixed race — runners with >100ms DNS never captured the spawn argv. Fixed in this batch: non-default `RL_PROXMOX_HOST` stub (RFC-5737 address, short-circuits before DNS) + `vi.waitFor` on the capture instead of a fixed timer. 15 consecutive green runs; suite 2.16s → 200ms.
- **[RESOLVED, was high]** `scripts/smoke/profile-avatar-prefs.smoke.spec.ts` timezone `toHaveValue('auto')` deterministic failure on cloned-prod/long-lived DBs (2026-06-18 entry, ~L574) — fixed in this batch: the spec now resets the admin timezone pref to `'auto'` through the app's own `PATCH /users/me/preferences` + reloads before asserting (assertion preserved, not weakened). Green against the live dev env (13 passed) and inside the full 646-test suite run.

### 2026-07-24 — rok-1418-suppression-scoping (surfaced during B1)

Deferred findings from the ROK-1418 suppression-scoping + bounded-extension fix (D4). All are out of scope for this `--static` PR (spec Deliverable 8); recorded here so B3/B4/B5 and the operator triage don't rediscover them. None is a failure on `origin/main` — they are known asymmetries the D4 change touches but deliberately does not resolve.

- **[med]** Duplicate suppression check per join — `api/src/discord-bot/listeners/voice-state-join.handlers.ts:77` (`handleGameBindingJoin`) calls `adHocEventService.trySuppressForScheduled(...)` and, when it returns false, `checkGameBindingThreshold` → `handleVoiceJoin` (`api/src/discord-bot/services/ad-hoc-event.service.ts:105`) runs the same suppression query a second time. Both queries + the (now bounded) `extended_until` write execute twice on every game-binding join. Not deleted here: the listener call is B3/B4 blast radius (ROK-1415 rewrites `checkGameBindingThreshold`). Suggested: after B3/B4 land, have the listener pass its suppression result through to `handleVoiceJoin` (or drop the listener-side check) so the query runs once.
- **[med]** Suppression-window extension skips the sync fan-out that the scheduled auto-extend path performs. `extendScheduledEventWindow` (`api/src/discord-bot/services/ad-hoc-suppression.helpers.ts`) writes only `extended_until` + `updated_at`, whereas `event-auto-extend.service.ts:161-180` also `eventCache.invalidate()`/`refresh()`, `adHocGateway.emitEndTimeExtended()`, and `scheduledEventService.updateEndTime()` (Discord scheduled-event end sync). A suppressed-join extension therefore leaves the event cache, the live web clients, and the Discord event end time stale until the next natural refresh. Suggested: extract the auto-extend fan-out into a shared helper and call it from the suppression write, or emit `emitEndTimeExtended` from the suppression path at minimum.
- **[low]** Dead OR-terms retained in the suppression clause. `buildBindingClause` still carries `events.channel_binding_id = <bindingId>` and (for the game path) the sibling subquery, but for `is_ad_hoc = false` rows `events.channel_binding_id` is only ever written by the ad-hoc INSERT, so both channel-based terms are structurally unreachable for scheduled events. Kept byte-identical on purpose (the intended landing zone if scheduled events ever gain a binding link — see spec D4(a)), not deleted. Suggested: leave until scheduled events gain a `channel_binding_id`, then either wire it or delete the terms with a test.
- **[low]** `extractGameIds` collapses to "all games" on any null-game or non-monitor binding. `api/src/discord-bot/services/voice-attendance-flush.helpers.ts:260-268` returns `null` (meaning "count every member regardless of game") as soon as one binding on the channel is not `game-voice-monitor` or has `gameId === null`. On a mixed channel (e.g. a monitor + a general-lobby, the permitted B2 shape) this silently widens attendance/flush counting to all games. Orthogonal to D4's suppression fix but adjacent in the same multi-binding topology. Suggested: decide whether a null-game monitor should widen the whole channel or be skipped, and encode it explicitly.
- **[low]** Multi-binding runtime asymmetries (batch plan flagged as deferred — the "§8" reference in spec D4 Deliverable 8 points at the resolved-questions section; the enumeration lives in the batch synthesis). Concrete instances the D4 change exposes but does not fix: (a) the anchored game clause scopes by the *querying* channelId while the sibling subquery matches *any* sibling binding on the physical channel, so game-match and sibling-match use different channel semantics; (b) `matchedBy` is a best-effort SQL `CASE` and can mislabel when more than one OR-term matches; (c) suppression runs once per binding, so an N-monitor channel evaluates the same scheduled event N times (the SQL monotonic guard dedups only the *write*, not the reads/logs). Suggested: revisit as a group when the multi-binding admin work (ROK-1416/B5) settles the intended per-channel topology.
