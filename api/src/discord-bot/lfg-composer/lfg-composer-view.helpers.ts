/**
 * ROK-1685 AC3/AC4 — WIP stub: the pinned card's `View games ↗` handler.
 */
import type { ButtonInteraction } from 'discord.js';
import type { ComposerLinkDeps } from './lfg-composer-link.helpers';

/** WIP — not implemented yet. */
export async function viewComposerGames(
  _deps: ComposerLinkDeps,
  _interaction: ButtonInteraction,
): Promise<void> {}
